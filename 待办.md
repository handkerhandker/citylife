# 待办
更新：2026-08-08 · 代码基线：**v38**（第 23／24 单游戏代码零改动、不涨号；**第 25 单 v34**；**第 26 单 v35**（动了 SIM 块新增 CATCHUP 段，`Sim.step` 逐字未动、世界指纹 `d19035af…` 逐字节不变）；**第 27 单 v36**（动了 `decide()` 的闲聊分支与兜底文案层，rng 流位移，世界指纹 `d19035af…` → `2d266238…`——任务书已授权，见该单交付件第九章）。**v34／v35／v36 由第 28 单补正**，同单立涨号铁律（宪法第 27 条，门禁＝`versiongate.js`）。**第 29 单 v37**（只动 DOM 层的落字时机与门禁，世界指纹 `2d266238…` 逐字节不变）；**第 30 单 v38**（只动 DOM 层的回城呈现与门禁，**未动 SIM 块**，世界指纹 `2d266238…` 与门禁指纹 `6c8f768a…` 均逐字节不变）。）

## 下一单候选（按优先级）
> **顺序经决策者裁定改写（第 19 单）**：①每日剪辑 → ②离线追帧 → ③目标系统 → ④视觉层二期。
> 原第 1、2 条（视觉层二期室内／室外篇）整体后移；原第 5 条「回城日报＋离线追帧」按裁定拆成①②两条独立单。
> **第 26 单裁定四追加（决策者裁定，2026-08-06）**：模板兜底文案层提前，即下面的「第 27 单」，排在目标系统之前。

~~**第 27 单 · 模板兜底文案层（已排期，决策者裁定四）**~~ **已完成待验收（第 27 单）**，见下方第 13 条。
承接的两条范围外发现（日记兜底无池、`CHAT_LINES` 用 `pick` 而非 `pickV`）**均已销账**。


1. ~~**每日剪辑**（原第 5 条前半，升为首位）~~ **已完成并结项（第 21 单，v32）**。
   一期（选材）落地：只做「挑」不做「写」，一个字没过 AI；判据＝决策者裁定的
   「谁今天最不像平常的自己」，未改一字。交付件＝`docs/交付/第21单-每日剪辑一期.md`（60 天选材全表为主证据）。
   **二期（AI 润色）已由决策者裁定取消**——短板不在文笔在素材，给本来没事的日子配好文笔，
   是第 18 单「素材越稀薄模型越硬凑」的翻版。**故本条不再挂账，剪辑层到此为止。**
   要让剪辑更有戏，该动的是模拟侧（见本页第 10 条「作息人格」与下方第 21 单范围外发现两条）。
   **下一单候选顺位因此上移：①离线追帧 → ②目标系统 → ③视觉层二期。**
2. ~~**离线追帧**（原第 5 条后半）~~ **一期已完成待验收（第 26 单）**。
   「同步时长不同步钟点」落成 `CATCHUP_MIN_PER_MIN=1`（1 真实分钟＝1 模拟分钟）；
   「封顶两三天（集中可调）」落成 `CATCHUP_MAX_DAYS=3`。
   **封顶不是性能妥协**——实测补 30 天只要 12.5ms（比一帧还短），真正的硬边界是
   `sim30` 只验到 30 天、以及玩家一次看得完几张卡。
   决策者裁定「离开期间一次 AI 都不调、回来也不补生成」已逐条落成可计数断言。
   回城呈现取的是**剪辑内容**，不是另做一张日报——第 21 单结项裁定「玩家该看的是剪辑」。
   **本行由第 30 单改写**：原文写「跨过至少一个『夜深了』即直接推到眼前」，那条判据有 bug 且形态已换。
   现状＝**回城弹窗**（第 30 单，v38）：判据只剩「补算期间真出了新卡」，有卡就把最新那张整张摆进弹窗，
   没卡就给这段时间的简略概括；开机一律进现场页，关掉即在现场页。
   交付件＝`docs/交付/第26单-离线追帧一期.md`＋`docs/交付/第30单-回城弹窗.md`。
   **本条未做的两半（原文列的四样里剩下两样，不挂在本单账上）**：
   ①「回城痕迹」里的**未读短信／日记惦念**——本单只把「临走前发的那条短信」接上了
   （补一条「已读不回」），没有做「住户主动惦记你」这一类新机制；
   ②「出门旅行」按钮与住户来信（接写信）——属新玩法，未开工。
3. 目标系统·规则侧全套（第 10 单）：目标状态字段、目标→行为旋钮编译层、前提检查器（v1 谓词仅限现存变量）、特质驱动的模板目标（硬口径：一半为无用小事，防全员内卷；目标外显于角色卡）；零 AI、确定性可测；前提破裂外显为日志事件并喂导演层。
4. 目标系统·换脑（第 11 单）：周目标四人打包生成、前提破裂单人重开一次决策（每人每日上限 1）、周记忆压缩同车；硬口径：玩家短信为周记忆压缩与目标生成的正式输入（影子机制）。
5. 视觉层二期·室内篇（原第 1 条，后移）：按《云港建筑规范》放样——便利店 12×10、公司 10×9 真瓷砖（坐标骨架已随 v25 甲案就位，本单补真瓷砖与室内通勤实走），素材一律走流水线机器产出、随 PR 入仓自动上线，决策者零手工零上传。（公寓两道内连门洞已于第 16 单补开，见《云港城市总规》五.1，本条不再挂账。）
6. 视觉层二期·室外篇（原第 2 条，后移）：街道／广场／江面／岸线铺装，按《云港城市总规》B 案骨架（幅面 47×28、广场 9×8 列 18–26、T 口列 21–23、岸线行 23、江面行 24–27）与《云港建筑规范》放样；街道件走带外放样（南檐带／北空档柱／地图边缘），开工前先出街道件「家具占位表」实测 Exteriors 真身（规范四.3），与总规二章估值冲突时以真身为准。三期·氛围（雨丝/天色/气泡）随后。
7. 浏览器缓存旧版治本：部署侧缓存策略或页内「有新版」提示（临时靠网址加尾巴）。口径补记：现况**代码与素材各有一套缓存，网址尾巴只管代码**——素材（assets/ 两张成品图）换新后仍可能吃旧缓存，治本方案须两者一并覆盖。
8. README 项目首页化＋验证门徽章。
9. 沉浸测试进行中：决策者日更式试玩，按周回收体感（已读不回比例等一并裁决）。
   **雨密度这一条已结案**——决策者裁定二「接受现状，不动导演层」，理由是角色对雨零反应（`decide()` 不读 `w.weather`），
   调密度改多改少都不影响体验；真正的题目转入「天气回流进行为」，见「已登记（暂不动）」区。
10. ~~**作息人格**~~ **已完成待验收（第 22 单，v33）**。
   原缺口两处（第 16 单补账实测）：①沈小满与陆知秋均无作息类特质、钟点逐分钟相同；②全员活动序列同构。
   **本单治的是①与「日间波动」**：就寝钟点／睡眠时长／饭点／耐饿程度／规律性六项一律由 `RHY_TRAIT`
   按既有特质推导（表外无第二处赋值），起床时刻改为「躺下时刻＋今晚该睡多久」的结果；
   波动全部由世界状态驱动、**零新增 rng**。实测四人起床标准差 0/0/0/0 → 54.0／26.6／10.5／44.2 分，
   四人钟点两两不再相同。交付件＝`docs/交付/第22单-作息人格.md`。
   **缺口②（活动序列同构、夜猫子只是把同一套流程整体后移）本单未做**——本单只动了「几点做」，
   没动「做什么」。要让夜里干点别的，得加新活动词或让特质改变行为分支，建议随目标系统同车。

11. ~~**切页返回后人物直线飞越**~~ **已完成待验收（第 25 单，代码基线 v34——原写「仍 v33」，第 28 单补正）**。
   病根＝`stepDisplay` 的唯一调用点长在 `draw()` 内，而 `draw()` 挂在 `#scr-live.active` 下，
   `updateWalkers`／`Sim.step` 却无条件跑 ⇒ 切页时真实位照走、显示位冻结（最大差 36.35 格），
   切回时被平滑器按直线补掉。治法＝把显示推进提到 `loop()`（`stepAllDisplays`），
   让落差压根不产生；**不新增任何直置通道**，`v.warp` 白名单逐字未动。
   门禁扩「渲染停摆普查」七情形 × 四断言，停摆模型由源码读出，挪回 `draw()` 即变红（已实测）。
   已变基到第 24 单合并后的新 main；相对新 main **世界指纹 `d19035af…`、门禁指纹 `a4d65e6a…`、
   SIM 块 md5 `91e3dadb…` 三个数一个也没动**（门禁指纹相对旧基线的变动全部来自第 24 单换量尺）。
   交付件＝`docs/交付/第25单-切页返回直线飞越排障.md`。
   **范围外发现（本单未做）**：`walkgate.js` 的「复演主循环」写法默认了主循环每一步都无条件执行，
   本单只补了「显示推进」这一步；主循环里其它有条件执行的步骤
   （每拍的各页重渲染 `renderRoles/renderClips/renderPhone/renderStats`、`drainLog`）
   是否也存在同类采样盲区，未逐一排查。

12. ~~**离线追帧一期**~~ **已完成待验收（第 26 单）**，详见上面第 2 条。
   病根不是「世界跑飞了」而是「世界压根没跑」：`Sim.step` 的唯一驱动是 `loop()`、
   `loop()` 的唯一驱动是 rAF，且 `dtFrame=Math.min(0.1,…)` 把恢复时的大 dt 削成
   1 模拟分钟/帧——**靠浏览器补一记大 dt 自动追帧这条路本来就是堵死的**。
   治法＝开机读 `meta.at`（这个数第 6 单起就一直在存，只是从没人拿它推过世界）→
   换算 → `Sim.catchUp` 一口气推上去。零新增存档字段，`SAVE_V` 不动，旧档直接可用。
   门禁三扩：`sim30` 新增「从存档补算 30 天」模式（与从零跑同等对待，双种子 38 条全绿）；
   `harness` 新增 55 条（含出网陷阱实数「补算 30 天出网 0 次」）；
   `walkgate` 新增情形 ⑧ 与「位置判据」（补算不许出现在 `loop()`／`draw()`，须排在 `state.vis` 建表之前）。

   **范围外发现**：
   - ~~日记兜底文案是单条常量、没有池~~ **已销账（第 27 单落地）**：换成逐人四池 `DIARY_FB`
     ＋新抽法 `pickFresh`（最近 `DIARY_FB_RECENT`=10 条不再抽）。离线 3 天双种子实测
     **12 条两两不同**（改前逐字全同 12 条），harness 有断言钉住。
   - ~~`CHAT_LINES` 用 `pick` 而不是 `pickV`~~ **已销账（第 27 单落地）**：整条 `CHAT_LINES` 撤掉，
     换成**逐人开口池 ＋ 逐人接话池**（开口的那句按开口人抽、接话的那句按接话人抽），抽法改 `pickV`。
     离线 3 天双种子实测**逐场不同**（改前最高频一组占 40–60%）。等概率的 `pick` 至此全站无调用点，已一并撤掉。
   - **`w.speed` 随档落盘**，故「暂停中关页」再打开仍是暂停态，补算据此判 0 拍（行为自洽），
     但玩家可能不记得自己按过暂停，回来发现城市没动会困惑。属体感，交决策者。
   - 第 25 单登记的那条**仍未销**：`walkgate` 的「复演主循环」对每拍各页重渲染
     （`renderRoles/renderClips/renderPhone/renderStats`）与 `drainLog` 是否也存在采样盲区，
     本单只补了「补算」这一处，未逐一排查。

13. ~~**模板兜底文案层**~~ **已完成待验收（第 27 单）**，承接第 26 单裁定四排期的两条。
   **口径层比形状层要紧**：旧那句「（写了两行，没写下去，合上了本子。）」是在告诉玩家「这里本该有内容」，
   而离线期间本来就不该有 AI——那不是缺失，**那就是那天的日子**。故新池一律写成真的日记、真的对白，
   不带括号外框、不自述「没写下去」，且**逐人分池**（按 `workKind` 挂，照 `WORK_THOUGHTS` 先例；
   挂 `workKind` 而不挂 `a.id` 是因为存档校验器只白名单校验前者）。
   闲聊这一处拆成**开口池 ＋ 接话池**：一场闲聊上句是开口人说的、下句是接话人说的，
   只按开口人分池的话下句永远带不上接话人的指纹；代价是接话句必须写成**万能承接**（两句独立抽取）。
   **内容只许用结构上必然为真的素材**（一天到了尾声／长期心愿与担忧／职业的日常质地／特质／住处去处／
   身体的一般感觉），**不许提任何具体事件**——与第 18 单「卡上给标签不给事件」同一条口径。
   **抽法分两支**：闲聊照任务书换 `pickV`；日记换不了——`pickV` 是当日去重，而日记一天只抽一次，
   当日去重对它**恒为空转**（4000 颗实测：走 pickV 时 12 条全不重样的比例在任何容量上都到不了 100%），
   故另立 `pickFresh`（`pickV` 的 free 表 ＋「只记最近 keep 条」，记 `w.fbRecent`）。
   **容量按实测定**：日记窗口 `DIARY_FB_RECENT`=10 ＝日志墙（`w.log` 封 400 条）同时挂得住的单人日记条数上限
   （15000 颗 × 30 天实测最大 10）⇒ 墙上看得见的日记一条不重样由构造保证；池 15＝窗口 10＋候选 5
   （候选数照 `pickTopic` 先例）。闲聊照第 13 单口径「≥ 全城单日抽取峰值」定：开口 10／接话 8。
   交付件＝`docs/交付/第27单-兜底文案扩池.md`（含离线 3 天 × 双种子日志墙全文实录，本单验收主证据）。

   **范围外发现**：
   - ~~**`REACT` 五条短信反应仍是「一条短信一条固定文案」**，无池~~ **已裁定不做，维持现状
     （决策者裁定三，2026-08-07）并销账**：它本就是「一条短信对一条反应」的一一对应
     （玩家发「记得吃饭」，TA 的反应就该是那件事），**不是缺陷**；重复只在玩家反复发同一条短信时
     出现，不常见。
   - **闲聊接话句全是敷衍式承接** —— 已按决策者裁定四登记为下一单候选，见本页候选表第 14 条。

   **本单撞出来的既有缺陷（已裁定认可）**：
   `harness`「无话题霸屏」那条断言 `top<=ceil(n/9)+2` 量的是**极值量**，1500 颗种子对跑实测
   **在改动前的老代码上就已 4.00% 击穿**（本单换 rng 流后 4.33%，两条分布几乎重合）——
   与第 23 单 `RAIN_MAX` 那次同一个形状：不是余量薄，是本来就红，只是种子 2027 没抽中。
   照第 24 单立的法「极值量换和式量」改成**话题分布的熵 ≥ 2.90 比特**（`PURE.entropy`，sim30 本来就在用）；
   闸值两头夹定（本体 4000 颗最小 2.9670 零击穿；人为让一类占 25% ⇒ 判红 92%、占 30% ⇒ 判红 100%），
   并附一条「对 25% 霸屏必须判红」的自证断言。`TOPIC_POOL`／`pickTopic`／`sim30` 一个字未动。
   **决策者裁定二（2026-08-07）：认可**，与第 23 单 `RAIN_MAX` 同案；第十章取证与自证断言保留原样。

   **决策者五项裁定（2026-08-07）已全部落实**：①撞招牌那句日记已改为
   `周日街市热闹归热闹，出门这一步我一向跳过。` 并重跑盲标（第三名标注者 58/60＝96.7%，
   **新句被正确标成顾云帆**，撞招牌那条路已断）；②换量尺认可；③`REACT` 不建池；
   ④新候选见第 14 条；⑤Actions 跟进见第 15 条。PR #31 已标 ready。

14. **闲聊接话句全是敷衍式承接**（决策者裁定四登记，本单不做，只登记）。
   **实证**：第 27 单那 32 条接话句**全部**以承接语起手（同感／不好说／细想也是／经这么一提／
   大抵如此／算是吧／确实／明白／记下了…），**无一例外**；离线 3 天实录里约半数对话答非所问，典型：
   　「饭吃了没呀，我这人一天到晚惦记着吃。」→「同感。我这行每天都在等一个跌，等习惯了。」
   　「新机器的钱还在攒，别问进度。」　　　　→「说起来也是，我总盼着能多认识几个人，热闹点好。」
   **成因**：上下句各抽各的，接话句必须能接在**任意**开口句之后，故唯一安全的形态就是敷衍承接
   ——这是第 27 单设计的必然结果，该单交付件第三章口径②已自行预告。
   **参谋判读**：这是第 18 单「招牌起手式连招」换了个形状——**「欸」治好了，换成了「同感」**。
   **候选治法方向（不定案）**：接话池按开口句的类别分组，配对时按类取，不再要求万能承接。

15. **Actions 停摆期间（第 26、27 单）门禁仅有本地七步记录，服务器侧未复核**
   （决策者裁定五登记）。取证：GitHub Actions 自 `2026-08-06T16:27Z` 起零运行，
   连第 26 单 PR #30 于 `18:08Z` 合并进 `main` 都没触发 gate（`main` 最近一次运行仍是
   `14:15Z` 的 `73ea78e`）；第 27 单 PR #31 两次推送同样零运行。**早于第 27 单，与该单改动无关**，
   判读已由决策者认可，取证记在 PR #31 的评论里。
   **跟进（必做）**：Actions 恢复后须把**第 26、27 两单在 `main` 上补跑一次 gate**；
   任一步在服务器环境下判红，当场立单排障。
   两单均不因此阻塞——本地七步逐条对齐 `.github/workflows/gate.yml` 口径，参谋接受本地记录。
   **进展（2026-08-07T12:56Z）**：**Actions 已恢复**，第 27 单 PR #31 的 gate 已跑并**判绿**
   （run 31180348620，14 秒完成，`06918017`）——服务器侧与本地七步结论一致，第 27 单这一半已销。
   **进展（2026-08-07T15:16Z，第 28 单开工时核）**：PR #31 合并触发的 `push: main` 已跑完并**判绿**
   （run 31191728501 · job 92909914861 · head_sha `005f0fe` · 16 秒 · 七步逐步 success：
   提取内联脚本／语法检查／无头模拟测试／30 天退化检查／走位三铁律门禁／中转函数语法检查／SIM 块纯净检查）。
   `005f0fe` 的树里含第 26 单的全部代码（第 27 单就建在它上面），故**第 26 单那一半也随之销账**。
   **一处如实登记**：`8a00b34`（PR #30 的合并提交）这个提交本身永远不会有自己的 gate 运行记录——
   Actions 不回填历史提交，能证的只是「含第 26 单代码的 `main` 树在服务器侧判绿」，
   不是「`8a00b34` 那一刻的树判绿」。两者在本单场景下等价（其间 `main` 无第三方改动），但口径不同，不含糊过去。
   ~~**本条销账（第 28 单）**~~。
16. **`assets/*.png` 与 `functions/relay.js` 不在涨号铁律的射程内**（第 28 单登记，等决策）。
   两张 png **不是内联的**——`html:3149` 是运行时按路径去取（`img.src=ASSET_BASE+'/'+file`），
   全文 `data:image/png;base64` 零命中；`tools/asset-pipeline/README.md` 明写它们是「唯一的对外产物」。
   `functions/relay.js` 部署到边缘，gate 对它只做 `node --check`。
   **后果：换一张 `characters.png`，玩家一定看得见，而 `city-life-framework.html` 一个字节不动 ⇒ 涨号铁律全绿。**
   升法是把判据从「HTML 整份字节」换成「产物清单哈希」（对照表条目存分项 sha 加一个总 sha）。
   第 28 单未擅自扩大——任务书的口径明写是 `city-life-framework.html`。等决策者裁定要不要扩。
17. **`宪法.md:24` 标题写「四、二十条规程」，实际已到 27 条**（第 28 单范围外发现，登记不改）。
   第 21 条起就已经超出「二十」，第 22–25 条挂在「排案总则（条款号接上顺延）」下，
   第 26 条（第 24 单）、第 27 条（第 28 单）继续顺延。标题那个数从没跟进过。
   与本单主题无关，按宪法「其他：范围外发现只登记禁止顺手修改」登记。

16. ~~**日志墙 AI 文案乱序与占位符长挂**~~ **已完成待验收（第 29 单，v37）**。
   **两条独立根因，都由取证坐实**：①三个挂点（`enhanceMessage`／`enhanceChat`／`runReflection`）
   都是 fire-and-forget 的 async 函数，各自在函数尾部直接 `patchLid` ⇒ **谁先回来谁先贴**；
   打乱顺序的岔路至少五条（并发上限 2、语气词闸重生成要再排一次队、`callClaude` 首轮 null 后
   `sleep(800)` 再排一轮、直连转中转两级死线差 11 秒、日记 `prio` 插队）。
   ②收尾时限＝协议上界 `jobLimit×2+800`＝**60800ms**，而 `1 真秒＝10 模拟分钟`，
   故合 **10.1 游戏小时**——与决策者实测的「挂了约 8 个游戏小时」正好同量级，
   **不必假设请求真的死了就能完整解释**。
   **取证第 2 问的明确结论**：`settleOrphanLLM`（第 26 单）**只管开机，不管会话中途**——
   全站唯一调用点在 `if(bootWorld)` 分支里；而三个挂点都没有 `try/finally` 罩住 `llmPending`，
   函数体内任何一处抛异常就永远停在 `llmPending=true`；`drainLog` 的闸是 `!e.llmPending`、
   游标 `logSeenLid` 只增不减，**双重永挂**。
   **治法**＝新增 `pen`（落字排队）：占位符挂上墙的同时 `penAdd` 登记（按 `e.t,e.lid` 升序），
   `penFlush` **只从队头连续放行**，`penSweep` 在主循环里按真实时钟判死线
   （**不用 `setTimeout`——后台标签页会把它节流到分钟级，那正是长挂的另一条路**），
   `penReady` 对已落定的条目返回 `false` ⇒ 迟到回包整包丢弃、绝不覆写。
   三个挂点补 `try/catch/finally`，`penAdd` 排在一切可能抛异常的语句之前。
   **`PEN_TTL_MS=20000`**：下边界由中转线路死线 15000ms＋33% 排队余量定，
   再往下压会砍掉中转线路的正常回包；实测把占位符最长存活从 60800ms 砍到 20000ms
   （10.1 → **3.3 游戏小时**，降 67%）。兜底**只认 `e.fb`**（第 27 单落条目那一刻抽好的那条），
   pen 整块一次骰子都不掷 ⇒ **世界指纹 `2d266238…` 逐字节不变**、SIM／CLIP 块 md5 均未变。
   门禁新增四条闸（落字顺序／占位符不长挂／迟到不覆写／**三条各自反向自查**，
   病态写法当场判红、合规写法不误伤）＋ 结构侧断言 ＋ 篡改档不抛错四条。
   交付件＝`docs/交付/第29单-日志墙落字顺序与占位符收尾.md`。
   **已按决策者指示变基到第 28 单合并后的 main（`e0f0c09`）**：号由原报的 v34 改为 **v37**
   （开工时 main 停在 v33 故报 v34，第 28 单已把第 25／26／27 单补成 v34／v35／v36，
   而涨号铁律要求「新号恰等于最大号 +1」）；`node versiongate.js --登记` 已把 v37 与
   HTML 字节 `dc2cb5b5…` 补进 `version-ledger.json`（只新增，v36 那条一字未动）。
   三件套冲突面按「保留第 28 单改动、本单内容并进去」解开，其禁区文件
   （`宪法.md`／`versiongate.js`／`.gitattributes`／`gate.yml`）diff 为空。
   变基后八步门禁全绿，`versiongate.js` 的 **PR 与 push 两种模式均实测判绿**
   （push 模式比 PR 多一条「落到 main 的号必须已登记」，正是靠补登才绿的）；
   三个指纹与 main 逐字节相同（世界 `2d266238…`／门禁 `6c8f768a…`／SIM 块 `029ec0c3…`）。
   ~~**留给决策者一项**：3.3 游戏小时是否仍偏长~~ **已裁定：`PEN_TTL_MS` 维持 20000，不下调**
   （决策者裁定，2026-08-07）。**理由（决策者原话）：等不到已有兜底句顶上，墙不会再有洞；
   再往下压是拿真文案换速度，不划算。** 判读记档：这条裁定把取值的落脚点从「快不快」
   挪到了「有没有洞」——本单治完之后 3.3 游戏小时的那一段里墙上挂的**不是空白而是兜底句**
   （占位符已被 `e.fb` 顶上、`llmPending` 已清），故那段时间的成本只是「这条暂时不是 AI 写的」，
   不是「这条没字」。而下调死线换来的每一秒，代价都是把本来能回来的中转线路真文案砍掉一条
   ——**拿真文案换速度**。故 20000 是终值，不是暂定值。本条从「等决策」销账。

## 等决策
- **第 30 单·回城弹窗的门槛要不要抬高**（唯一待裁定项）。
  现状：**沿用第 26 单那一个门槛**——补算发生了就弹，即**离开满 10 真实分钟**
  （一拍＝10 模拟分钟、`CATCHUP_MIN_PER_MIN=1`）。任务书点名问过「离开 10 分钟弹一次会不会太烦」，
  施工**不擅自改**（宪法第 7 条：已发布设计行为禁止随意更改），把取舍摆在这里请决策者拍板：
  · **维持现状的好处**：弹窗与日志墙那条 ⏱ 用同一个数、同进同退。
    **本单修的那个 bug，根子正是「两个数各认各的节点」长出来的缝**——多一个门槛就多一类
    「日志墙说城市走了 13 小时、弹窗却不认」的静默不一致，而这种缝没有任何机器闸拦得住（两边单看都对）。
  · **代价**：切走标签页十几分钟回来也会弹一次。离开 10–30 分钟这一档基本必落「乙·概括」形态，
    概括往往只有一两行（实测离开 20 分钟那一档是「城市很安静，一条新记录都没有」）。
  · **若要抬高**：改 `CATCHUP` 段那一个门槛（`catchUpPlan` 里「一拍＝10 模拟分钟」那个除数，
    或 `CATCHUP_MIN_PER_MIN`），弹窗与日志墙两处会同时跟着动。
    **不建议为弹窗单开一个数**——那就把刚堵上的那类缝又开一条。
    注意：这两个常量第 26 单已裁定为「改它等于改玩法，须决策者拍板」。
- ~~**第 29 单·占位符收尾死线取值（`PEN_TTL_MS=20000`＝1× 速度下 3.3 游戏小时）是否仍偏长**~~
  **已裁定：维持 20000，不下调（决策者裁定，2026-08-07）。本条销账。**
  **理由（决策者原话）：等不到已有兜底句顶上，墙不会再有洞；再往下压是拿真文案换速度，不划算。**
  施工侧记档：这条裁定把取值的落脚点从「快不快」挪到了「有没有洞」。第 29 单治完之后，
  那 3.3 游戏小时里墙上挂的**不是空白而是该条自带的兜底句**（`e.fb` 已顶上、`llmPending` 已清、
  `patchLid` 已刷），故这段时间的成本只是「这条暂时不是 AI 写的」，**不是「这条没字」**——
  而「有洞」正是第 29 单立单的病症本身。反过来，下调死线换来的每一秒，
  代价都是把本来能回来的中转线路（`AI_TIMEOUT.relay=15000`）真文案砍掉一条。
  **故 20000 是终值，不是暂定值**；日后谁想动它，先回答「这一改砍掉的是哪一类真文案」。
  （取值两头夹定的原始推导见 `docs/交付/第29单-日志墙落字顺序与占位符收尾.md` 第三章裁决②。）
- ~~**第 27 单·三项待裁定**~~ **已全部裁定并落实（决策者裁定，2026-08-07，共五项）**：
  ①**那句撞招牌的日记：改**——`周日街市热闹归热闹，能不去还是不去，省钱也省事。` 改为
    `周日街市热闹归热闹，出门这一步我一向跳过。`。落点仍在「懒得出门」（夜猫子＋摸鱼），
    全句零钱字；「这一步／跳过」是流程化措辞，与他卡上的程序员词同层。已重跑盲标：
    第三名标注者 58/60＝96.7%，**新句被正确标成顾云帆**，标注者自行写出的判读依据是
    「『这一步我一向跳过』是流程化冷幽默归顾」——**撞招牌那条路已断**。
  ②**harness「无话题霸屏」换量尺：认可**——与第 23 单 `RAIN_MAX` 同案（闸本来就红，非本单造成）；
    交付件第十章取证与自证断言保留原样。
  ③**`REACT` 不建池，维持现状**——已销账，理由记在第 13 条范围外发现。
  ④**新登记候选「闲聊接话句全是敷衍式承接」**——见候选表第 14 条。
  ⑤**Actions 停摆判读认可，另加跟进**——见候选表第 15 条。
- ~~**第 26 单·四项待裁定**（换算口径 1:1／封顶 3 天／边界情形／日记全同）~~
  **已全部裁定并落实（决策者裁定，2026-08-06）**：
  ①**换算口径 1:1 确认，维持现状**——`CATCHUP_MIN_PER_MIN=1`，理由「关十分钟看刚才这十分钟只有 1:1 才逐字成立」按裁定写进交付件第二章；
  ②**封顶 3 模拟日确认，维持现状**——`CATCHUP_MAX_DAYS=3`，交付件里那句「封顶不是因为算不动」按裁定**保留**，防止日后当成性能妥协随手放宽；
  ③**边界情形改判据**——由「跨过夜就推剪辑页」改为「跨过夜 **且** 真出了新剪辑才推，没有新卡就别推」，已改并复测六档，
    `B-away-60min` 那一档由「剪辑页 · 出横幅 · 0 张卡」改为「现场页 · 不出横幅」。
    **落地时有一处必须记档的偏离**：判据没有照字面取「`w.clips` 条数增量」，而是取「**最新剪辑日**变没变大」（`clipTopDay`）——
    因为 `w.clips` 的条数被 `CLIP_KEEP=60` 封着（`html:1474`），过了 60 个剪辑日之后增量恒为 0，
    照字面实现会让这条呈现规则**在第 61 个剪辑日悄悄失效且不报错**。裁定要的效果逐字守住，只是量它的尺子换了一把；
    harness 有 5 条断言钉住（含「饱和后条数增量恒为 0」的反面证据）。
  ④**日记兜底 12 条全同接受现状，本单不动，另立第 27 单专治**——已提到候选表最前面并标为已排期，见上。
- ~~**第 22 单·雨次数余量剩 2（必须回收）**：门禁双种子实测 **52 ／ 57**，上限 59。~~
  **已结案（第 23 单）**：决策者已裁定「另立一单」，即第 23 单。复核结论——
  **这条闸不是余量薄，是已经红了**：5 万颗种子实测 `RAIN_MAX=59` 击穿 **40 颗（0.080%）**，
  零代码改动、只换颗种子（如 580→61、644→60）门禁就红。第 22 单那份 42 颗复测没看见，
  是因为按 0.08% 的击穿率，42 颗抽中的概率只有 3%。**治法落任务书第一档「换量尺」，
  `RAIN_MAX` 到最后一刻仍一个字未改**（是被整条替换，不是被放宽）。详见下方「雨密度贴近上限」条。
- ~~**第 23 单·指纹口径请拆成两个数（宪法第 10 条提请修订）**：现行「指纹」＝`node sim30.js | md5sum`，
  即**门禁自己那 20 行输出**的 md5，把「世界变了」与「门禁多打印一行字」混成同一个数。~~
  **已裁定并落地（决策者裁定一 → 第 24 单）**：拆成两个数分别记账、分别复报——
  ①**世界指纹**＝`node worldsig.js`（采集器已落成仓库文件，口径与盲区写在文件头；v33 基线
  `d19035af7477984608b376115cb04e24`，第 24 单改动后逐字节不变；**第 27 单起＝`2d266238…`，见交接说明「指纹分两个数」条，
  该条的版本号绑定已由第 28 单补正**）；
  ②**门禁指纹**＝`node sim30.js | md5sum`（第 24 单 `a4d65e6a…`；**第 27 单起 `6c8f768a…`**）。
  已按宪法第 10 条入档为**规程第 26 条**，往后所有单按新口径复报。**本条销账。**
- ~~**第 23 单·`GAP_MAX=1080`（最长无戏间隔）同样已被击穿，未改，请裁定另立单**；
  同批顺手捡到 `MARKET_MIN=4` 余量只有 2（1 万颗实测最小值 6）。~~
  **已复核结案（第 24 单）**，交付件＝`docs/交付/第24单-sim30六项阈值全面复核.md`。三点：
  ①**5 万颗口径下击穿加严为 11/50000**（最大 1210，种子 47351）。
  ②**右尾成因查清且不指向真实缺陷，故未触发停手报告**——击穿最深那颗报「1210 分钟无戏」，
  可那 1220 分钟里城市**推了 31 条日志、含一场真的闲聊**；`html:1557` 判的是**日志墙尾条**
  是不是闲聊，那场闲聊被后一条做饭日志顶下尾条就当没发生。另两成因是几何分布尾巴
  （雨窗内连续掷 22 次 30% 全落空，`0.7²²≈4.3e-4`）与**雨兜底**（把雨起停不算戏，
  最长无戏间隔由 均值 771／最大 1210 涨到 **均值 1393／最大 3560**）。
  ③**故换量尺**：改为「静默超过 200 分钟」的时长占比 ≤27%（200 取自导演层自己的「太平淡」门槛，
  非新立的数）。`MARKET_MIN` 同样换尺（到访人次 → 滞留人·分钟 ≥100），因实测同款反相关：
  每趟停留 ×3，广场上人·分钟 755→1570 而**到访人次反而 14.9→13.7**。**本条销账。**
- **第 22 单·顾云帆被挑中 20/60（33.3%）是否算偏**：改前四人 15/15/16/14。成因说得清——
  他规律性系数最高，而作息类偏离项正是本单新开的一类，本来就该集中在最不规律的人身上；
  第 21 单立的两条门禁（四人都被挑中过、最多一人不超过五成）仍绿，16 种子复测上限 43%。
  若要压回更均匀，做法是给作息类偏离项也上一道常态化折除，但那要动剪辑层判据（本单禁区），须另立单。
- **第 22 单·已知接受项**：①`worn_out`（体力见底还在硬撑）是转正四条里最薄的一条，
  60 天 1 次、480 天折合每 60 天约 1.1 次，且逐种子波动 0–3 次；它是绝对红线判据，稀有是应该的。
  ②`hunger_peak` 的 99% 集中在陆知秋、`worn_out` 100% 是顾云帆——人设的必然，不是缺陷；
  陆知秋的命中率实测 0.17–0.45，尚在剪辑层 0.5 折除线以下，最高那颗种子只差 0.05，日后若越线会被自动折除。
  ③周末赖床是逐人常量、不参与常态化折除，故 `lie` 上限刻意压在 45 分以内，
  让「周末」单独一条推不过 40 分钟的最小可报偏差，必须叠加别的成因才出得来。
  ④玩家短信「别迟到」的作息效果只在傍晚收到时才看得见（`earlyWork` 标次日上班后即清）。
  ⑤`RHY_TIGHT_*` 把钱牵进了作息层（负债刹车，缘由见交付件 9.2 第 6 条），
  代价是沈小满饿肚子的时候比改前多。
- **范围外发现·登记不改（第 22 单，宪法第 19 条）**：`decide()` 的工作时段判定**仍然没有星期检查**
  （第 17 单已登记同一条），四人周六周日照常上班。本单给「周末多睡一会儿」做了逐人差异，
  于是出现「周末睡到九点、然后照样去上班」的拧巴观感。非本单引入，只登记不顺手改；要治得动工作时段口径。
- **第 22 单·未治两条判据，照实登记**：①`hunger_nomeal`（一整天没吃）**结构上不可能**——
  四人每天产生的饥饿量约 130 点，而生存红线把开饭线封在 92，只要「饿了就吃」这条规则还在就打不着；
  它真正对应的世界是「忙到／穷到没吃上饭」，得等目标系统或经济侧改动。
  ②`money_flatpay`（发薪日却没涨）**本单不动收入故仍为 0**——周五 18:00 无条件发全额工资 ¥200–450，
  一天饭钱只有 ¥8–20 × 2–3 顿；交租固定每月 2 号、30 天窗口里落在周二，与周五永不重合。
  要让它活起来得动收入侧（工资是否必发／大额支出会不会落在周五），按宪法第 4 条本单不顺手改。
- ~~**第 21 单·判据是否认可**~~ **已回收（判据认可，第 21 单收工时裁定）**。机器读数留档：
  60 天里**强档 65%**（最高单项 ≥2.0，确有一件明确的事）、中档 25%、弱档 10%；
  四人被挑中 15/15/16/14（均匀为 15），**无人霸榜**；57% 的卡有两条以上偏离项，
  平均每卡引用 3.1 条日志原文，零空卡。判据一个字未改，改的是「平常」怎么算（见交付件第三章）。
- ~~**第 21 单·三个待决策项**~~ **已裁定（详见交付件 11.2，三项均只改措辞、代码零改动）**：
  ①**薄卡照出**，不设下限、不合并。理由：现在把薄卡藏起来，等于**用剪辑层掩盖世界的贫瘠**——
  问题还在，只是看不见了；留着它是个持续的提醒。
  ②**二期不做 AI 润色**。理由：短板不在文笔在素材；给本来没事的日子配好文笔，
  是第 18 单「素材越稀薄，模型越硬凑」的翻版。
  ③**剪辑页不加筛选**。四人规模不需要，人数变多再议。
  **三项共同指向：剪辑层到此为止，下一步该动的是这个世界本身。**
- **第 21 单·已知接受项**：①**处境类偏离没有可引的日志原文**——第 17 单铁律定死外围角色事件不落日志墙，
  故「挂上逆境」只能退到引当天的工作条目，卡上会出现事由与引文对不上号的观感；要治得改第 17 单口径。
  ②七条偏离项 60 天零命中，其中五条**结构上不可能**（见下条范围外发现），本单保留不删——
  它们是模拟侧变丰富后的现成接口。③满 60 天剪辑约 21–22 KB，全档由约 58 KB 增至约 80 KB（`CLIP_KEEP` 可调）。
  ④共性折减按「≥3 人」判，四人城市里等于「至少四分之三」，人数变了要重估。
- **范围外发现·登记不改（第 21 单，宪法第 19 条）**：①**起床时刻逐分钟固定、零方差**——
  `decide()` 睡眠分支把 `busyUntil` 设成「唤醒时刻+10」，实测四人 30 天恒定
  （白一鸣 06:10／沈小满与陆知秋 07:10／顾云帆 08:10），故「反常早起／晚起」两条判据**结构上无法命中**。
  与本页第 10 条「作息人格」同源，建议同车治理。②**清醒时饥饿与体力被 `eatAt`／`napAt` 夹死**
  （实测上限 78、下限 14.5，而绝对线是 88／12），故「饿着没吃饭／体力见底还在硬撑」这类绝对判据打不着；
  本单已补相对判据（比自己平常饿／比平常累）让这一类有输出，绝对线保留为兜底。
  ③**发薪日没涨结构上不可能**——周五 18:00 无条件发全额工资，日常餐费远小于最低工资；
  且交租日固定每月 2 号，30 天窗口里落在周二，与周五不重合。
  **这三条合起来说明一件事：选材判据的分辨力已经超过了世界的丰富度**，后续要让剪辑更有戏，
  该补的是模拟侧的事件与作息差异，不是再调判据。
  **决策者裁定二期不做 AI 润色后，这条从「备查」升为「剪辑要变好看的唯一入口」**——
  剪辑层已封顶，观感能不能起来全看模拟侧补不补得上。
- ~~**方案乙 · 生成后正则检查＋不合格重生成一次**（第 13 单机制侧后手方案，原挂本区等决策，指针见 tools/voice-check/说明.md 末行）~~ **已启用（第 18 单，v29）**。启用时按任务书改了判据：**由「禁词表」改为「结构」**——只检查开场（剥引号括号空白后取前 `LEAD_SCAN`=10 字）是否以 `LEAD_INTERJ` 表里的语气词起手，不检查具体短语，因为禁词表已被两次实证可绕过（第 13 单点名「你闻见没」→ 模型换「欸你看见…没」；v28 又直接退回原句）。不合格则**由代码指定另一类开场**（走现有 `OPEN_KINDS`／日记走 `DIARY_OPEN_KINDS`）重生成一次；至多一次、失败即接受、受 AI_TIMEOUT 约束、绝不阻塞日志墙；覆盖短信/对白/日记三挂点；`ZHAO_EVERY` 配额原样保留，本闸是配额之外的兜底。**登记口径更正**：档案瘦身时本区这一条被清掉了，只在 tools/voice-check/说明.md 末行留了指针；本次按该指针据实销账。
- **第 18 单三条只能目验（v29，必须回收；机器已尽力，剩下的机器验不了）**：详见 `docs/交付/第18单-AI文案层返工.md` 第七章。①**日记体铁律五条**——桩只验到「五条写进提示词了」，验不了模型守不守；判据＝日记里出现第二人称／问号／引号对白／「你刚才说…」任意一项即不合格。②**重生成后的第二版质量**——桩只验到「不合格能被拦下、代码会指定另一类开场重开一次」，验不了第二版写得好不好，也验不了模型改用别的套路连招（例如全都拿「今天」起手）；判据＝连看同一人 6–8 段开口，换了个字继续连招即闸未治本。③**派定话题后模型是否真在类里聊**——桩只验到「话题写进提示词且不霸屏」，验不了跑不跑题，也验不了跨天物件矛盾是否消失。
- **第 18 单·真模型复测同样未执行（照实登记，未绕过）**：本单改了三处提示词，交接说明铁律适用。**当场实测**：`api.deepseek.com`（线上通道后端）与决策者站点域名均被沙箱代理 403 拒绝；`api.anthropic.com` 可达但任务书明令禁止用 Claude 直连结果冒充线上表现，故一次未用。**本单交付件里所有文本均为提示词或代码产出，无一字来自模型。** 复测请在能连通部署站的环境按 tools/voice-check/说明.md 补跑。
- **第 17 单假说待验真（本单核心，必须回收）**：本单是「让事件在人身上挂住，四人说的话会不会自然分开」的第一次验证。规则侧已落地并全绿，结构侧统计也支持分化（30 天双种子：睡前日记时刻四人处境组合两颗种子均 30/30 天各不相同，四人同挂同一档的时间占比 1.4%／4.0%）。但**说话像不像四个人，机器验不了**——只能由决策者目验裁定。回收方式＝试玩看四人日记/对白是否开始各说各的；若仍趋同，下一步候选是让处境状态改变**行为**（而不只是进角色卡），即接目标系统。
- **第 17 单铁律 1 的验收只能靠目验**：「状态不许被念出来」已在角色卡那行写死禁令措辞，但**桩测试验不了模型守不守**（第 13 单同款教训）。目验判据：产出里若出现「因为被退稿了所以我今天……」这类播报式复述，即为不合格，后手是改卡上措辞或降低注入强度。
- **第 17 单·真模型复测未执行（照实登记，未绕过）**：交接说明铁律要求「每次改提示词必须跑 tools/voice-check 真模型复测」，本单改了 agentCard（属提示词），铁律适用。但线上通道（DeepSeek 中转站）施工沙箱出网 403，**AI 产出的实际观感本单测不了**；按任务书要求，未用 Claude 直连结果冒充线上表现。工具本身已修好并跑通到出提示词那一步（含处境注入与整行省略两种形态实证），复测请在能连通部署站的环境补跑，重点看新增的「今天心里压着的事」一栏是否挤占语言指纹服从率。
- ~~第 17 单·SIT_TTL 取值待体感裁定~~ **已裁定（补充指令一）**：取任务书口径上限 14 小时。改后日记时刻挂载比例 73.3%/69.2% → **93.3%/95.0%**；余下 5–7% 已非时效所致，而是约 6.7% 的人天压根没抽中事件，再加时效也补不满。
- ~~第 17 单·事件表好坏比例~~ **已定案（补充指令二）**：**逐人 3 好/3 坏/1 平，每人 7 条、全城 28 条**。补充指令一的「全城对半 + 每人 6 条」已作废——该约束原为顾虑 pickV 池容量，但本表不是抽词池；且全城对半会让两人结构性偏逆、两人结构性偏顺，30 天累积污染假说验收。代价：同档共现由 0.0%/0.6% 升至 1.4%/4.0%（算术必然），换来组合去重升至逐拍 150/154、日记时刻 30/30。
- **第 17 单已知接受项**：①约 6.7% 的人天当天抽不中任何事件（decide 在上班期间约每 60 分钟一次、一天仅 8 次机会所致），这部分既无「有戏」也无「平淡」档，角色卡整行省略——与「一天至多一次」口径不冲突，但若要「没戏也必须是平淡档」，需把触发改成当日定点必抽。②同一事件跨天可复现（只保证相邻两次不复读，不做更长窗口去重；6 条池子做长窗口去重会显著压低随机性）。
- **范围外发现·登记不改（第 17 单，宪法第 19 条）**：`decide()` 的工作时段判定**没有星期检查**，四人周六周日照常上班（`inWork` 只看 mod 与 start/end）。故外围角色事件周末也会触发。此为既有行为、非本单引入，按规矩只登记不顺手改。
- ~~**雨密度贴近上限（第 17 单实测捡到，登记备查）**：sim30 的 `RAIN_MAX=59`，main 基线实测 52/49，第 17 单 rng 流位移后为 54/52 —— **六项全过、阈值一行未动**，但雨次数是六项里离上限最近的一项（余量 5–7）。成因是导演层雨密度本就偏高（待办「导演层雨密度体感复核」条早有记载，其"30 天约 45–49 场"的旧估值与实测已有偏差）。任何后续改动只要动 rng 流，都可能把它顶穿；建议随雨密度体感复核一并裁定，**不得靠放宽 RAIN_MAX 解决**。~~
  **已复核结案（第 23 单）**，交付件＝`docs/交付/第23单-雨密度余量复核.md`。四点：
  ①**红线逐字遵守**——`RAIN_MAX` 一个字未改，本单是把整条闸**换成了别的量尺**，不是放宽它。
  ②**这条闸的立法本意查到了**（PR #4 逐字：`49×1.2=58.8 → 取 59`，初值 40 抄自 hezuriji
  `scripts/sim-check.mts` 的「2~40 条」，而云港自家设计就是 1.5 场/天＝45 场，**一落地就注定顶穿**）；
  本意是防「导演层跑飞」的退化检查，**不是防体感太密**。
  ③**「次数」不是本意的合适量尺**：把一场雨时长加倍，城市湿一倍，雨次数反而 52/57 → **37/42，闸更绿**。
  病根是次数只数边沿，而雨在这个世界是持续状态（雨幕挂着，`decide()` 根本不读它）。
  ④**新量尺＝雨占全时长比例，阈值 10%–18%**，口径＝5 万颗实测「均值 ±5 个标准差、向外取整到整数百分点」，
  取整后实到 **−5.72σ／+6.03σ**，5 万颗零越界（旧闸同口径只有 +2.76σ、击穿 40 颗）。
  反向自查：**17 种人为改动**逐个实测，新闸拦下 **13 种**、旧闸只拦下 **5 种**，基线 5 万颗零误红。
  **本条到此销账**——余量不再是会被下一次 rng 位移吃掉的绝对差，往后动 rng 流不必再赌这一项。
  **第 18 单复测（v29，任务书点名要求）**：话题派活位移 rng 流后实测 **53/49**，不升反降，上限余量由 5–7 扩到 **6–10**；阈值一行未动。本条继续挂账（余量仍属六项里最小）。
  **第 21 单复测（v32，任务书点名要求）**：本单在 `advance10()` 末尾（导演层之后）加了一行剪辑调用，任务书要求触及导演层须复测。实测 **53/49，与 v31 一字不差**，指纹亦逐字节未变——因为剪辑层**只读不写、零 rng 消耗**，不参与任何随机决策。本条继续挂账。
  **第 22 单复测（v33，任务书点名要求）**：本单必然位移 rng 流，实测 **52/57**，种子 424242 余量由 10 缩到 **2**。42 颗种子复测证明**分布没被推高**（50.7±2.9 → 51.9±2.2，最大 56→57，超上限者改前改后皆为 0），是那颗种子换了位置。**阈值一行未动**，本条升级为「等决策」区第一项，见上。
- **第 18 单·已知接受项**：①短信挂点也上了语气词闸（任务书实证只在闲聊/日记；理由＝同一个病留个洞下周就会被截图，代价是回信偶尔多一次调用）——若认为回信天然承接来信、「欸」是合理反应，说一声即可摘掉。②日记挂点明喻配额的极轻微算术副作用：`simile` 判据是 `n%3===0 && !zhao`，日记不再发 zhao，故第 15、30… 轮在日记上由「两项都不发」变成「发明喻许可」，每 3 次一发的节律不变。③同一话题仍可跨窗口复现（`TOPIC_RECENT=4` 只保证最近 4 次不复读，不做更长窗口去重，照 PEER_EVENTS 同款接受项）；30 天实测最集中一类占 13.7%。④物件级矛盾未根治——派话题只压低同一物件的复现频率，不给模型物件记忆；彻底解决要等「周记忆压缩」（本页目标系统·换脑）。⑤`TOPIC_POOL` 未覆盖的场景一律不聊（如手机/网购/追剧不在 9 类里）——刻意为之，池子越大频次过滤越弱；要加类直接说。
- **范围外发现·登记不改（第 18 单，宪法第 19 条）**：对白挂点的招牌配额四人同步——`aiTurn` 从 0 起同步递增、`ZHAO_EVERY=5`，导致四人的招牌轮次落在同一批（实测第 2、7 轮四人齐发招牌）。日记挂点因第 18 单不再发招牌，该同步在日记上已自然消失；**对白挂点仍在，属 v27 既有行为、非第 18 单引入**。若哪天四人在同一场对白里齐用招牌，病根在此，治法可照 `styleOffset` 给配额也加相位偏移。
- **第 19 单·治法档位与红线（照实登记，供验收对账）**：病症「多名角色占同一落脚点、视觉人叠人」已取证证实——`ANCHORS` 是「一功能一点」，显示坐标由锚点几何直算、逐人零区分，**落位逻辑零占用检查／零让位／零排队**。治法落**任务书第一优先档（纯显示层错开）**，且七处会叠人的锚点**全部适用第一档**：因第 11 单「坐标与规则解耦」已实证（SIM 只认锚名、坐标仅渲染层消费），「给功能补足落脚点」整件可在显示层完成，**第二档塌缩进第一档、第三档无从谈起**。故**未动 SIM、未动 rng、未动 `ANCHORS`**，sim30 指纹逐字节不变、六项阈值一行未动，**「30 天生存不变量」零风险**（本单零让位逻辑，结构上不可能让人做不成活动）。全量普查表与前后对照图见 `docs/交付/第19单-人物落位重叠排障.md`。
- **第 19 单·待决策一项**：**站位是逐人固定的**（a1 永远右椅、a2 永远左椅……）。好处＝没人挪窝、观感稳定；副作用＝一个人独自在家时也总坐同一个位置。若认为该「谁先到谁坐主位」，需引入到场顺序记账（本质即占用查询＝任务书第三档），代价明显更高，故本单不做。**要不要改，一句话即可。**
- **第 19 单·已知接受项**：①走到锚点后有一次短程横滑（先走到锚点中心，再滑到自己站位，最远 4.5 格、1× 速度下约 0.4 秒；沿用补充指令四平滑器，滑动期间放站立帧）。要彻底消掉需让 `buildPath` 直接以站位为终点，会动到走线逻辑，超出本单范围。②名牌在低缩放下仍可能横向相邻挤在一起——名牌是屏幕空间固定字号，两人相隔 1.5 格时地图缩小后名牌宽度会大于间距；本单只保证「名牌不压在别人身上」，不保证「名牌互不相邻」，此为既有观感项（任何两人靠近时皆然）。
- **第 19 单追加一·已修与排查结论**：短信往来记录跨天混叠（决策者实证：04:40/05:40/06:30/03:30/04:20… 看似时间倒流，实为三个不同日子）已治——全站排查按时间排列的列表共 5 处，**全部中招、全部已改**为 `PURE.fmtStamp`（「D75 04:40」）：城市日志（400 条 ≈ **9.5 天**，比报案处更严重）、短信往来记录（14 条 ≈ 5 天，报案处）、侧栏（120 条 ≈ 2.9 天）、角色页·最近记录、画布浮层最近两条。存档信息行早已印 D 号，并入同一函数后输出逐字不变；顶栏时钟豁免（当前时刻非列表，旁边 `#tb-day` 已印 D 号）。未动列表容量／排序／样式／额度机制／短信生成，SIM 零改动。
- **第 19 单追加二·已修与排查结论**：语气词闸漏「哈」已补——判据由「是哪个字」推进到「字后面跟什么」（后跟标点／空白／句末＝语气词拦，后跟汉字＝实词放行），**未改回禁词表**。同类缝隙排查：主表 16 条既有条目逐条通查，**无第二条缝需补**，歧义表现仅「哈」一条。误伤自查用例已实跑并列入交付件十一.4（覆盖任务书点名的哈尔滨／哈欠／哈密瓜／「哈哈」连用／「哈」在句中五类，另加哈喽与引号包裹两类）。
- **第 19 单·真模型复测未执行（照实登记，未绕过）**：追加二改了 `redoLead` 一句举例（属提示词），交接说明铁律适用。**当场实测**：DeepSeek 中转站在施工沙箱仍出网 403（第 17、18 单已两次实测同款），按既有口径不用 Claude 直连结果冒充线上表现，故一次未用。**闸的判据本身是纯代码、机器已完整验收**；本单交付件所有文本均为代码产出或代码判定结果，无一字来自模型。复测请在能连通部署站的环境按 `tools/voice-check/说明.md` 补跑，重点看沈小满是否改用别的套路连招（例如全都拿「今天」起手）。
- **范围外发现·登记不改（第 19 单追加二，决策者点名登记，参谋后续排单）**：沈小满两条回信的重复**不止起手字**——「你谁啊」「是不是发错了」是**内容层面**的复读。机理与第 18 单病症三同源：**短信挂点没有话题指派**。`TOPIC_POOL`/`pickTopic` 只作用于同屋闲聊（`enhanceChat`），短信挂点（`enhanceMessage`）走「回信天然承接来信」的情境豁免口径，既不派轮转开场也不派话题，模型手里只有来信那一句，自然收敛到同一套反问。本单不治。施工侧提示：若照病症三手法治，改动点在 `enhanceMessage` 提示词与一张短信专用类目表，属提示词改动，届时真模型复测铁律适用。
- **范围外发现·登记不改（第 19 单，宪法第 19 条）**：`ANCHORS.store_shelf`（便利店·货架，格 30,8）是**死锚**——全代码没有任何分支把人派去它，30 天双种子到访 0 人次。属既有状态、非本单引入。本单只做到「不给它登记站位」，其显示落点零改动。
- **第 23 单·已知接受项**：①**设置页「城市体检」仍只显示雨次数，没跟着换成占比**（`html:371`／`2187`）。
  不是疏漏，是**做不到而不是不想做**——面板读 `w.stats`，要显示占比就得往 `w.stats` 新增雨分钟累加器，
  而 `w.stats` 进世界状态签名，**加字段等于改世界指纹**，正是硬口径明令停手的那一档
  （sim30 能算，是因为它在门禁侧逐拍观测、不写世界状态）。面板现在显示的东西**并没有变错**
  （sim30 那一行仍原样打出「52 场」，两边口径一致），变得不完整的是「面板没显示新立闸的那个量」。
  治法两条都超出本单：给 `w.stats` 加字段（需授权、指纹必变）／DOM 层自己逐拍累计（不进存档、跨存档读数会断）。
  ②新闸拦不住 ±20% 以内的密度漂移，且这一点**在任何量尺上都不可挽回**（基线实测跨度 11.04%–16.74%，
  要拦住 16.6% 就得把上限压到实测最大值以下，基线自己就会红）；对应地「无戏阈 200→150」由旧闸拦得住
  变成新闸拦不住，是本单唯一的净能力损失。③本单**未回答「雨到底密不密」**（属决策者四权，见上）。
- **范围外发现·登记不改（第 23 单，宪法「范围外发现只登记禁止顺手修改」）**：
  `walkgate.js:46` 的正则 `/function stepDisplay\(ag,dtSec,pixOn\)\{[\s\S]*?\n\}\nlet rainSeed/`
  **依赖 `let rainSeed=0;`（`html:3021`）紧跟在 `stepDisplay` 结尾之后**。
  将来谁为雨幕／雨密度动渲染层、挪动或改名 `rainSeed`，`walkgate` 会在取源码那一步直接抛错。
  本单不动渲染层，未触发。属既有耦合、非本单引入。
- **第 24 单·已知接受项**：①**街市那条闸换尺买到的是准头，不是检出率**——「周日逛街市概率砍到 0.02」
  （主分支基本废掉，人·分钟 755→438）**新旧两把尺子都拦不住**。成因是街市只在周日发生、
  30 天窗口恰好 4 个周日，小计数天然噪声大（变异系数 15–17%），5σ 阈值必然松。
  换尺解决的是**方向反了**这件事（街市越热闹读数越低），**检出率的短板另想办法**，本条即登记。
  ②「静默>200 分占比」是**单侧上限**，防「城市死掉」不防「日志墙刷屏」；现行那条也是单侧，
  故无能力回退，要不要补下限属新题目，未擅自扩权。
  ③**四条阈值收紧的代价**：熵 −50.2σ→−6.0σ、基尼 +34.0σ→+5.5σ、闲聊 −15.8σ→−5.5σ、钱包 −9.9σ→−6.9σ。
  收紧后**后续任何真的改了世界行为的单都更容易把这几条顶红**（例如社交频次跌 30%，闲聊那条就贴到线上）。
  **这是有意的**——5σ 的本意是「只在世界真的变了时才响」，不是「永远不响」；基线已证零误红
  （5 万＋8000＋500 颗）。④设置页「城市体检」仍显示旧口径读数（雨场次／街市人次／最长无戏间隔），
  **未动，理由同第 23 单**：面板读 `w.stats`，新的量都在门禁侧观测、不进世界状态，加字段等于改世界指纹；
  面板显示的东西**没有变错**，sim30 每条断言行都把旧口径读数原样打在同一行。
  ⑤`worldsig.js` 覆盖不到 SIM 内的纯文案改动，那一类单须另附 SIM 块源码 md5（口径写在文件头）。
- **范围外发现·登记不改（第 24 单，宪法「范围外发现只登记禁止顺手修改」）**：
  ①**`PURE.gini`（`html:64–72`）对负资产不成立**——`if(mu<=0) return 0;` 会把「全城资不抵债」
  报成「完全平均」，`mu` 接近 0 时读数发散（反向自查里「做饭 8→40 元」一档实测 **39.879**）。
  基尼系数本就只对非负量定义，而本作钱包可以为负。属既有实现、非本单引入；
  目前不影响门禁（基线钱包均值恒为正，且钱包那条闸会先红），**将来若做「负债玩法」须一并治**。
  ②**门禁的 `SILENT_K=200` 与导演层 `(w.t-w.lastSpark)>200`（`html:1545`）是一条显式耦合**——
  门禁刻意引用导演层自己对「太平淡」的定义（照宪法第 1 条对标既有，不另立数字），
  故**将来谁改那个 200，门禁的读数与阈值须一并复算**。必须登记，否则下一个人会当成巧合。
  ③**`html:1557` 的「看日志尾条」写法本身是一处缺陷源**——它让「发生过的闲聊被后一条 act 顶掉就不算数」
  （第 24 单实证）。`w.stats.maxGap` 与体检面板都还在用它。本单**只在门禁侧绕开**（不再拿它当判据），
  **游戏代码一个字未改**；若哪天要治本，改动点是那两行，属 SIM，会动世界指纹，须单独授权。
- **交叉引用（第 23 单）**：本页「下一单候选」第 6 条末的「三期·氛围（**雨丝**/天色/气泡）」与本单同题——
  第 23 单把雨的体感判据定义成「雨幕挂着的时长占比」，而「雨丝」正是雨幕的规划名。
  分工写明：那一单若只改雨幕的**视觉形态**（不改时长），本闸读数不变，**这是对的**——本闸量的是
  「下多久」，不是「长什么样」。若它顺带改了雨的时长或密度，则须复跑 sim30 并按第 23 单口径对账。
- 宪法修订提议A：任务书不再指定具体分支名，仅要求从最新 main 新开、PR 指向 main。缘由：第 1 单知会项。
- 宪法修订提议B：接入任何新平台/新服务前，先向决策者询问既往踩坑记录。缘由：EdgeOne 默认域名大陆 401 一事。
- 后置补证：claude.ai 工件内 noStore 态实测；第 8 单合并后部署站整晚不冻实测。

## 观感优化（目验捡到，待排期）
- 门洞缺视觉记号：2 格门洞现为纯地板留口，无门框/地垫等记号，易被误认为断墙。治法走素材流水线（门框贴条或地垫小件），随视觉层二期·室内篇同车最省。
- 名牌横向相邻挤字：错开落位后同锚多人的名牌并排，地图缩到很小时名牌宽度大于间距会互相挤。治法候选＝名牌按屏幕空间做避让排布，或人数多时只显示选中者名牌。属既有观感项（第 19 单登记，非该单引入）。
- 睡觉仍为站姿：入睡只是站姿精灵叠一层毯面，无躺卧帧。补躺卧需改 characters.png 邮票册契约（现为 8 行 × 每行 24 帧＝右/上/左/下×6，属渲染契约），牵动流水线与游戏侧切帧口径，另立单办。
- 浏览器缓存旧版：见「下一单候选」第 6 条（代码与素材各有缓存，网址尾巴只管代码）。
- **客厅餐桌西侧过道净宽仅 1 格（第 20 单实测捡到，属既有放样，非该单引入）**：餐桌左椅占格 `(2,9)` 与客厅西墙之间只剩 1 格，第 20 单的避实体走线会从这里过（实测 `1,9` 共 491 人帧，占 0.101%）。《云港建筑规范》二.1「单人通道 ≥2 格」、二.2「宽 1 格的通道是零余量下限，放样时一律不得按 1 格通道设计」——该处不合规。**未在第 20 单修**：治法要动客厅家具放样（挪餐桌或去掉左椅占格），牵动 `build_assets.py` 登记表、素材成品与 `PIX_SOLID`，超出该单「治法全部落在渲染层、指纹逐字节不变」的硬口径。走线本身合法（脚底零进实体格，人宽 42–48px 可通过），故按读数上报而非门禁断言。**建议随视觉层二期·室内篇同车治理**（那一单本就要重放样客厅）。

- **`stepDisplay` 的限速器自第 25 单起结构性地不再生效**（登记，暂不动）：治后 `len<=cap` 恒成立
  （`cap=(5.5×速度+6)×dt` 恒大于 `updateWalkers` 单帧步长 `5.5×速度×dt`，余量 `+6×dt`），
  故永远走精确赋值那一支，那段插值成了死代码。**未删**——它属第 20 单立的机制，
  删它超出第 25 单范围，且留着是无害的安全网。日后若要动平滑器，先读这一条。

## 已登记（暂不动）
- 借鉴 WorldX：四模型分角色配置形状（花子百万播放已验证看点，等公开版通道分层）；「广播事件」做成导演层调试工具；地图生成＋视觉审查循环；AI 生成新住户；周日夜谈；四住户立绘一次性生成入库。
- 借鉴星原/旅行青蛙：日报高光分享卡（远期）；把陪伴做穿不做广场（定位铁律）。
- 关系状态机；可变雇佣/住处机制（目标系统谓词扩展依赖）。
- ~~**导演层雨密度体感复核**（复核那一半由第 23 单交齐，「定夺」那一半等决策者一句话）~~
  **已裁定并结案（决策者裁定二，第 24 单销账）：接受现状，不动导演层。**
  **裁定理由（决策者原话要点）**：第 23 单已查明 `decide()` **全程不读 `w.weather`**，
  **没有任何角色因为下雨改变过任何一个决定**——雨目前是**纯视觉层的壁纸**。
  既然角色对雨零反应，调密度改多改少都不影响体验，故这一条不值得动。
  留档实测（3000–50000 颗种子 × 30 天，供日后立单时取用）：每天起雨 **1.74 场**
  （设计意图「约 1.5 场/天」，高 16%）／30 天里下雨 **29.98 天**（天天下雨）／
  最长连续下雨 **29.86 天**／白天 09–21 挂着雨幕 **27.35%**／全天 **13.90%**／
  两场雨间隔有 **40%** 落在结构下限 300–390 分。旧估值「30 天约 45–49 场」**作废**
  （它是拿 v15 那两颗门禁种子的实测值 49／45 倒着写成的，非独立读数）。
  **真正该问的那个问题另开一条**，见下「天气回流进行为」。
- **天气回流进行为（雨→决策／心情／路线）**——**列入世界丰富度序列，不排期，等决策者哪天要做时再立单。**
  由裁定二引出：既然「雨要不要调密度」的答案是「角色对雨零反应，调了也没用」，
  **真正该问的就是「雨要不要让人有反应」**——那是另一个题目，不是密度题目。
  形状待议，可选的落点举例（未设计、未排期）：躲雨改路线／雨天心情档位／雨天改活动选择
  （如不出门散步、改去便利店）／雨进剪辑层判据。
  **开工前须知两条**：①这会让 `decide()` 读 `w.weather`，**必然改 rng 流与世界指纹**，须复测 sim30 全六项；
  ②届时雨那条闸（雨占全时长 10%–18%）的**立法本意会变**——现在它量的是「雨幕挂了多久」＝纯视觉体感，
  雨一旦影响行为，它就同时成了行为量，须按第 23／24 单口径重新复核。
- 完整事件溯源与重放界面；天色昼夜渐变；求职/请客等新生活动词。
- 站点入口改名 index.html；已读不回比例阈值待体感反馈。
- LimeZu 视觉层；Godot 平移；公开版模型通道分层（callClaude 唯一替换点）。
- 留存层暂缓清单：写信、章节制、城市档案、星露谷式心事件、代际重开。
- Actions 运行环境 Node 20 弃用告警（gate.yml 例行维护时一并处理）。
