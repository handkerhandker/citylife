# 交接说明（citylife · 云港小事）
更新：2026-08-07 · 代码基线：**v36**（第 23／24 单游戏代码零改动、不涨号；**第 25 单 v34**（只动渲染层与门禁）；**第 26 单 v35**（动了 SIM 块新增 CATCHUP 段，但 `Sim.step` 逐字未动、世界指纹不变）；**第 27 单 v36**（动了 `decide()` 的闲聊分支与整个兜底文案层，rng 流位移，世界指纹 `d19035af…` → `2d266238…`，任务书授权）。**v34／v35／v36 三个号由第 28 单补正**——这三单都动了游戏代码却没涨号，界面上那个数一直钉在 v33；补正后立涨号铁律（宪法第 27 条，门禁＝`versiongate.js`，对照表＝`version-ledger.json`）。第 28 单本身不动游戏代码，故不另占号。）

## 项目一句话
《云港小事》：LLM 驱动的都市生活旁观原型。四个 AI 小人在同一城市自主生活；玩家每天至多发 3 条短信旁敲侧击，不能操控任何人。

## 仓库文件
- main 为唯一权威（宪法第11条）。
- city-life-framework.html：整个游戏，单文件（HTML+CSS+JS 内联）。游戏内 设置→AI 节最后一行「构建版本」显示当前版本号，**唯一权威处＝`html:405` 的 `<span id="set-build">`**（全仓零 reader，无 JS 读写、无断言消费；门禁＝`versiongate.js`，宪法第 27 条）。**本行原写「基线 v12」，停在第 1 单从未跟进，由第 28 单改成指向权威处而不再写死数字。**
- harness.js：无头验证门脚本。
- sim30.js：30 天退化检查门禁（固定双种子六项；常量区逐项写明量尺判定、5 万颗实测四数、阈值算术与反向自查结论）。**第 26 单扩：两种模式各跑一遍十条判据——`fresh`＝从零跑 30 天（原口径一字未动）、`catchup`＝从存档补算 30 天（离线追帧的真实形态，逐拍走 `Sim.catchUp`）。两种模式不要求读数逐字节相同，只要求十条各自成立。**
- walkgate.js：走位三铁律门禁（第 20 单立；逐帧位移上限／全程不穿实体／显示位单一来源，含反向自查）。**第 25 单扩：新增「渲染停摆普查」节——七种「渲染停摆而 SIM 继续」的情形 × 四条断言（真显差恒零／逐帧位移上限／零穿实体／直置零出白名单），停摆模型由源码读出而非写死。第 26 单扩到八种（新增「⑧ 离线追帧补算」），并新增「位置判据」一节：补算不许出现在 `loop()`／`draw()` 里、必须排在 `state.vis` 建表之前——这条不是采样判据而是源码结构判据，因为「补算期间没有帧」这件事采不到。**
- worldsig.js：**世界指纹**采集器（第 24 单立，宪法第 26 条）。诊断工具不是门禁——不进 gate.yml、不判红、只印一个 md5；采集口径与覆盖盲区写在文件头。
- versiongate.js：**涨号铁律门禁**（第 28 单立，宪法第 27 条）。两层判据：①版本对照表（`city-life-framework.html` 整份字节的 sha256 与界面上那个号绑死，不依赖 git）；②git 差分（相对与 `main` 的分叉点，HTML 变了而号没变即判红）。反向自查**拿 git 里六个真提交对喂**（第 25／26／27 单三红、第 22→23／23→24／21→22 三绿），git 不可达时退回合成场景并印 SKIP。带 `--登记` 开关把当前号与字节写进对照表（**只有这个开关会写盘，门禁本身绝不自动写——自动写等于这道闸自己把自己关了**）。
- version-ledger.json：构建版本对照表（第 28 单立，宪法第 8 条硬名单登记）。`.条目`＝已发版的号→HTML sha256（判据用）；`.历史`＝第 28 单查清的 v32–v36 取证账（不参与放行，但由门禁拿 git 逐条自核）。**它不是版本号的第二处定义**，权威处始终是 `html:405`。
- tools/offline-catchup/probe.cjs：第 26 单五问机理取证（世界推进的驱动链／存档口径／推进 N 拍的耗时／模板兜底形状／存档恢复的可复现性，另附「读档时 drainLog 会重新点着几条 AI」）。诊断工具不是门禁。
- tools/offline-catchup/shots.mjs：第 26 单实机取证（真浏览器七档离开时长）。存档由生产自己落盘后取出，只改 `meta.at` 一个数造「离开了多久」；改过的档用 `addInitScript` 在**新上下文**里先于页面脚本写入——同页 `goto` 会触发旧页 `pagehide→saveNow()` 把 `meta.at` 盖回此刻，取证前提当场失效（已实测踩中，写在文件头）。
- tools/asset-pipeline/pageswitch_shots.mjs、pageswitch_sheet.mjs：第 25 单切页返回取证（连续帧＋前后对照拼版）；切页走生产 `setScreen()`、出画与否问生产判据，故截到的是生产代码本身的行为。
- 宪法.md / 交接说明.md（本文件）/ 待办.md：三件套。

## 固定验证门（宪法第3条所指；GitHub Actions 对每次 push 与 PR 自动执行，PR 页 gate 红叉即未过门，禁止合并；定义见 .github/workflows/gate.yml）
本地手跑同套**八步**（仓库根目录，全绿才算过；**第 28 单由七步扩到八步，新增涨号铁律那一步**）：
```
python3 -c "import re;m=re.search(r'<script>([\s\S]*)</script>',open('city-life-framework.html').read());open('app.js','w').write(m.group(1))"
node --check app.js
node harness.js
node sim30.js
node walkgate.js
cp functions/relay.js relay-check.mjs && node --check relay-check.mjs && rm relay-check.mjs
node versiongate.js
python3 -c "import re;s=open('city-life-framework.html').read();m=re.search(r'/\*SIM-START\*/([\s\S]*?)/\*SIM-END\*/',s);assert m and 'Math.random' not in m.group(1);print('SIM 纯净')"
```
**`gate.yml` 的 `actions/checkout` 自第 28 单起写 `fetch-depth: 0`**（默认的浅克隆取不到与 `main` 的分叉点、也取不到反向自查要喂的六个历史提交；取不到时 `versiongate.js` 退化成「只剩第一层＋合成场景」并如实印 SKIP，不会静默变绿）。
app.js 与 relay-check.mjs 为临时产物，禁止提交。harness＝快检（布局/寻焦/JSON/8 天不变量/种子化/指标纯函数/站位表几何）；walkgate.js＝走位三铁律（复演主循环逐帧采样 48 万人帧＋源码原文反向断言）；sim30.js＝30 天六项退化检查（固定双种子：生存不变量、动作熵、财富基尼、社交密度、导演与时间锚点、同种子确定性），全部阈值集中于其顶部常量区（保守初值，注释可调）。改动若波及门禁本身，须任务书明确授权并在 PR 说明。

## 交付与 PR 口径（第 23 单起，决策者裁定，往后所有单的默认做法）
**施工产出后直接开「草稿（draft）PR」指向 main，不必再问决策者要不要开。**
- **draft 状态即对应「等决策者点头再标 ready」**——开草稿不等于呈交验收，点头之后才 mark ready。
- **必须开的理由**：`gate.yml` 只在 `push: [main]` 与 `pull_request` 上触发，
  **光推分支门禁一次都不会跑**，PR 页没有 gate 结果，参谋无从独立验收（宪法第 5 条：证据优先于声明）。
- **收工报告给两个链接**：①PR 页 ②本单交付件文件（`docs/交付/第NN单-*.md`）。
- 仓库现无 PR 模板（`.github/` 下只有 `workflows/`），PR 正文照宪法第 2 条第 4 款的六项写：
  用户可见变化摘要、用户可见文案原文、门禁逐项结果、双审计固定行、改动逐处归因、已知接受项与待决策项。

## 代码结构（单文件内三层）
1. /*PURE-START*/…/*PURE-END*/：纯函数（decideLayout 布局判定、fmtTime/weekday、navScore 空间寻焦评分、parseJsonLoose 宽容 JSON 解析）。可被 node 直接测试。
2. /*SIM-START*/…/*SIM-END*/：模拟核心（makeWorld / step / decide / sendMessage / sendCustomMessage）。10 模拟分钟为一拍。文件内有 typeof document 守卫：node 环境 require 时在 DOM 层之前 return 并导出 {PURE, Sim}。SIM 块所有随机数经 w.rng()（mulberry32 种子化；makeWorld(seed) 可指定，未指定取时间派生）；SIM 块内禁止出现 Math.random（CI 强制检查）。URL 加 ?seed=数字 可复现同一局（AI 生成文案除外）。随机源状态存于 w.rngState（存档友好），serialize/hydrate 提供世界（反）序列化（SAVE_V 版本护栏）；存档续跑与连续运行同命运由 harness「存档往返」断言保证。
3. DOM 层：布局（#app[data-layout] 四模式 compact-portrait / compact-landscape / medium / expanded，靠 grid-template-areas 重排）；输入抽象（键鼠/触屏/手柄 → dir/confirm/back/tabPrev/tabNext，data-input 驱动提示条）；虚拟焦点（.vfocus + 空间寻焦，作用域感知对话框）；画布相机（保底可读缩放、跟随选中小人、拖动平移、"回到跟随"）；渲染纪律＝建一次、原地改数（禁止周期性整块 innerHTML 重建）。

## AI 层（多脑兜底链）
- 三级兜底链（rawCallClaude）：通道1＝Claude 免钥直连 fetch https://api.anthropic.com/v1/messages（model claude-sonnet-4-6，max_tokens 1000，仅在 claude.ai 工件内有效）→ 失败则通道2＝同源中转站 POST /relay（EdgeOne Pages 部署后有效，背后 DeepSeek，密钥只在 EdgeOne 环境变量）→ 再失败回退模板。callClaude 的一次自动重试作用于整条链。当前通道在 设置·AI「通道」行显示。
- 三挂点：短信（inner 内心独白＋reply 回信原文或空字符串＝已读不回）；同屋闲聊（整段对白＋双方各自"记在心里"a_mem/b_mem，制造信息不对称）；睡前日记（每日 21:50 触发、四人一次打包调用、触发时当场立条目锁时间戳、占位"（在台灯下写着⋯）"、回包后 patch 填入）。
- **不补生成的水位线（第 26 单）**：`aiFloorLid`＝开机补算完成那一刻的 `w.lidSeq`，`drainLog` 的 AI 闸加 `e.lid>aiFloorLid`。缘由实测——开局游标是 `-Infinity`，读档后 `w.log` 每一条都算「没上过墙」，跑满 30 天的存档**每开一次页就要为 44–62 条旧闲聊重新掏一次钱**。水位线只挡 AI 不挡上墙，历史照旧显示。配套两处：`rawCallClaude` 首句 `if(catchupActive){catchupAiHits++; return null;}`（可计数硬断言，排在 `L.calls++` 之前）；`settleOrphanLLM` 在开机时把「关页时正在等回包」的条目照 AI 挂掉那条路收尾（回落 `e.fb`、清 `llmPending`、被读掉的短信补「已读不回」；**第 27 单起日记条目也随身带 `e.fb`，故与对白／短信同走一条分支，旧档里没有 `e.fb` 的日记条目就地从该住户那一池现抽一条**），否则水位线会把占位符「（聊得正起劲⋯）」永远钉在墙上。
- 管线：请求队列并发 2＋优先车道（日记插队）＋失败 0.8 秒重试一次＋15 秒超时（Promise.race 实现）＋状态可观测（设置·AI 调用统计、短信页状态灯）＋✨ 标记 LLM 文案＋任何失败落模板兜底＋首次最终失败向城市日志广播 ⚠ 一次。
- 喂给模型的角色卡：特质/心愿/担忧（钱<300 动态变"凑不齐房租"）/钱包/饥饿体力/正在做/今天的心境（处境档位标签，第 18 单起只给档位不给事件原文）/说话方式（语言指纹）/本次开场派活/上次开口/最近 3 条个人记录。

## 关键设计口径（宪法第7条冻结对象）
特质约束行动空间；心愿担忧上角色卡；时间锚点＝周五 18:00 发薪、每月 2 号 8:00 交租 ¥200、周日街市；导演层＝200 分钟无戏后 30% 概率下雨 90–140 分钟；短信每日 3 条 0:00 重置；同一对角色闲聊 90 分钟冷却；口水话池防连续重复；短信页只显示真实电波（你发的 / TA 回的 / 已读不回），内心独白只进城市日志；代谢与阈值逐人差异化（hungerRate/energyRate/eatAt/napAt 集中于 agents 卡 metab 字段，注释可调）；**作息逐人人格化（第 22 单起）——就寝钟点／睡眠时长／规律性／周末赖床／逆境反应／硬扛系数六项一律由 `RHY_TRAIT` 按特质推导，起床时刻＝躺下时刻＋今晚该睡多久（不再是参数）**；模板独白全部入池并经 pickV 防连续重复；**离线追帧（第 26 单起）——城市在页面关着时不走，回来时按「1 真实分钟＝1 模拟分钟」补算，封顶 3 模拟日，补算全程零 AI 且回来也不补生成；离开期间跨过至少一个「夜深了」（`REFLECT_MIN`＝21:50）**且补算期间真出了新剪辑**（判据＝`clipTopDay` 最新剪辑日变大，**不是 `w.clips` 条数增量**——条数被 `CLIP_KEEP` 封着会在第 61 个剪辑日自锁）才把剪辑页推到玩家面前，两条不同时满足则不打扰**。
生活类活动时长与工作独白节奏经逐人 tempo 参数（durMul/durJit/mutterGap，兜底 TEMPO_DEFAULT）错峰，随机走 w.rng；独白经 pickV 当日去重（第 13 单挑剔升级为全城一本账：一句当天任何人说过即不再抽；不同数组不得共键，lw 键并入 workKind；w.saidDay 记账跨天重置，池尽允许重复；共用池已按全城日耗峰值扩容）。**第 27 单起全站抽词共两支：`pickV`（当日去重，记 `w.saidDay`）与 `pickFresh`（最近 keep 条不再抽，记 `w.fbRecent`）——后者专给「一天只抽一次」的挂点（睡前日记），那种节奏下 pickV 的当日去重恒为空转；等概率的 `pick` 已随 `CHAT_LINES` 一并撤掉，全站无调用点。**
AI 调用链带保险丝：AI_TIMEOUT 集中死线（直连/中转/打包），线路占还成对（任何结局必还线），部署站直连首败后本局跳级，日志墙不等 AI（规则句当拍上墙、✨ 迟到原位补、缺席模板顶）。
角色语言指纹走「代码派活」（DOM 层 AI_VOICE 覆盖表＋styleAssign；SIM 块 voice 原文零改动）。两条教训写死：①点名短语治不住换词绕过（「你闻见没」→「欸你看见…没」），故不用禁词表；②纯提示词配额同样不成立——真模型实测四人各 6 次，三人不达标，病根是配额窗口（3-5 次）大于注入窗口（1 次），模型看不见自己前几次的开场，再听话也无从遵守。故配额改由代码记账并当场派活：轮转挂点（对白/日记）每次指定一类开场（直接说事/抱怨吐槽/直接问对方/接对方上次的话，逐人相位偏移防同批撞车，撞上次派的类则顺延），招牌起手式每 5 次、明喻每 3 次由代码发放许可（招牌轮次不派类目，二者天然互斥）；短信挂点按情境豁免——回信天然承接来信，不派轮转，只管两项配额。卡里只留底色与招牌式定义，自律式配额语一律移出。记账字段 aiTurn/lastOpenKind 随存档序列化、旧档缺省兼容（照 lastUtterance/tempo/saidDay 先例，缺省不判坏档、代码兜底）。验收铁律：桩测试只验条款写没写清，**不验真模型守不守**——每次改提示词或派活规则，必须用 tools/voice-check 跑真模型复测。
AI 文案层返工（第 18 单，v29）：四处硬伤，治法总纲＝**凡能由代码收走话语权的，一律不靠嘱咐模型**（前三次同类返工都栽在「把条款写清楚」被当成「治好了」）。①**处境状态只上档位标签**：`agentCard` 那行由注入 `ag.sit.text` 改为注入 `SIT_MOOD[ag.sit.k]`（顺／逆／平淡三档中文标签），事件原文一律不上卡——模型拿不到就无从复述；`ag.sit.text` 仍留世界状态（后续日报/剪辑要用），只是不喂 AI；档位不明（篡改档）整行省略；harness 逐条正则扫 28 条事件文案 ×336 张卡锁死零泄漏。②**语气词起手结构闸（方案乙启用）**：生成后检查开场（剥引号括号空白后取前 `LEAD_SCAN`=10 字）是否以 `LEAD_INTERJ` 表里的语气词起手，命中则**由代码指定另一类开场**重生成一次；**判据是结构不是禁词**——禁词表已实证两次可绕过（第 13 单「你闻见没」→「欸你看见…没」，v28 又退回原句）；至多一次、失败即接受、受 AI_TIMEOUT 约束、绝不阻塞日志墙（模板句落条目时已上墙，AI 只是迟到补写）；覆盖短信/对白/日记三挂点；`ZHAO_EVERY` 配额仍在，本闸是配额之外的兜底。③**闲聊话题也由代码派**：第 13 单只派「怎么开口」不派「聊什么」，话题归模型自选必然收敛到公寓最典型物件（实证：七段闲聊七段围着冰箱，同一盒酸奶跨三天反复过期）；现由 SIM 的 `pickTopic` 每场派定一类写进日志条目，`enhanceChat` 照此拼提示词，模型只能在该类里发挥；频次过滤 `TOPIC_RECENT` 全城一本账，撞上顺延；**每场恰好消耗 1 次 rng**（v28→v29 唯一的 rng 流位移点，sim30 指纹随之变更，见第 18 单交付件）。④**日记挂点与对白挂点分家**：机理已由代码证实——`OPEN_KINDS` 里「直接问对方」「接对方上次的话往下聊」本身就是对话指令，v28 原样派给日记挂点，等于代码在命令模型把日记写成对话体（决策者两段实证一一对应；其中「你刚才说散步到第三个路口…」复述的是 STROLL 模板独白原文，经「今日片段」上卡后被「接对方上次的话」误认成他人话语）。治法三道：日记走 `DIARY_OPEN_KINDS`（四类全为写给自己的起手）、日记不发招牌许可（招牌是开口的套路，且 a2 的招牌按定义就是第二人称构造）、`runReflection` 提示词写死日记体铁律五条（只写给自己/不得第二人称/不得提问/不得复述他人话语与误认自己的独白/不得对白结构）。**前两道由代码收口、机器验得了；第三道是纯提示词，只能目验**（照第 13 单教训，桩测试验不了模型守不守）。
同锚错开落位（第 19 单，v30）：`ANCHORS` 是「一功能一点」，同一活动的多名住户被派到同一个锚点，而显示坐标由锚点几何直算（`v.x=a.x+0.5/v.y=a.y+0.5`）**逐人零区分**，故 N 人渲染到同一像素、只看得见最后画的那一个；**落位逻辑里没有任何占用检查／让位／排队**（全仓唯一的 queue 是 AI 请求队列），且 `pixStandPos` 对全部 15 个锚点均为空操作（无一锚的脚底格落在 `PIX_SOLID`），钳制器也救不了。30 天双种子实测：15 个锚点中 **7 个**会被多人同占且全部真实发生过（客厅 `home_table`/`home_tv` 天天发生、最长连续 190 分钟；厨房与便利店隔天量级；公园／江边／街市偶发但确有），全城 20.3%/20.7% 的拍至少一处在叠人——**稳定复现不是偶发**。治法取纯显示层错开（任务书第一优先档）：DOM 层登记 `STAND_SPOTS`（锚名→四个相对该锚显示点的**格偏移**），按住户在 `agents` 数组中的固定下标派活——零 rng、零状态、零占用查询，故同锚任意两人恒取不同站位，且一个人的站位不因别人来去而移动。**「补足落脚点」整件在渲染层完成，不必往 `ANCHORS` 加条目、不必改 `decide()` 派活分支**（第 11 单「坐标与规则解耦」已实证，SIM 只认锚名），故任务书第二档塌缩进第一档、第三档无从谈起；**本单零让位逻辑，「占用冲突导致角色做不成活动」结构上不可能发生**。口径三条：①只在「已走到」时生效，判据是 **`v.path` 空**而非 `v.moving` 为假（半路按暂停时 `v.moving` 也是假，那种情形偏移会让人横移）；②行进中零偏移、显示照旧＝SIM 插值原样，切换走既有平滑器限速滑动（沿用补充指令四）；③登记的是相对偏移不是绝对坐标，`ROOMS`/`ANCHORS` 日后迁址自动跟随。配套改齐：画布点选测距改为两条渲染路径都认 `dspX/dspY`（否则错开后点不准）。SIM 零改动、rng 零位移、`ANCHORS` 零改动，**sim30 双种子指纹逐字节不变**（`4061fd2e…`，满足《云港建筑规范》五.2 坐标类铁笼）。交付件＝`docs/交付/第19单-人物落位重叠排障.md`（含全量普查表与七处前后对照图）。
站位并入寻路 ＋ 走位三铁律（第 20 单，v31）：第 19 单把站位做成**渲染期贴在锚点显示点上的装饰**，寻路与平滑器都不知道它存在，于是走到锚点那一帧贴上、起步那一帧撤销，**各跳一次**；而平滑器有一道「位移 >3 格即视为传送、直置」的**距离旁路**，站位偏移最大 4.61 格恰好整个溜过去 —— 第 19 单交付件登记的「一次短程横滑」，实测是**最大 4.67 格的整帧瞬移**，30–69 次/3 天（把偏移关掉即 0 次，故全部由它引起）。该「已知接受项」第 20 单撤销。治法走任务书第一优先档：**站位成为寻路的真终点与真起点**——`buildPath(from,to,spot)` 的终点由「锚点显示点＋站位偏移」算出，起点恒取 `v.x/v.y`＝人当前实际所在处；`draw()` 里贴偏移那两行删除，显示坐标的产出收口成 `stepDisplay()` 一个函数；距离旁路拆除，改认显式 `v.warp` 标（只有首帧落位与 reduceMotion 直置两处设它）。**「贴上／撤销」这个动作不存在了，跳变从结构上消失，而非被限速掩盖。**另两处一并治：①**家具遮挡**——深度排序（按脚底 y 把小人与 `PIX_FURN_TALL` 混排）本来就有、也工作正常，错的是**选点**：餐桌左席 `[-3,0]` 落在 (2.5,9.5)，脚底 y=10.0 小于餐桌脚底 y=10.813，于是排序把餐桌排在它后面画、盖住人身 29.7%；改 `[-3,1.5]` 即可，并把漏掉的判据补进站位表口径④（**包围盒与家具矩形相交时，站位脚底 y 必须严格大于该家具脚底 y**）。②**直线穿桌**——**只有一成同根**：按成因拆开逐帧统计，「寻路行进中」穿实体 1169 帧/3 天、「偏移贴撤滑动」仅 87 帧，而把偏移整个关掉前者几乎不变（1160 帧），**九成以上是第 19 单之前就有的老账**——`buildPath` 的同房间捷径只给一根直线，客厅三锚互走时正压过餐桌实体格。故新增**避实体走线**：`segHitsSolid`（0.02 格采样）＋ `bfsAround`（非实体脚底格四邻域 BFS，邻域序写死＝确定性唯一来源）＋ `avoidSolids`（只对撞上实体格的那一段改线，其余段逐字不动，再拉绳简化）；`buildPath = avoidSolids(buildPathRaw(...))`，原文逻辑一行未动地留在 `buildPathRaw`。`PIX_SOLID` 全在 `APT` 内，楼外走线永不触发，B 案骨架判例零影响。三条铁律落成 `walkgate.js` 进 CI（判据与踩坑理由见踩坑备忘「走位三铁律」条）。SIM 块 md5 逐字节一致、**sim30 指纹 `4061fd2e…` 未变**。交付件＝`docs/交付/第20单-站位落地返工与走位铁律.md`。
每日剪辑·选材层（第 21 单，v32）：连续直播没有「回来发现」的时刻，一天的事全倒在流水账上，只能靠文字撑观感；**每日剪辑是治这个的第一步——一天过完，导演层挑出最值得讲的那个人单独呈现**。本单只做「挑」不做「写」：**全部文案为固定模板，零 LLM 调用点**（判据成不成立全看挑出来的东西本身有没有戏，先摆事实让决策者判断，二期再上 AI）。判据由决策者裁定＝「**谁今天最不像平常的自己**」（落差最大者），不是「最反常的事」、也不是「串成一条线」。落地口径六条：①**一天的边界取凌晨 4 点**（窗口＝[D 04:00, D+1 04:00)），因实测全城最晚就寝在日切后 1250 分钟（次日 00:50）、最早起床在日切后 130 分钟（06:10），按 0 点切会把夜猫子的就寝记到第二天头上，而那正是「作息异常」的取数来源；门禁按真值锁死「每窗每人恰好一次入睡、一次醒来」。②**三级权重＋丙级封顶**治任务书点名的「落差最大 ≠ 最有戏」：甲（有戏）1.2–3.0／乙（弱信号）0.6–1.0／丙（纯行为频次）每条 0.2 且当日合计封顶 0.4，故甲级最低是丙级单条的 6 倍、封顶的 3 倍；**行为侧证据比权重表更有力——60 天入选卡上甲 99 条／乙 13 条／丙 1 条，没有一天靠纯行为频次入选**。③**共性折减**：同一偏离项当天 ≥3 人同时命中即判为城市事件（发薪／交租），权重 ×0.25——不折的话每逢周五必然挑出一个「钱包大额进账」的人，而那天四人一模一样。④**常态化折除（本单跑出来才发现的坑，最值得记）**：绝对阈值判据描述的是**长期状态，而长期状态恰恰就是「平常的自己」**。第一版沈小满 62 天占 42 天、白一鸣一颗种子零上榜——她工资 300、大方、餐餐 ¥20，钱包天天见底（命中率 0.57）；她外向、`decide()` 社交分支对她恒真，天天跟三个人都聊（0.73）。治法＝逐人给每条绝对判据记命中率账，**≥0.5 即判为此人常态、该项不再计入他的落差**；账记**候选**而非计分后的那份，否则一折除命中率就再也变不动、闸会自锁。治后四人 15/15/16/14（60 天，均匀为 15）。⑤**基线取逐人「开局至今累计均值＋标准差」、不含当天**（只存 n/Σx/Σx² 三个数，存档不随天数膨胀；本作无长期漂移故不用滚动窗，周期性交给共性折减而非让基线去吸收）；**每指标另设 sd 下限与最小可报偏差**，否则作息恒定的人晚睡 10 分钟会被算成无穷大落差。⑥**头几天口径**：需基线的判据满 3 天才启用，不足时只走绝对判据并在卡头挂标记；不完整的窗（开局首日 08:00 开城／存档中途接上）照出剪辑但**不计入基线**。**呈现**＝新页「剪辑」（✂）插在日志与短信之间，页签排列现场／角色／日志／剪辑／短信／设置；每天一条倒序、可翻 60 天；每卡含天号周几、挑中谁、逐条偏离项（带计分后权重与折减标记）、当天相关日志原文若干条（**直接引用不重写**）、页脚四人落差分与基线天数。**SIM 零 rng 消耗、零 w.stats 触碰、零住户字段写入，sim30 指纹 `4061fd2e…` 逐字节不变、雨次数 53/49 与 v31 一字不差**。零 rng 由两道独立证据背书：源码原文断言（**断言前必须剥注释——本段注释里成段写着「零 rng」，不剥会被自己的说明文字命中，首次编写实测踩中**）＋运行期隔离实验（同种子两世界推到日切前一拍，把对照组日表提前改成下一窗使其不结算，比较那一拍各自摇了几次 rng，实测相等且随机源状态逐位相同）。交付件＝`docs/交付/第21单-每日剪辑一期.md`（含 60 天选材全表、四人分布、「有几天值得看」读数、七条零命中判据逐条交代、实机截图）。
作息人格（第 22 单，v33）：第 21 单交付件自行判定「判据的分辨力已经超过了世界的丰富度」——四人起床时刻 30 天**零方差**（`decide()` 把 `busyUntil` 设成「唤醒时刻+10」，逐分钟固定 06:10／07:10／07:10／08:10）、清醒饥饿被各人 `eatAt` 顶死（峰值 78 vs 绝对线 88）、清醒体力被 `napAt` 托住（谷值 14.5 vs 绝对线 12），七条偏离判据零命中里有五条是**世界结构上不产生那种状态**。本单治世界这一头，不治判据（剪辑层判据与权重表一字未动）。**三条口径写死在实现里**：①**差异落在人格上**——逐人参数一律由 `RHY_BASE` ＋ `RHY_TRAIT`（特质 → 七格）逐条相加得出，**表外没有第二处赋值**，门禁逐格核对；规律性 `reg` 本身是人格的一格（陆知秋［工作狂］0.10 最铁打、顾云帆［夜猫子·摸鱼］0.85 最松散），**「有人天天六点二十起、有人今天七点明天十点」这个差别就是这一格做出来的**。②**波动有成因，不许是纯噪声**——今晚几点睡／睡多久是「此刻已经成立的世界状态」的函数（挂着什么处境／明早是不是周末／昨晚是不是睡饱了／今天眯了几觉／此刻体力剩多少／玩家发没发短信），**本段零 `w.rng()`，一次骰子不掷**（门禁：源码原文剥注释后零 rng ＋ 反向自查）；**起床不再是参数，是「躺下＋睡多久」的结果**，故每次晚起都追得到因果链。其中「昨晚睡饱了 → 今晚睡不了那么久」是**两晚成链**的一环，反常早起主要由它出活。③**生存红线**——就寝／起床／时长三处硬钳位 ＋「饿到 `RHY_HUNGER_FLOOR`=92 一律先吃」＋「累到 `RHY_ENERGY_FLOOR`=12 一律先睡（工作时段也不例外）」，故长期饿着／体力见底不睡结构上不可能；钳位同时把第 21 单「每窗每人恰好一次入睡一次醒来」由实测碰巧升格为**由构造保证**（就寝 ≤ 次日 01:00 ＋ 单次活动最长 100 分 ⇒ 躺下不晚于 02:40，起床 ≥ 05:00）。**饭点也逐人化**：＝本人上班起点（含工作狂 −30，不含玩家短信 `earlyWork`）＋ `RHY_LUNCH_AFTER`(180)，四人 11:30／12:00／12:30／13:00 天然错开，未引入新数字。**挂着逆境时「到点就吃」那一支整个让位**，只剩「实在饿了才吃」而那条线被逐人 `grit` 抬高——这是「饿着没吃饭」的成因。**踩过的两个坑记死**：(a) 成因若只挂高频状态（处境逆境约占 43% 人天），分布会变成两个等高的峰、标准差被撑大，剪辑层的 z≥1.5 永远够不到，故**常见成因幅度要小、罕见成因幅度要大并允许叠加**；(b) 成因链的门禁必须立在**排期函数**上而不是涌现出来的入睡时刻——实际躺下＝max(排期, 手上活儿干完)，若那天他忙到比三档都晚，三档会落在同一拍上，比出来的是活动时长不是成因（首版实测踩中）。实测（双种子 60 人天）：四人起床标准差 0/0/0/0 → **54.0／26.6／10.5／44.2 分钟**，散度排序＝规律性排序、极差 5.1 倍；第 21 单七条零命中判据**转正四条**（反常早起 5 次／反常晚起 6 次／饿着 12 次／体力见底硬撑 1 次，均为入选卡计数）；剪辑大类覆盖**作息 3→19 天、生存 5→24 天**，薄卡 26→3 天，强档卡 39→53 天。**未治两条照实登记**：`hunger_nomeal`（一天没吃）结构上不可能——日产饥饿约 130 点而生存红线 92，只要「饿了就吃」还在就打不着；`money_flatpay`（发薪日没涨）本单不动收入故仍为 0。`sim30` 指纹 `4061fd2e…` → **`484e5cb2bfecbcce7fb2023b441958a2`**（本单授权），六项阈值一行未动。交付件＝`docs/交付/第22单-作息人格.md`（含改前改后作息分布对照表、七条判据逐条命中数、60 天选材全表）。
雨密度余量复核（第 23 单，代码基线仍 v33，**游戏代码零改动**）：sim30 第五项的量尺由「雨次数 1–59」换成「**雨占全时长比例 10%–18%**」。**这条闸不是余量薄，是已经红了**——5 万颗种子实测 `RAIN_MAX=59` 击穿 40 颗（0.080%），零代码改动、只换颗种子（580→61、644→60）门禁就红。**立法本意**（PR #4 逐字）：`49×1.2=58.8 → 取 59`，初值 40 抄自对标件 hezuriji `scripts/sim-check.mts` 的「导演层下雨有触发且不失控（2~40 条）」，而云港自家导演层「按设计约 1.5 场/天」＝30 天 45 场，**抄来那一刻就已越界**，第一次跑就只好 ×1.2 补一个 59 上去；本意是防「导演层跑飞」的**退化检查**，不是防体感太密（待办第 100 行早已写死「不得靠放宽 `RAIN_MAX` 解决」）。**「次数」为什么不是合适量尺**：`decide()` 全程不读 `w.weather`，**没有任何住户因为下雨改过任何一个决定**，雨在玩家那头唯一的存在方式就是**雨幕挂着**（`html:3157–3166`）＋顶栏 ☔ ＋两条日志；故体感＝雨幕挂着的时长占比，而次数只数边沿——**把一场雨时长加倍，城市湿一倍，雨次数反而 52/57 → 37/42，闸更绿**（反相关）。**新阈值口径**＝5 万颗实测「均值 13.8934% ±5×0.6807pp」向外取整到整数百分点 ⇒ 10%／18%，取整后实到 **−5.72σ／+6.03σ**、5 万颗零越界（旧闸同口径只有 +2.76σ）。**采集为门禁侧纯观测**（每拍读一次 `w.weather.rain`），只读不写、零 rng、不碰 `w.stats`，**世界状态签名 `d19035af…` 逐字节不变**、`city-life-framework.html` diff 为空；**但门禁指纹变了 `4061fd2e…`／`484e5cb2…` → `613ff573…`**，因为指纹口径是 `node sim30.js | md5sum`＝门禁自己那 20 行输出的 md5，而本单的任务就是改其中两行（已停手报告，见待办等决策）。**反向自查**：**17 种人为改动 ＋ 1 个基线对照**逐个实测（各 300 颗种子），新闸拦下 **13 种**、旧闸只拦下 **5 种**、只放宽阈值的乙案更差只有 4 种；新闸并补上旧闸整个盲掉的一侧（雨变少／雨变碎六种改动旧闸一种都没拦住，新闸六种全中）；基线 5 万颗零误红。诚实登记一处净损失：「无戏阈 200→150」（雨变重 20%）旧闸拦得住、新闸拦不住，且**这一点在任何量尺上都不可挽回**——基线实测跨度 11.04%–16.74%，要拦住 16.6% 就得把上限压到实测最大值以下，基线自己就会红。交付件＝`docs/交付/第23单-雨密度余量复核.md`（含四问机理报告、5 万颗分布表、三候选闸 18 项对照的反向自查表）。
sim30 六项阈值全面复核（第 24 单，代码基线仍 v33，**游戏代码零改动、世界指纹逐字节不变**）：把第 23 单那套流程（大样本→量尺判定→按分布定阈→反向自查）一次跑完六项。**三处量尺是错的，病根同源**——`w.stats` 的计数器数的是 `decide()` **派活次数**，玩家看见的却是那件事**持续多久**，故一件事拖长次数必然变少，闸与体感**反相关**：①**动作熵**——睡眠拉到 14 小时（一件事吞掉大半天），时长熵 2.079→**1.956**（真的更单调），**次数熵反而 2.096→2.195，闸更绿**；反过来把「在家待着」由 30 分切成 10 分（同一件事拆三段、丰富度没变），次数熵 2.096→2.053（假装更单调）而时长熵纹丝不动。②**街市**——每趟停留 ×3，广场上人·分钟 755→**1570**（热闹一倍有余），**到访人次反而 14.9→13.7**，下限闸离红更近。③**最长无戏间隔**——见下。故三条换尺：熵改**时长口径**、街市改**滞留人·分钟**、无戏改**「静默>200 分」时长占比**。**`GAP_MAX=1080` 的右尾成因查清了，不是世界缺陷**：击穿最深那颗（种子 47351 报 1210 分钟）那 1220 分钟里城市**推了 31 条日志、含一场真的闲聊**——`html:1557` 判的是**日志墙尾条**是不是闲聊，那场闲聊被后一条做饭日志顶下尾条就当没发生；另两成因是几何分布尾巴（雨窗内连续掷 22 次 30% 全落空，`0.7²²≈4.3e-4`）与**雨兜底**（把雨起停不算戏，最长无戏间隔由 均值 771／最大 1210 涨到 **均值 1393／最大 3560**）。**并撞出第 23 单那条口径的适用边界**：均值±5σ 只对**和式量**成立，`GAP_MAX` 是极值量（实测最大 +7.93σ），照口径算出的 1050 比现行 1080 **还红**（击穿 29 vs 11）。新阈值：熵 ≥2.0、基尼 ≤0.50、闲聊 ≥120／对数 ≥6、街市 ≥100 人·分钟、静默 ≤27%、钱包 ≥−110；雨／发薪／交租一字未动。**5 万颗训练集＋8000 颗留出集零击穿**；**34 种人为改动**反向自查判红 **25 种**（旧闸 16 种），**净能力损失为零**，净新增检出 9 种（其中「每日『新的一天』系统条不落墙」是六项里没有第二条看得见的）。旧闸此前的真相：**动作熵 ≥1.0 判红 0 种、最长无戏间隔 ≤1080 判红 1 种而那一种雨闸已 100% 抓住**——两条等于没立。`SILENT_K=200` 不是新数，是导演层自己写死的「太平淡」门槛（`html:1545`），该耦合已登记待办。交付件＝`docs/交付/第24单-sim30六项阈值全面复核.md`（含 5 万颗全量分布表、逐条量尺判定、GAP_MAX 成因专章、34 种改动反向自查全表）。
时间戳一律带日期（第 19 单追加一，v30）：**凡按时间排列、且同一屏可能同时出现不同日子的列表，一律走 `PURE.fmtStamp(t)`＝「D75 04:40」，禁止只印 HH:MM**。缘由（决策者实证 v29 线上）：短信往来记录呈现为 04:40/05:40/06:30/03:30/04:20… 看似时间倒流，实为三个不同日子——条目本身严格按 `e.t` 升序，日期信息是在渲染这一步被 `fmtTime`（只取 `t%1440`）丢掉的。全站排查五处按时间排列的列表**已全改**：城市日志 `#log-list`（400 条 ≈ **9.5 天**，比报案处更严重）、短信往来记录 `#ph-history`（14 条 ≈ 5 天，报案处）、侧栏 `#side-list`（120 条 ≈ 2.9 天）——**三者共用同一个渲染器 `logLine`，改一处即三处齐修**；角色页·最近记录与画布浮层最近两条为同缺陷的自渲染副本，各一行同改。存档信息行早已印 D 号（既有先例），并入同一函数后输出逐字不变。**豁免一处**：顶栏时钟 `#tb-clock` 是当前时刻非列表，且旁边 `#tb-day` 已印「D75 周四」。格式对标既有先例（顶栏与存档行），不引入界面上不存在的新名词。取「逐条打戳」而非「跨日插分隔条」的理由：分隔条**有状态**，而三个列表都会从头部裁剪、被 `patchLid` 原地替换、页面重载重灌，任一环节都可能把分隔条裁掉或错位；逐条打戳只依赖条目自身的 `t`，无状态、不可能错位。门禁另有一条**反向断言**锁死回潮：全站裸 `PURE.fmtTime` 只许剩 `fmtStamp` 内部与顶栏时钟两处。
语气词起手闸补上「哈」这条缝（第 19 单追加二，v30）：沈小满两条回信「哈？买房？…」「哈，这话说得跟我妈似的…」从闸下钻过去——病根是第 18 单自己写在注释里的取舍（「哈」单字会误伤哈尔滨/哈欠/哈密瓜，故当时只收「哈哈」）。治法**仍只看结构、绝不改回禁词表**，但把判据从「是哪个字」推进到「**字后面跟什么**」：新增歧义单字表 `LEAD_INTERJ_AMB`（现仅 `哈` 一条），其成员只在**后跟标点／空白／句末**时判为语气词（哈？／哈，／哈！／哈。／全句一个哈 → 拦），**后跟汉字即实词放行**（哈尔滨／哈欠／哈密瓜）。主表另加整词「哈喽」（哈＋汉字，在新判据下会被当实词放行，照「哈哈」先例以整词入表）。**排查结论**：主表 16 条既有条目逐条通查，各字作词首时除语气词外无常用实词，**无同类缝隙需补**。门禁两条断言锁死两表零重叠——歧义字一旦同时留在主表就会被单字直判、结构闸等于失效。误伤自查用例（任务书硬口径，实跑非推演）见交付件十一.4，覆盖哈尔滨／哈欠／哈密瓜／「哈哈」连用／「哈」在句中五类。
地图骨架经甲案重整（第 11 单，v25）：SIM 决策只经房间名/锚名，ROOMS/ANCHORS 坐标数字仅渲染层消费——坐标与规则解耦已实证；《云港建筑规范》（docs/云港建筑规范.md）为既有与未来一切放样铁律；sim30 双种子逐字节铁笼永久适用于坐标类改动（改前改后输出 md5 必须一致，不一致一票否决）。
城市骨架按《云港城市总规》（docs/云港城市总规.md，与建筑规范同等效力）B 案落地（第 16 单，v27）：广场为中心节点（列 18–26 × 行 15–22，常设街市与夜谈角在内）、江面为南锚（行 24–27，岸线步道行 23 贯通东西）、三处边缘扩张插座（西·二号住宅／东·餐馆与二号雇主／东南·码头水岸）登记为保留用地——将来扩张一律走插座，不动老城。
同楼房间通勤走内部连通（第 16 单，v27）：起讫同属一栋楼时经纵廊/门厅直达，不出楼门、不上街，跨楼通勤照旧经步行动脉；楼身归属与内部骨架由楼身分组表集中登记（DOM 层 BUILDING_OF / BUILDING_PLAN / BUILDING_BODY），仅渲染层走线，SIM 零改动。
**交付物一律落成仓库文件（第 17 单补充指令一立，本单起长期有效）**：任何交付物——PR 正文、机理报告、统计读数、事件表全文、对照表——一律写成仓库内的文件并推上分支，然后把该文件的 GitHub 链接给决策者。**不开 PR 的只读任务也照办**：单开分支放文件、不合并、链接照给。**禁止让决策者用截图或手工复制长文本转运。** 存放约定：按单归档于 `docs/交付/第N单-标题.md`（首件＝`docs/交付/第17单-外围角色事件表与统计读数.md`）。缘由：决策者手机操作，长文本转运既费事又易丢字。
**链接必须用提交号锁定，每次都是新地址（第 17 单实测踩坑，本条与上条同等强制）**：格式 `https://github.com/<owner>/<repo>/blob/<完整提交号>/<路径>`。**禁止给分支名链接**（`/blob/<分支名>/…`）——分支名地址内容会变而地址不变，决策者再次打开只会读到缓存里的旧版，看不见刚推的修改，且双方都不易察觉。提交号地址每次推送必然不同，天然满足「每次新地址」，且内容永久不可变、便于回溯对账。同一文件改一次就给一次新链接，绝不复用旧链接。取号：`git rev-parse HEAD`（推送后取）。
双道验收制度（长期有效，第 10 单补充指令四立、第 16 单重申）：机器自检管几何，决策者目验管观感，两道都过才算过；产出前置呈图（全景＋专项截图），决策者点头后方可开或更新 PR。
外围角色与处境状态（第 17 单，v28）：**外围角色＝一张事件表，仅此而已**——顾云帆·组长／沈小满·店长／陆知秋·客户／白一鸣·编辑，不进 ROOMS/ANCHORS、不在地图上、不走路、**不进日志墙**、不进角色页、不进短信；外围角色不得获得内心世界／日程／语言指纹／画像（他们是事件源，不是人）。四个称谓一律非新造：客户见 WORK_THOUGHTS.trade、编辑见 WORK_THOUGHTS.write 与 a4.fear（仓库文案）；组长/店长未写进仓库文案，但 AI 生成的对白中早已在用（第 17 单补充指令二，决策者截图实证），故不构成称谓出入。事件表按 workKind 挂（四人 workKind 互异，照 WORK_THOUGHTS 先例，语义等于按人），每人 **7 条＝3 好/3 坏/1 平、全城 28 条**，**每条只许 k＋text 两字段**（多一个字段就等于给外围角色发人设，harness 有断言卡死）。触发口径：只在该住户当天工作时段内、一天至多一次（`flags.peerDay` 封当日，封后当日零 rng 消耗）；命中即在该住户身上写 `ag.sit`（k 顺/逆/平淡、from 外围角色、text 处境短语、i 下标、until 到期时刻），**每人至多挂 1 条、新的顶掉旧的、到 until 自然消失**（不做定时清扫，按 w.t 判活）。**唯一去处是 AI 角色卡的一行**「今天心里压着的事」，无状态时整行省略——不上日志墙、不进 personalLog、不进角色页。**铁律：这是底色不是内容**，卡上那行写死了「不许直接提起它」的措辞，改文案时不得改成陈述句；验收看的是产出里不出现「因为被退稿了所以我今天……」这类播报式复述。防连日复读照 styleAssign「撞上次则顺延」先例，恰好消耗 1 次 rng。**好坏配平（补充指令一立、补充指令二定案）**：现行口径＝**逐人 3 好/3 坏/1 平，全城 28 条**。演进缘由须留档：补充指令一曾附带「每人 6 条不变」（顾虑 pickV 抽词池容量），因 6＝1 平＋5 好坏、5 为奇数，逐人对半不可整除，只得走「全城对半」变通；补充指令二撤回该约束（**本表不是抽词池，容量顾虑不成立**），奇数死结随之解开。**必须逐人配平的理由**：全城对半下顾云帆与白一鸣结构性偏逆、沈小满与陆知秋结构性偏顺，30 天累积成境遇系统性分化，会**污染本单假说的验收**——试玩者把「两个人一直倒霉」误读成「处境让四人说话分开了」；假说要验的是语气差异，不是运气差异。代价照实记：逐人配平使「四人同挂同一档」由 0.0%/0.6% 升到 1.4%/4.0%（每人 3/7 抽中 good 的算术必然），换来组合去重种数升至逐拍 150/154、日记时刻 30/30。harness 三条断言锁死：**逐人好坏相等**、全城 28 条含 4 平、28 条文案互不重复（互不相交是「四人同挂同一条」恒为 0 的结构前提）。
挑剔通道：文风与观感类调整走一句话挑剔、不另立单——AI_VOICE 四段措辞与各模板独白池文案为调剂点，素材观感则走流水线配方区重跑（见 docs/素材制作说明.md 第二章）。
修理型任务取证先行：任务书只锁病症与验收标准，病因假说仅为线索；施工先出机理报告证实或证伪，再按实修理，禁止照假说直接下刀。
视觉层素材走同源侧载（apartment.png 于第 16 单重产一次，仅补开两道内连门洞，其余布局零改动；characters.png 自第 9 单起逐字节未变）：两张拼合成品（assets/apartment.png、assets/characters.png）入仓随游戏一起部署（第 10 单补充指令二裁定，决策者零上传），渲染开关可回退旧画风，素材缺席自动回落零依赖。入仓物仅限流水线产出的两张拼合成品——自组布局创作，公开程度与游戏页面本身等同（决策者认可）；LimeZu 原包任何文件严禁入仓。原素材站（citylife-assets 项目，绑 assets.cydgames.com）已退役，决策者随时可删。素材成品由 tools/asset-pipeline 流水线从 LimeZu 原包程序化生成（捏人先斩后奏、决策者自然语言挑剔单点重生成，新成品随 PR 入仓自动上线）；素材验收双道制（补充指令四）：机器自检管几何（含整件±0 与逐件对外验真），决策者目验管观感（流水线附带三区实机预览截图 preview_shots.mjs，点头后方可开/更新 PR），两道都过才算过。原包经决策者私有 GitHub 仓库中转短暂过境、仅供授权施工会话读取，永不入公开仓库与公开路径——中转不构成分发，私仓永不转公开，内容用毕留存或删除由决策者自便。

## 硬编码名单登记（宪法第8条，新增须回写本节）
第 21 单·每日剪辑选材层（均在 SIM 块 `/*CLIP-START*/…/*CLIP-END*/`，随 `Sim` 导出 16 个符号供门禁按真值断言，改常量不必改门禁）：**可调常量区** `CLIP_CUT=4*60`（日切点，改它须复测「最晚就寝／最早起床」两个数）、`CLIP_KEEP=60`（保留天数）、`CLIP_QUOTE_MAX=5`、`CLIP_BASE_MIN=3`（基线启用天数）、`CLIP_COMMON_N=3`／`CLIP_COMMON_MUL=0.25`（共性折减）、`CLIP_FREQ_CAP=0.4`（丙级当日封顶）、`CLIP_HABIT=0.5`（常态化折除线）、`CLIP_BROKE=60`／`CLIP_STARVE=88`／`CLIP_DRAINED=12`（三条绝对线；后两条在现有代谢下结构上打不着，保留为兜底，日常由相对判据 `hunger_high`／`worn_low` 出活）；**`CLIP_NOISE`** 逐指标 sd 下限与最小可报偏差；**`CLIP_W`／`CLIP_TIER`** 偏离项权重与级别登记表（22 条，新增偏离项须回写本节与交付件）；**`CLIP_ABS`** 受常态化折除管辖的绝对判据名单；**`CLIP_LOGCAT`** 日志正文→类目表（取 `decide()` 实际写出的字串，**harness 有覆盖率断言：30 天里每条 act 条目都须归到某一类，谁改了 logText 而没同步这张表，那条即红**）、`CLIP_CATNAME`、`CLIP_FREQ_CATS`（丙级只数 eat/work/nap/stroll——**刻意不含 chat**，闲聊次数已由甲级 `social_burst` 按同一个数出活，两级同时命中会在同一张卡上把同一句话印两遍；**也不含 idle**，在家待着 logText 恒为 null 不上墙）、`CLIP_QCAT`（偏离项→引用类目）。世界新增三字段 `w.clips`／`w.clipDay`／`w.clipBase`（随存档序列化；旧档缺省从空账起并把接上的那一窗标记为不完整，篡改档畸形值就地重建，照 `w.chatTopics` 先例）。DOM 层新增：页签 `data-tab="clip"` 与 `#scr-clip`、`#clip-list`、`.clip*` 一组样式、`clipRows`／`clipQLine`／`clipCard`／`renderClips`（只往头部插新卡，不做整块重建）。截图工具 `tools/asset-pipeline/clip_shots.mjs`（**注意 `fullPage` 对本作无效——`#app` 是 100dvh 定高、各屏内部自己滚，整页截图与视口截图逐字节相同，要证明「翻得动」只能真滚一段再拍**；且须抢在第一次 AI 挂点之前关掉 `state.llm.on`，否则沙箱不通网会在日志墙留一条 ⚠ 连线失败干扰目验）。
第 22 单·作息人格（均在 SIM 块 `/*RHY-START*/…/*RHY-END*/`，随 `Sim` 导出 17 个符号供门禁按真值断言，改常量不必改门禁）：**钳位区** `RHY_BED_MIN/MAX`（21:00–次日 01:00）、`RHY_WAKE_MIN/MAX`（05:00–10:00）、`RHY_DUR_MIN/MAX`（6–9.5 小时）；**生存红线** `RHY_HUNGER_FLOOR=92`、`RHY_ENERGY_FLOOR=12`；**成因幅度** `RHY_GOOD=0.4`／`RHY_CRASH_BED=45`／`RHY_CRASH_DUR=60`／`RHY_OVER=30`／`RHY_REBOUND=70`／`RHY_NAP_BED=30`／`RHY_NAP_DUR=45`／`RHY_SMS_BED=60`／`RHY_SMS_DUR=30`／`RHY_LUNCH_AFTER=180`／`RHY_GRIT_E=0.7`；**负债刹车** `RHY_TIGHT_LINE=60`／`RHY_TIGHT_SPAN=120`／`RHY_TIGHT_GRIT=26`（钱越紧下一顿拖得越久，赤字因此有界——本单把作息摊开后开局那一周的顿数开始浮动，沈小满谷底由恒定 ¥−106 变成最深 ¥−126、击穿过 sim30 的 ¥−123，治法是负反馈不是调阈值；治后 20 种子谷底 ¥−70～−94）；**`RHY_BASE` ／ `RHY_TRAIT`**＝特质 → 作息人格对照表（8 条特质 × 7 格：bed/dur/reg/lie/owl/keep/grit；**钱的特质节俭／大方一律零贡献**，列在表里是为了证明没漏）——**新增特质或新增住户必须回写本表**，否则那个人的作息里会出现一段说不出理由的差异（门禁有「四人全部特质都在表里登记」一条）。住户新增字段 **`ag.rest`**（`bed` 几点躺下／`wake` 几点起／`dur` 上一觉睡了多久／`nap` 今天眯了几觉；随存档序列化，旧档缺省从空账起，篡改档畸形值就地重建，照 `ag.sit` 先例；**注意 `ag.rest.bed` 是时刻，与 `ag.bed` 那个床位锚名无关**）。`svAgentUsable` **刻意不给 `rest` 加必填校验**——加了旧档会被整档判坏。
第 27 单·兜底文案扩池（均在 SIM 块「兜底文案扩池」段，随 `Sim` 导出 `DIARY_FB/DIARY_FB_RECENT/CHAT_FB_OPEN/CHAT_FB_REPLY/pickV/pickFresh/diaryFallback` 供门禁按真值断言）：三张池表**一律按 `workKind` 挂**（`work/clerk/trade/write`，照 `WORK_THOUGHTS` 先例；**刻意不挂 `a.id`**——`svAgentUsable` 只校验 `a.id` 是非空字符串，却白名单校验了 `a.workKind`，故 workKind 是全站唯一一条已经有闸守着的索引）。**`DIARY_FB`**＝睡前日记兜底，逐人 15 条；**`DIARY_FB_RECENT=10`**（可调，**必须 < 各池长度**否则无候选可派）＝「最近 N 条不再抽」的窗口，取 10 的依据是**日志墙上同时挂得住的单人日记条数上限**（`w.log` 封 400 条，15000 颗种子 × 30 天实测最大 10、均值 9.002），故「墙上看得见的日记一条也不会重样」由构造保证；池容量 15＝窗口 10＋候选 5（候选数照 `pickTopic` 先例：该处 9 池记 4 条留 5 候选）。**`CHAT_FB_OPEN`（逐人 10 条）／`CHAT_FB_REPLY`（逐人 8 条）**＝闲聊兜底，**开口的那句按开口人抽、接话的那句按接话人抽**，pickV 键 `co:`＋workKind／`cr:`＋workKind（两个不同数组严禁共键）；容量照第 13 单口径「≥ 全城单日抽取峰值」实测定（同一份样本：开口峰值最大 9／均值+5σ=9.13 ⇒ 10；接话峰值最大 8／均值+5σ=7.94 ⇒ 8）。**接话句是万能承接**——它与开口句分两次独立抽取，故每条都必须能接在任意一句开口之后，写死具体回应内容必然错配。**代价已登记（决策者裁定四，2026-08-07）**：32 条接话句全部以承接语起手，离线实录里约半数对话答非所问；参谋判读为「第 18 单招牌起手式连招换了个形状——『欸』治好了，换成了『同感』」，候选治法方向是接话池按开口句类别分组、配对时按类取（不定案，见待办候选表第 14 条）。**文案内容口径（硬）**：只许用结构上必然为真的素材（一天到了尾声／各人长期心愿与担忧／职业的日常质地／性格特质／住处与去处／身体的一般感觉），**不许提任何具体事件**（外围角色言行、涨薪退稿、发薪交租、今天下没下雨、买了吃了什么、另外三位住户的言行）——与第 18 单「角色卡上给标签不给事件」同一条口径，缘由是兜底文案写死，一旦提具体事件必然有某天对不上；日记另守第 18 单五铁律；**三池文案自己也过第 18／19 单的语气词起手闸**（`leadsWithInterj`，harness 逐条断言——闸原本只查模型产出，模板若自带语气词起手等于闸旁开后门）。世界新增字段 **`w.fbRecent`**（`pickFresh` 的记账，随存档序列化；旧档缺省从空账起、篡改档畸形值就地重建，照 `w.chatTopics` 先例）；日记日志条目新增 **`e.fb`**（落条目那一刻就抽好，**不许等 AI 失败了再抽**——抽字要掷骰子，等失败再抽会让 rng 流跟着网络快慢漂移，harness 有源码顺序断言）。
第 29 单·落字排队 pen（均在 DOM 层「落字排队」段，紧邻三个 AI 挂点；**不在 SIM 块，不入存档，一次 `w.rng()` 都不掷**）：**`PEN_TTL_MS=20000`**（可调，占位符收尾死线，**真实毫秒**）＝墙上占位符能挂多久的唯一开关。取值两头夹定：**下边界**由健康请求定——单轮健康上界＝中转线路死线 `AI_TIMEOUT.relay=15000ms`（直连 4000ms 更短），留约 33% 排队余量 ⇒ 20000ms，**再往下压会连中转线路的正常回包一起砍掉**，墙上就几乎全是模板字；**上边界**由病症定——必须明显小于协议上界 `jobLimit×2+800=60800ms`，否则等于没治。它恰好卡在「一轮打得完、两轮打不完」之间。**换算口径（改这个数之前必读）**：主循环写着 `1 真秒 = 10 模拟分钟 × 速度`，故 1× 速度下 **1 游戏小时＝6 真秒**，20000ms＝**3.3 游戏小时**；决策者是按游戏小时体感的，改常量时请一并给出游戏小时数。`penReg` 是会话级登记表（按 `e.t, e.lid` 升序），**不落盘**——关页时在途的那些仍由 `settleOrphanLLM` 在开机时收（第 26 单，未动）。**本单零新增入档字段**：用到的 `e.llmPending`／`e.fb` 改前就在。四个函数各管一件事：`penAdd`（登记，须排在一切可能抛异常的语句之前）／`penReady`（回包到了，**返回 `false` ＝ 已落定，整包丢弃**）／`penFlush`（**只从队头连续放行**）／`penSweep`（死线扫描，**唯一驱动是主循环 `loop()`，全站没有第二个定时器入口**，harness 有断言）。三个挂点（`enhanceMessage`／`enhanceChat`／`runReflection`）一律 `try/catch/finally`＋`penReady`，**函数体内不再自己 `patchLid`**（自己贴就绕过了顺序闸，harness 有断言）。
**harness「无话题霸屏」那条同期换了量尺（第 27 单撞出来的既有缺陷）**：旧判据 `top<=ceil(n/9)+2` 量的是「最高频一类的次数」＝极值量，1500 颗种子实测**在改动前的老代码上就已 4.00% 击穿**（本单换 rng 流后 4.33%，分布几乎重合），照第 24 单「极值量换和式量」的治法改成**话题分布的熵 ≥ `TOPIC_ENT_MIN`=2.90 比特**（`PURE.entropy`，sim30 第②项本来就在用）；闸值由两头夹定（本体 4000 颗最小 2.9670；人为让一类占掉 25% ⇒ 熵 2.826、判红 92%，占掉 30% ⇒ 判红 100%），并就地附一条「对 25% 霸屏必须判红」的自证断言。
tools/fallback-pool：第 27 单两支诊断脚本（不进 gate.yml、不判红）。`wall.cjs`＝「离线 3 天 × 双种子」日志墙全文实录（本单验收主证据）；`capacity.cjs`＝池容量实测曲线＋定容量所依据的两个底数。**关键性质**：`pickV` 与 `pickFresh` 无论池多大都恰好消耗 1 次 rng，故把池截短再跑世界演化逐字节不变，曲线量的是纯粹的「容量→重样」关系。
PURE 块：`fmtStamp(t)`（第 19 单追加一）＝按时间排列的列表唯一时间戳格式函数，输出「D75 04:40」；新增此类列表一律走它，禁止直接用 `fmtTime`（门禁有反向断言）。
均在 SIM 块：ROOMS 与 ANCHORS（地图与锚点，label 细粒度＋s 粗粒度双标签）、MSGS（快捷短信）、REACT（模板反应，custom 为自定义短信兜底）、IDLE_THOUGHTS / SLACK_THOUGHTS / WORK_THOUGHTS（口水话池）、**DIARY_FB / CHAT_FB_OPEN / CHAT_FB_REPLY（第 27 单·兜底文案三池，逐人挂 workKind；原 `CHAT_LINES` 已整条撤掉）**、agents 四人卡（id a1–a4）。
w.stats 只读统计字段（act/pair/rain/pay/rentPaid/market/maxGap，SIM 块内只写不读）；sim30.js 顶部阈值常量区。
COOK/NAP/SLEEP/STROLL/MARKET/STORE_THRIFTY/STORE_GEN/STORE_PLAIN 独白池及其 pickV key（lc/ln/lz/lo/lm/lt/lg/lp）；agents.metab 逐人代谢参数。
存档常量：SAVE_KEY='citylife-save-v1'、BAK_KEY='citylife-save-v1-bak'、导出码前缀 'CLS1.'、SAVE_V=1。
agents.tempo 逐人磨蹭参数与 TEMPO_DEFAULT 兜底常量；w.saidDay 当日独白去重记账（随存档序列化，旧档缺省兼容；第 13 单挑剔起全城一本账、键不再挂人，旧档遗留 住户id|key 记录自然搁置）。
AI_TIMEOUT={direct:4000, relay:15000, batch:30000}（毫秒，可调）；AI 状态行新文案「换线路中…」「网络不通，已用模板兜底」。
DOM 层 AI 语言指纹模块：AI_VOICE 四人卡（底色＋招牌式定义）、OPEN_KINDS 开场四类、**DIARY_OPEN_KINDS 日记专用开场四类（第 18 单，全为写给自己的起手，零对话类目；与 OPEN_KINDS 同为 4 类是硬约束——styleAssign 措辞写死「四类」二字，且日记提示词「四段必须风格迥异」靠四人四类分配成立，harness 锁死两池均为 4）**、ZHAO_EVERY=5/SIMILE_EVERY=3 配额周期（可调；**第 18 单起 diary 挂点不发招牌许可**，aiTurn 照旧递增故短信/对白节律不受影响）、styleOffset 逐人相位、styleAssign 派活；agent 记账字段 aiTurn/lastOpenKind/lastUtterance（随存档，旧档缺省兼容）。
DOM 层 AI 文案层新增常量（第 18 单）：`SIT_MOOD`（处境档位→上卡心境标签，good/bad/flat 三条，可调）、`LEAD_INTERJ`（语气词起手表，条目可一字或两字；「哈」单字会误伤哈尔滨/哈欠一类实词故只收「哈哈」，可调）、`LEAD_SCAN=10`（只看开场前 N 字，可调）、`LEAD_TRIM`（开头引号/括号/空白剥离正则）、`leadsWithInterj`/`chatLeadBad`/`reOpenKind`/`redoLead`（方案乙闸的四个助手）。
DOM 层语气词闸补表（第 19 单追加二）：`LEAD_INTERJ_AMB`（歧义单字表，现仅「哈」，可调；成员只在后跟标点／空白／句末时判为语气词，后跟汉字即实词放行）、`LEAD_STOP`（判「语气词收尾」的标点／空白正则，可调）；`LEAD_INTERJ` 主表新增整词「哈喽」。**两表须零重叠**——歧义字同时留在主表会被单字直判、结构闸失效，harness 有断言卡死。新增歧义字的判据：该字作词首时**存在常用实词**（如哈→哈尔滨/哈欠/哈密瓜）则入歧义表，否则入主表。
SIM 块新增（第 18 单）：`TOPIC_POOL`（闲聊话题 9 类，写到「地点／事由」粒度即止、不写到具体物件，走文风口径可调）、`TOPIC_RECENT=4`（全城最近 N 次派过的类不再派，可调；**必须 < TOPIC_POOL.length**，否则无类可派）、`pickTopic`（派活＋顺延＋记账，每场恰好 1 次 rng）；均随 Sim 导出供门禁按真值断言。世界新增字段 `w.chatTopics`（全城话题记账，随存档序列化，旧档缺省从空账起、篡改档非数组就地重建）；chat 日志条目新增 `topic` 字段（对渲染惰性，logLine 只读 type/lid/t/name/text/thought；旧档条目无 topic → 提示词整句省略，回落 v28 行为）。
DOM 层同锚错开落位表（第 19 单）：`STAND_SPOTS`（锚名→四个 `[dx,dy]` 格偏移，现登记 7 个多人锚：home_table/home_tv/kitchen/store_counter/park_bench/river_walk/market；**单人锚一律不登记**＝bed1–4／desk1／desk2／home_desk／store_shelf，未登记者零偏移、显示落点与 v29 逐字一致）、`STAND_ZERO` 零偏移常量、`standSpot(ag)` 派活（按 `state.world.agents` 下标取模，陌生住户/陌生锚/空住户一律落回零偏移不抛错）。**每张表的条目数须等于住户人数**（现为 4，门禁按真值断言）。新增/迁移锚点时：多人锚必须补登站位表，否则门禁「多人锚全部登记站位表」一条即红。放样判据六条由 harness 锁死：脚底格非实体格（钳制恒为空操作）／精灵包围盒落在所属房间内／街市站位落 T 竖净道且不越岸线行 23／同房间可同时在场的落点两两 ≥1 格／**零「名牌压人身」组合**（近同列 |Δx|<1 格时纵向须错开 ≥2.5 格）／四人同锚必得四个互不相同的站位。
tools/voice-check：真模型验证协议（collect_diary.cjs / collect_sms.cjs / measure.cjs / 说明.md），提示词从 HTML 抽源码求值故与生产逐字节同源；工作目录 .work/ 已入 .gitignore。**求值环境量须与生产同步（第 17 单补记）**：两个 collect_*.cjs 用 `new Function('Sim','state', …)` 供给 `Sim` 与 `state`（形如 `{world}`，取卡前须 `vcState.world=w` 对齐），因生产 agentCard 读这两个 DOM 层环境量；日后 agentCard 若再引用新的环境量，必须同步补进这两处，否则工具在取卡时抛 ReferenceError（第 17 单实际踩中一次；**第 18 单又踩一次同款——agentCard 改读 SIT_MOOD，两处抽取表已同步补上**）。**同一手法第 18 单起也用于 harness**（从 HTML 抽 AI_VOICE/SIT_MOOD/hungerWord/styleAssign 块/方案乙闸/agentCard 六段源码求值），故 DOM 层 AI 文案层的门禁与工具**共用同一套抽取正则约定**：三条提示词（短信/对白/日记）必须保持「只依赖本函数形参的纯表达式」形态，**禁止把其中任何一段抽成临时变量**（抽出来的表达式会引用外部量，`new Function` 求值即 ReferenceError）——第 18 单给对白加话题行时特意把三元式写在拼接里而非抽 `topicLine`，代码注释已写明缘由。
第 17 单·外围角色事件表：`PEER_ROLE`（workKind→称谓四条）、`PEER_EVENTS`（四张表各 7 条＝3 好/3 坏/1 平，每条仅 k＋text）、`PEER_CHANCE=0.35`（可调；注意 decide() 上班期间约每 60 分钟才轮到一次，一个工作日仅 8 次左右机会，故 0.35≈97% 的工作日会抽一次）、`SIT_TTL=14*60`（可调；任务书口径 10–14 小时量级，补充指令一裁定取**上限 14 小时**。缘由：12 小时时事件多在 8–11 点命中、恰在 21:50 睡前日记之前到期，日记时刻挂载比例仅约 70%，而睡前日记是本单假说的主要观察窗口，丢三成样本会让假说验不干净。改到 14 小时后实测日记覆盖 93.3%/95.0%；**余下 5–7% 已非时效所致**，而是那约 6.7% 的人天压根没抽中事件，故再往上加也补不满)；均在 SIM 块，随 Sim 导出（PEER_ROLE/PEER_EVENTS/SIT_TTL/currentSit）供门禁按真值断言（门禁读 Sim.SIT_TTL，改常量不必改门禁）。住户新增字段 `ag.sit`（处境状态，随存档序列化，旧档缺省或 until 缺/坏→currentSit 返回 null）与 `ag.flags.peerDay`（当日封印）。
甲案新蓝图（第 11 单，v25）：地图幅面 MAPW=47/MAPH=24/STREET_Y=13，ROOMS 七间几何与 door.x、ANCHORS 十五点 x/y 全部按新蓝图落位（新旧对照全表见第 11 单 PR）；公寓包围盒 19×11（APT 常量），apartment.png 912×528，布局表见 docs/素材制作说明.md 第三章；放样规则文件 docs/云港建筑规范.md（与三件套同等效力）。
B 案城市骨架（第 16 单，v27）：**城市结构与用地的权威成文＝docs/云港城市总规.md**（放在哪里看总规，怎么放样看建筑规范；两者与三件套同等效力）。落地口径 MAPH 24→28（幅面 47×28，SIM 块内纯坐标常量）；ANCHORS.market 由 (28,13.5) 迁至广场中心 (22,19)——锚名/room/label/s 零改动，只迁 x/y。渲染层广场与水系几何常量集中登记于 DOM 层「云港城市总规 B 案骨架」区段：PLAZA（9×8 列 18–26 × 行 15–22）、PLAZA_WAY（T 口/T 竖净道 列 21–23 宽 3 格）、PLAZA_STALL（常设街市 ×3，列 18–19 行 15–20 贴排）、PLAZA_BROWSE（驻足带 列 20）、PLAZA_TALK（夜谈角 列 24–26 行 15–17）、PLAZA_TREE（江畔景观锚 列 24–26 行 20–22）、SHORE_Y=23（岸线步道贯通东西）、RIVER_Y=24（江面 4 行落地为南锚）。原摊位硬编码 24/28 已随迁并整格化，旧「周日摊位压动脉封店门」三重违规解除。
**楼身分组表（第 16 单，v27）＝DOM 层「楼身分组表与楼内连通」区段**：BUILDING_OF（房间 id→楼身 id，现仅 living/kitchen/bedroom→'apt'，表外房间一律按跨楼处理）、BUILDING_PLAN（楼身内部骨架：纵廊竖轴 hallX=12.1，各房 gate 内连门点与 hy 廊上行位；公寓纵廊＝列 11–12 × 行 7–11，门厅横轴行 10）、BUILDING_BODY（楼身包围盒由成员房间坐标推导，ROOMS 改坐标自动跟随，无第二处硬编码）。走线与 SIM 无关，door.via 字段零触碰。配套素材侧已补开两道内连门洞（客厅东墙 x=11 行 9–10、厨房西墙 x=13 行 10–11，各 2 格），登记于 build_assets.py 的 INNER_DOORS 并与本表逐一对账；缘由与实测见总规文档第五章第 1 条。
像素画风开关文案「像素画风/已启用/素材未就位」；素材基址常量 ASSET_BASE='assets'（同源相对路径，素材路径＝ASSET_BASE+'/apartment.png' 与 ASSET_BASE+'/characters.png'，即仓库 assets/ 目录；改回独立素材站只改本行）；署名行「美术素材：Modern Interiors / Modern Exteriors by LimeZu（limezu.itch.io）」；characters.png 布局约定（PIX_IDX 行序 a1–a4 每人先 idle 后 walk 两行、每行 24 帧＝右/上/左/下×6）与公寓贴图覆盖区 APT / 接管房间表 PIX_ROOMS（均在 DOM 层像素画风模块）。characters.png 八行铁律与 apartment.png 布局表为渲染契约（成品适配代码，代码不迁就成品）；tools/asset-pipeline/build_assets.py 顶部配方区（RESIDENTS 部件表、FLOOR 与家具坐标）为挑剔通道唯一改动点；私仓地址与附件下载串仅存在于施工会话与决策者操作中，仓库零出现。PIX_FURN_TALL（高件家具矩形+脚底世界 y）与 PIX_SOLID（实体格集合）及 pixStandPos 钳制助手（DOM 层像素画风模块，补充指令三）：人物与高件家具按脚底 y 统一排序绘制形成遮挡，人物显示脚底落入实体格时就近钳制到可站格（仅显示，SIM 坐标零改动）；两表须与 tools/asset-pipeline/build_assets.py 的 GAME_FURN_TALL / GAME_SOLID_CELLS 登记表保持一致（流水线跑完会打印对照）。落位显示经平滑器（v.dspX/v.dspY 渲染态，补充指令四）：移动中零钳制显示＝SIM 插值原样，静止才按登记落位，切换按 (5.5×speed+6)格/秒 限速短程滑动、禁跨格瞬现；>3 格跳变（初始化/传送）直置豁免。

## 踩坑备忘（改码前必读）
- **阈值不许拿「一次实测值 × 一个倍数」定，要拿分布定（第 23 单立，门禁通用口径）**：`RAIN_MAX=59` ＝ 49×1.2（PR #4 逐字自述「留 20% 余量」），落在一个变异系数只有 4.7% 的量上，算出来的余量就是 **+2.76σ**——5 万颗种子里击穿 40 颗，**它从写下那天起就是红的，只是那两颗门禁种子还没抽到**（第 22 单那份 42 颗复测没看见，因为按 0.08% 的击穿率 42 颗抽中的概率只有 3%）。同理：**别把别的项目的阈值抄过来**——40 抄自 hezuriji 的「2~40 条」，而云港自家设计就是 45 场/30 天，抄来的那一刻就已越界。正确做法见「指标与退化检查」区的余量口径条。**反面写法：「实测 49，那就定 59」。**
- **量尺选错了，余量再大也是白搭；先问「这个量变了，玩家看得见的东西变没变」（第 23 单立，门禁通用口径）**：雨次数与雨的体感在时长这一维上**反相关**——把一场雨时长加倍，城市湿一倍，雨次数反而从 52/57 掉到 **37/42，闸更绿**。病根是次数只数**边沿**，而雨在这个世界里是一个**持续状态**（雨幕挂着，`decide()` 全程不读 `w.weather`，没有任何住户因为下雨改过任何一个决定）。**判别法：看这个量在玩家那头对应的是状态还是事件——状态就量时长占比，事件才量次数。** 反面写法：因为 `w.stats` 里现成有个计数器，就拿计数器当闸。附一条同源提醒：**「连续下雨天数」这类量在本作恒等于 30（天天下雨），立成闸就是一条永远绿的闸**，定量尺前先看它在基线上有没有方差。
- **给世界加「日间波动」时，成因不能只挂高频状态（第 22 单立，模拟侧通用口径）**：剪辑层要的是「比自己平常早／晚 ≥40 分钟且 z≥1.5」。若波动只由一条约 43% 人天都成立的状态（挂着逆境）驱动，分布会变成**两个等高的峰**——偏离量被均值吃掉一半、标准差却被撑到最大，z 永远够不到 1.5，**看起来摊开了，判据一条也命中不了**。正确形状＝**常见的成因幅度小、罕见的成因幅度大，并且允许叠加**；再补一条**由前一天推出来的罕见成因**（本单用「昨晚睡饱了 → 今晚睡不了那么久」，两晚成链），尾巴才出得来。
- **验「成因推不推得动」要立在排期函数上，不能立在涌现结果上（第 22 单立，门禁通用口径）**：实际躺下时刻＝max(今晚排期, 手上这件活儿干完)。首版门禁固定世界只换处境档位、再推到入睡那一拍比时刻，结果那天他忙到比三档排期都晚，**三档落在同一拍上**，比出来的是活动时长不是成因（三条断言齐红）。改法＝把排期函数导出来直接读它的输出，环境量（时刻／体力／小憩记账）全部固定，只换一个变量。**另配一条反向自查**：把规律性系数压到 0，断言同样的成因一分钟也推不动他——一条恒绿的闸等于没立。
- **「最不像平常的自己」不能用绝对阈值去量（第 21 单立，选材层通用口径）**：绝对阈值判据（钱包见底 <¥60、跟三个人都聊、饥饿越过 88）描述的是**长期状态**，而**长期状态恰恰就是「平常的自己」**——判据问的是落差，绝对阈值答的是水平，两者在「天天如此」的人身上恰好反号。实证：第一版 62 天里沈小满占 42 天、白一鸣一颗种子零上榜，因为她工资 300、大方、餐餐 ¥20，钱包**天天**见底（命中率 0.57）、外向使 `decide()` 社交分支恒真故**天天**跟三个人都聊（0.73）。**治法＝逐人记每条绝对判据的命中率，≥0.5 即判为此人常态、不再计入他的落差**；关键细节：命中率账记的是**候选**而非折除后计分的那份，否则一折除命中率就再也变不动、闸自锁。另一条同源教训：**取「峰值／谷值」这类极值时先问「那一刻人醒着吗」**——陆知秋每天早上以「饥饿 94」入选，实为睡梦中的代谢曲线（睡眠期饥饿仍以 0.35/拍上涨），而任务书那两条写的是「饿着**没吃饭**」「体力见底还在**硬撑**」，说的都是醒着的时候。
- **给一段代码写「本段零 X」的注释，会把断言「本段零 X」的门禁自己打红（第 21 单立，门禁通用口径）**：源码原文类断言（走位规矩三先例）扫的是整段文本，注释也在里面。CLIP 段注释里成段写着「零 rng 消耗」「不写 w.stats」，于是三条断言全部被自己的说明文字命中。**判据要立在代码上就必须先剥注释**，并**另加一条「剥注释后代码仍完整」的断言**——剥过头会让下面几条断言变成空转，那比红更危险。
- **无头模拟里「按天分桶」的天界要跟人的作息对齐，不能想当然取 0 点（第 21 单立）**：本作夜猫子 00:00–00:50 才睡（`sleepAt` 为 24:30 已越过 1440，`mod>=sleepAt` 恒假，实际靠 `mod<wakeAt` 那一支在过零点后触发），按 0 点分桶会把「他今天几点睡」记到第二天头上。**先量最晚就寝与最早起床，再在两者之间取切点**（本作取 04:00：实测最晚就寝在切点后 1250 分、最早起床在切点后 130 分），并把「每桶每人恰好一次入睡、一次醒来」立成门禁断言，日后谁改作息参数撑破区间即红。
- **给异步收尾定死线，先问「玩家是拿什么单位在数这段时间的」，再问「这把尺子会不会被浏览器冻住」（第 29 单立，AI 层与门禁通用口径）**：决策者报的是「占位符挂了约 8 个游戏小时」，而代码里所有死线都写成毫秒——`AI_TIMEOUT={direct:4000,relay:15000,batch:30000}`＋`jobLimit=2×死线`＋`callClaude` 还要试两轮，单条对白的协议上界是 `30000×2+800=60800ms`；主循环写着 **`1 真秒 = 10 模拟分钟 × 速度`**，故 1× 下 **1 游戏小时＝6 真秒**，60800ms **＝ 10.1 游戏小时**——**病症不必假设「请求真的死了」就已被协议自己完整解释**，只是从来没人把这个数换算到玩家那头去看过。**判别法：死线的单位必须至少换算一次到玩家感知的单位再复核；游戏内时间被加速时，倍速越高这条越致命**（本作 `w.speed` 可到 64×，那时 15 秒的中转死线就是 10 个游戏小时）。第二条同源教训：**死线本身不许用 `setTimeout` 判**——浏览器对后台标签页把定时器节流到秒级、隐藏 5 分钟后进一步降到分钟级，**那正是「长挂」的另一条独立路径**；改把死线放进主循环按真实时钟判（`loop()` 里紧跟 `drainLog`），页面隐藏时循环不跑但墙也没人看，回到前台第一帧就把过期的全收干净。**反面写法：「超时 15 秒够用了吧」＋「拿 setTimeout 兜底」。**
- **「开机时收一次尾」不等于「有人收尾」——清道夫必须覆盖会话中途（第 29 单立，通用口径）**：第 26 单的 `settleOrphanLLM` 是照「关页把在途调用带走」这一种孤儿写的，全站唯一调用点在 `if(bootWorld)` 分支里；而三个 AI 挂点当时都**没有 `try/finally` 罩住 `llmPending`**，函数体内任何一处抛异常（DOM 查询、`noteUtter`、`pushLog`、以及 fire-and-forget 的 async 函数**无人 catch 的 rejection**）就把那条永远停在 `llmPending=true` 上。更狠的是它**没有第二次机会**：`drainLog` 的闸是 `!e.llmPending`（卡在 true 就永远进不来），游标 `logSeenLid` **只增不减**（已上过墙的不会被第二次遍历到）——**双重永挂**。**判别法：给「在途」类标志位写收尾时，逐条列出它可能在哪些时刻被置上，再问每个时刻对应的清道夫是谁；只答得出「开机那次」的，就是漏了会话中途。** 配套：置标志位的那一句与登记进收尾表的那一句必须**紧挨着**，且排在一切可能抛异常的语句之前——登记不上就没人给它收尾。
- **异步回包写墙之前先问「这条是不是已经落定了」，落定之后到的一律丢弃（第 29 单立，AI 层口径）**：占位符被兜底句顶上之后，迟到的 AI 回包若照写不误，玩家会**看见字自己变了**——比一直挂着占位符更吓人。治法是把「落字」收口成一个返回布尔的函数（`penReady`，已落定即返回 `false`），并且**副作用一并归它管**：短信的回信／已读不回、对白两人的「记在心里」全部挪进只在本次落字真的赢了时才跑的回调里，否则会出现「内心独白是兜底句、回信却是 AI 写的」这种自相矛盾的一屏。**反面写法：只判文本要不要覆写，副作用照跑。**
- Artifact 的 fetch 桥不支持 AbortSignal：给该 fetch 传 signal 会使请求整体失败。超时只能用 Promise.race。
- text-overflow:ellipsis 对 flex 容器无效，必须落在内层文本节点上。
- 周期性 innerHTML 重建会吞掉进行中的点击（按下与抬起之间节点被销毁）。一律建一次、原地更新。
- 信息流只在读者本就位于底部（距底 <60px）时自动跟随，否则出"↓ 回到最新"按钮。
- 事件时间戳必须在触发时定死；禁止等异步回包后再创建条目（睡前日记曾因此变成早晨日记）。
- iOS：输入框 font-size ≥16px 防聚焦缩放；软键盘弹出压矮视口，布局模式判定在输入聚焦期间冻结。
- 100dvh＋safe-area-inset 全覆盖；宿主背景不可信，html / #app / #content / 画布全帧各自铺深色。
- 手机竖屏顶栏：仅地点一格可伸缩（粗粒度地名），其余项 flex:0 0 auto 永不收缩。
- **卡上给标签，不给事件（第 18 单立，AI 文案层通用口径）**：凡是「给模型一条具体信息、同时叮嘱它不许说出来」的设计，一律不成立——这是**结构问题不是措辞问题**。角色卡上当天带戏剧性的具体信息越稀缺，模型越会把仅有的那条拿去复述；第 17 单把禁令写到「不许直接提起它，更不许解释自己为什么这样」的强度，实证仍被逐字搬进日记。正确形状＝**只注入模型需要知道的那一层抽象**（要它换语气就只给语气档位，不给引发语气的那件事），把原文留在世界状态里供别的用途。加强措辞与降低注入强度都是同一条死路上的两步（后者反而让抓手更稀缺）。
- **「不重叠」的判据不能只算精灵，得连名牌一起算（第 19 单立，渲染层通用口径）**：第 19 单第一版站位表把客厅第 3、4 位排成上下两格同列，几何自检全绿（间距 2 格、脚底格合法），目验一看**还是叠着**——精灵分开了名牌没分开：名牌画在精灵顶（脚底 y −2 格），靠下那人的名牌正好落在靠上那人胸口。凡是「屏幕空间固定尺寸的附着物」（名牌、气泡、状态图标）都不随世界缩放，**几何间距达标不等于观感不叠**。判据已入门禁：同房间可同时在场的两点，近乎同列（|Δx|<1 格）时纵向须错开 ≥2.5 格；站位表因此一律优先横排，只有横向摆不下时（如广场 T 竖净道仅 3 格宽）才走多排并加大纵距。
- **只印 HH:MM 的列表必然跨天混叠（第 19 单立，渲染层通用口径）**：条目按绝对时刻严格升序，渲染只取 `t%1440` 就把日期丢了，屏上便呈现为「时间倒流」（决策者实证：短信往来记录 04:40/05:40/06:30/03:30/04:20…，实为三个不同日子）。判据＝**列表容量 ÷ 每日条目数 > 1 天即会混叠**，按此判，全站五处列表全部中招，其中城市日志可跨 9.5 天、比报案的短信记录（5 天）更严重。**新增任何按时间排列的列表，时间戳一律走 `PURE.fmtStamp`。**另记一条：**修这类同源缺陷时，先看是不是共用同一个渲染器**——三处列表共用 `logLine`，改渲染器一处即三处齐修，反而比「只修报案那一处」（要给渲染器加旗标传参）diff 更小。
- **不许给模型两条会打架的指令（第 18 单立，同上通用口径）**：模型会精确执行冲突指令里的一条，然后看起来像是「不听话」。第 13 单已记过一次（招牌许可与派定类目互斥），第 18 单又栽一次——`OPEN_KINDS` 里两个对话类目被原样派给日记挂点，等于代码在命令模型把日记写成对话体，而提示词另一边写着「日记是写给自己的」。**凡冲突源在代码里，就在代码里掐掉，不要写条款让模型自行调和。**
- **走位三铁律（第 20 单立，渲染层通用口径；门禁＝`walkgate.js`，CI 强制）**：瞬移是重复犯病——补充指令三、四、第 19 单各栽过一次，每次都靠人眼看出来、靠交付件写一句「已知接受项」放行，下一单照样复发。故本单改立机器可查的闸，三条：
  - **规矩一·逐帧位移上限**：正常运行（非首帧落位、非 reduceMotion 直置）下，显示坐标相邻两帧位移 ≤ `行走速率 5.5 格/秒 × 速度档 × dt × 1.05`；绝对上限 1.155 格（`dtFrame` 封顶 0.1 秒 × 速度档最高 2×）。**上限要绑在「人腿能走多快」上，别拍绝对数字**——绝对数字换个帧率就失效。豁免只有两处且必须打显式 `warp` 标，门禁另断言 warp 次数恰等于住户人数，免得「打个标就能瞬移」变成新后门。实测最大 0.092 格（60fps 1×），本单前 4.671 格。
  - **规矩二·全程不得穿实体**：判据取 `PIX_SOLID`（＝`build_assets.py` 的 `GAME_SOLID_CELLS`，墙带＋家具占格的权威登记，比拿家具绘制矩形当禁区精确）。**移动全程与静止落位一视同仁**——第 19 单只校验静止，于是「走的时候横穿餐桌」漏了一整年。校验须两层：逐帧脚底格 ＋ 逐条走线折线按 0.02 格采样（只查逐帧会漏，单帧步长 0.09 格可能跨过薄片而不落在里面）。**另记一条易踩**：竖向墙体不在 `PIX_SOLID` 里（登记表只收墙带与家具占格），所以「不踩实体格」不蕴含「不穿墙」，避实体改线必须单独断言航点落在房间内。
  - **规矩三·显示位置只有一个来源**：显示坐标不得由「寻路结果」与「渲染期偏移」两处叠加。判据立在**源码原文**上（`draw()` 对站位表零引用、`v.dspX/dspY` 只有一处写入、`standSpot` 只有一个调用点）。**为什么不能只立行为断言**：行为断言只能证明「这一版没跳」，挡不住下一单再写一次「绘制期贴个偏移」——只要偏移小于旁路阈值就抓不到。防写法就得断言写法。
  - **配套两条经验**：①**别按距离猜意图**。原平滑器写「位移 >3 格即视为传送、直置」，而站位偏移最大 4.61 格，恰好从这道缝整个溜过去（实测 30–69 次/3 天）。要区分「传送」与「走路」，就让上游显式打标，不要让下游按数值猜。②**闸立完必须反向自查**：把病态写法复演一遍，断言它确实被判违规。一条恒绿的闸等于没立（`walkgate.js` 末段即此用途）。
- **量尺反相关是通病，不是雨一家的毛病；立闸前先做「把它变好，闸会不会反而变红」这一测（第 24 单立，门禁通用口径）**：第 23 单在雨上发现「时长加倍→次数下降→闸更绿」，本单一测，六项里**还有两项同病**——①**街市**：每趟停留 ×3，广场上人·分钟 755→1570（热闹一倍有余），**到访人次反而 14.9→13.7**；②**动作熵**：睡眠拉到 14 小时（一件事吞掉大半天），时长熵 2.079→1.956（真的更单调），**次数熵反而 2.096→2.195**。病根全是同一个：**`w.stats` 里现成的计数器数的是 `decide()` 派活的次数，而玩家看见的是那件事持续了多久**——一件事拖长，派活次数必然变少。**操作化的判别法**：新立或复核任何一条闸，必须跑一组「把这件事变好」的扰动，断言闸读数往绿的方向走；**往红走就是量尺反了**。反面写法：因为 `w.stats` 里现成有个计数器，就拿计数器当闸。
- **「均值 ±5σ」那条口径只对和式量成立，对极值量不成立（第 24 单立，门禁通用口径）**：第 23 单立的余量口径默认这个量的尾巴按正态收敛，**取最大／取最小的量不收敛**——同一批 5 万颗里「最长无戏间隔」的实测最大值落在 **+7.93σ**（拆掉雨兜底后 +10.12σ、「距上一条闲聊最长」+12.28σ），而同批的和式量（占比、总数）全在 ±4.6σ 以内。于是照口径给它算出来的上限 1050，**比现行的 1080 还红**（击穿 29 颗 vs 11 颗）。**治法是把量尺换成和式量（时长占比），不是给口径开例外。** 判别一句话：**这个量是「加起来的」还是「挑出来的」？挑出来的就别拿 σ 外推。**（例外：`MONEY_FLOOR` 也是极值量，但它是**红线**不是退化探针——红线按定义就该问最坏情形，故保留极值口径，只把阈值按分布重定，并接受它的尾巴比正态重。）
- **闸要判「这段时间里有没有发生 X」，就别去读「日志尾条是不是 X」（第 24 单立，门禁通用口径）**：`html:1557` 判的是 `w.log` 的**最后一条**是不是闲聊，于是一场真的闲聊只要被后面任何一条行动日志顶下尾条，计时器就当它没发生过。这正是 `GAP_MAX` 右尾的成因——击穿最深那颗种子（47351，报 1210 分钟无戏）**那段时间里城市推了 31 条日志、含一场真的闲聊**。正确写法＝**按发生记账**：本拍新增了哪些条目就看哪些条目。**另附一个当场踩到的坑**：`w.log` 是 400 条环形缓冲（`html:882–884`），**按数组下标做增量扫描会漏条**（本单第一版实测把 30 天的闲聊数从 173 漏成 57）；要按 `w.lidSeq` 的增量算「这一拍新增了几条」，再从数组尾部取那几条。
- **「复演主循环」的门禁，必须把「主循环里哪几步是有条件执行的」一并复演（第 25 单立，门禁通用口径）**：第 20 单的 `walkgate.js` 复演主循环时，把 `stepDisplay` 无条件写在 `updateWalkers` 后面——**等于默认主循环每一步都无条件执行**。而生产代码里 `stepDisplay` 的唯一调用点长在 `draw()` 内，`draw()` 又挂在 `#scr-live.active` 下：**一切页，真实位照走、显示位冻结**，攒到 36.35 格再由平滑器按直线补掉，穿墙穿家具。**三条铁律一条也没响，不是因为落进了哪个豁免（`v.warp` 全程未置位，「归进豁免分支」的假说实测证伪），而是因为这些帧从来没被生成过。** 两条可迁移的教训：①**判据对不等于闸有效——判据 × 采样范围才是闸**；立完闸要再问一句「生产代码里有哪些分支是我的复演循环走不到的」。②**「落差」本身就该立成判据，别等它变成超限位移**：那 36 格落差被限速器切成每帧 0.19 格，**在任何单帧里都不违反铁律一**；直到新增「真显差恒零」这条才抓得住。另附一条同源提醒：**平滑器的追赶速率若大于位移上限判据，二者之差就是一个只在积压时才显形的缺口**——本作 `cap=(5.5×速度+6)×dt` 比铁律一阈值 `5.5×速度×dt×1.05` 快 1.47–1.99 倍，正常运行下限速器从不是瓶颈，故这道差平时完全看不出来。

- **「每单收工记得改一下」这种规矩，没有机器闸就等于没有（第 28 单立，流程通用口径）**：构建版本号（`html:405` 的 `<span id="set-build">`）从第 1 单起就是「一单动了游戏代码就涨一位」，v12→v33 连着二十几单一次没错过；但它**全仓零 reader**——没有任何 JS 读它、没有任何断言消费它、`gate.yml` 也不校验它，**它是一个纯粹靠人记得的数**。于是第 25、26、27 单**连着三单漏涨**：第 25 单改了渲染层（+25/−1）、第 26 单新增 CATCHUP 段（+188/−6）、第 27 单改了 `decide()` 又新增 132 条文案连世界指纹都变了（+279/−18），三单都停在 v33。**没有任何人做错事**——三份交付件开头都自洽地写着「代码基线：v33」，与当时 HTML 里那个数一字不差；错的是这个数已经不再跟踪任何东西了，同一个 v33 先后挂过**四份不同字节的产物**，而「v33 基线＝世界指纹 `d19035af…`」这条活规程（交接说明「指纹分两个数」条）也因此同时指向两个不同的指纹。**三条可迁移的教训**：①**零 reader 的量必然漂移**，一个没人读、没人验的数，漂了也不会有任何反馈回来告诉你；②**「文档与代码互相自洽」不等于「对」**——这三单的文档和代码是完全一致的，一起错而已，故自洽性检查抓不到这类病，只有拿**外部事实**（HTML 字节的哈希）对表才抓得到；③**漏账会自我掩盖**：第 25 单漏了之后，第 26 单照抄「代码基线：v33」反而显得是对的，第 27 单再照抄一次，**越漏越像常态**。治法＝`versiongate.js`（宪法第 27 条），判据不是「提醒施工记得改」而是「把号和字节绑死，对不上就红」。
- **闸的反向自查能拿真实历史喂就别造合成场景（第 28 单立，门禁通用口径）**：第 20 单立下「闸立完必须把病态写法复演一遍」的规矩，历次照做时都是**造**一个病态场景。第 28 单发现更强的一种做法：`versiongate.js` 的反向自查直接拿 git 里的六个真提交对喂——第 25／26／27 单三单是真实的「改了没涨号」样本（判红），第 22→23、23→24 是真实的「没动 HTML 所以正确地没涨」样本、第 21→22 是真实的「动了且涨了」样本（判绿）。**好处有三**：证的不再是「这道闸能拦住我编的病」而是「这道闸当初就该拦住那三单」；正反两面都由历史给出，不存在「构造得太巧所以恒绿」的嫌疑；且这些样本永远不会过期。**代价一条要登记**：它依赖 git 历史可达，浅克隆（`actions/checkout` 默认 `fetch-depth: 1`）下取不到，故 `gate.yml` 须显式写 `fetch-depth: 0`，取不到时退回合成场景并**如实印 SKIP，不许静默变绿**。

## 指标与退化检查
- 采集：SIM 块 w.stats 只读累计（活动计数/闲聊对/雨/发薪/交租/街市/最长无戏间隔），严禁被模拟逻辑回读。**第 23／24 单起，门禁要量而 w.stats 里没有的量，一律在门禁侧逐拍纯观测（只读世界字段、不写 w.stats、不碰 rng），不给世界加字段**——加字段等于改世界指纹。现由此法采集的有四个：雨占全时长、动作熵（时长口径）、街市滞留人·分钟、静默占比。
- 无头判定：sim30.js 固定双种子 30 天六项检查，CI 强制；阈值放宽须经任务书授权并在 PR 已知接受项列 原值→新值→实测值。
- **指纹分两个数（第 24 单起，宪法第 26 条，往后所有单按此复报）**：**世界指纹**＝`node worldsig.js`（双种子 30 天世界状态签名的 md5，输入里不含任何门禁打印文字，只在 SIM 行为或 rng 流真动了时才变；**v33（第 22–24 单）＝`d19035af7477984608b376115cb04e24`；v34／v35 沿用该值不变；v36（第 27 单起）＝`2d2662383a628ac1a72ddbab47d8b5ef`**）；**门禁指纹**＝`node sim30.js | md5sum`（连门禁措辞都算在内；第 22 单 `484e5cb2…`、第 23 单 `613ff573…`、第 24／25 单 `a4d65e6a…`、第 26 单 `2e371176…`、**第 27 单起 `6c8f768a71d3f45fb99bc8328c15679a`**）。**改门禁措辞时门禁指纹必变、世界指纹不变，这是正常的，不得判成「疑似动了世界」。** 世界指纹的盲区（不含日志原文、活动标签、busyUntil、sit、rest、flags、clips、rngState）写在 `worldsig.js` 文件头；**只改 SIM 文案的单须另附 SIM 块源码 md5**（v33＝`91e3dadb…`、v35＝`c015937e…`、**v36＝`029ec0c39e362e9ba7f54165d6939a0c`**，命令见该文件头）。**本条的版本号绑定由第 28 单补正**：原文把三个数一律绑在「v33」上，而 v33 这一个号先后挂过四份不同字节的产物（第 22–24 单一份、第 25／26／27 单各一份），绑定因此失真——这正是立第 27 条涨号铁律的直接动因。
- **阈值余量口径（第 23 单立，本仓此前无成文口径）：阈值＝大样本实测「均值 ±5 个标准差」，再向外取整。**
  **锚必须下在均值与标准差上，不能下在实测最大值上**——最大值随样本量长（雨占比 3000 颗时 16.25%、5 万颗时 16.74%），拿它当锚每重算一次就换个答案；均值与标准差在每组样本上都稳到三位数。取整一律向外，故取整只加余量不减。样本量要跑到尾部收敛为止（本单 5 万颗；3000 颗时最大值还在长，1 万颗与 5 万颗已同值）。**反面写法＝「实测 49，那就定 59」**（`RAIN_MAX` 原值即 49×1.2，落在变异系数 4.7% 的量上算出来只有 +2.76σ，5 万颗里击穿 40 颗）。**新立或重定阈值后必须做反向自查**（照走位三铁律先例）：构造若干会让它变红的人为改动逐个实测判红率，并同时喂未改动的基线断言零误红。
- **口径的适用边界（第 24 单补，撞出来的）：「均值 ±5σ」只对「和式量」成立，对「极值量」不成立。** 取最大／取最小的量尾巴不按正态收敛——同一批 5 万颗里，旧的「最长无戏间隔」实测最大值落在 **+7.93σ**（拆掉雨兜底后 +10.12σ），而同批的和式量（占比、总数）全在 ±4.6σ 内；于是照口径给它算出来的上限 1050 **比现行的 1080 还红**（击穿 29 颗 vs 11 颗）。**治法是把量尺换成和式量（时长占比），不是给口径开例外。** 判别一句话：**这个量是「加起来的」还是「挑出来的」？挑出来的就别拿 σ 外推。** 例外：`MONEY_FLOOR` 也是极值量，但它是**红线**不是退化探针，红线按定义就该问最坏情形，故保留极值口径、只按分布重定阈值。
- **sd＝0 的量怎么定阈值（第 24 单立）**：标准差为零的量（闲聊对数恒 6、发薪恒 4、交租恒 1），「±5σ」这条带塌缩到常数本身，阈值就取那个常数；取 `≥` 而非 `＝`，以便将来住户增员时仍成立（交租那条写成 `===1` 是既有先例，因它是「恰逢一次」的日历口径）。
- **六项现行量尺与阈值（第 24 单全面复核后；每条的立法本意与实测写在 `sim30.js` 常量区）**：

  | 项 | 量尺 | 阈值 | 阈值距均值 | 换过尺？ |
  |---|---|---|---|---|
  | 最低钱包 | 最小值（红线） | ≥−110 | −6.94σ | 否（阈值按分布重定） |
  | 动作熵·最低一人 | **活动时长占比**的熵 | ≥2.0 比特 | −6.03σ | **第 24 单换**（原为派活次数） |
  | 财富基尼 | 终局截面 | ≤0.50 | +5.47σ | 否（阈值重定） |
  | 闲聊总数 ／ 对数 | 次数 ／ 集合基数 | ≥120 ／ ≥6 | −5.47σ ／ 恒定 | 否（阈值重定） |
  | 雨 | 雨占全时长比例 | 10%–18% | −5.72σ／+6.03σ | 第 23 单换（原为雨次数） |
  | 发薪 ／ 交租 | 次数 | ≥4 ／ ＝1 | 恒定 | 否，一字未动 |
  | 街市 | **滞留人·分钟** | ≥100 | −5.03σ | **第 24 单换**（原为到访人次） |
  | 最长无戏间隔 | **「静默>200 分」时长占比** | ≤27% | +5.90σ | **第 24 单换**（原为 max，已被击穿 11/50000） |

- **换尺的三条，病根都是同一个：`w.stats` 里现成的计数器数的是 `decide()` 派活的次数，而玩家看见的是那件事持续了多久**，一件事拖长派活次数必然变少，于是闸与体感**反相关**。三条实证：雨时长加倍→次数 52/57→37/42（闸更绿）；街市每趟 ×3→人·分钟 755→1570 而人次 14.9→13.7（下限闸反而离红更近）；睡眠拉到 14 小时→时长熵 2.079→1.956 而次数熵 2.096→2.195（闸更绿）。**新立或复核任何一条闸，必须跑一组「把这件事变好」的扰动，断言闸读数往绿走；往红走就是量尺反了。**
- **换尺后旧口径读数仍原样打印**（雨次数与场均、街市人次与趟均、旧的最长无戏间隔），只是不再当判据——这样设置页「城市体检」显示的旧口径与门禁行仍然对得上。
- 游戏内观察：设置页「城市体检」区实时显示同一套指标（熵/基尼由 PURE.entropy / PURE.gini 计算）。

## 存档
- 三态：normal（浏览器本地自动留档，15 秒/页面隐藏/发短信后落盘）；ephemeral（URL 带 ?seed，只玩不写档，仅可导出）；noStore（工件等无 localStorage 环境，功能整体禁用）。
- 启动：normal 态读档失败时原串移入 BAK_KEY 后开新城，绝不静默覆盖坏档。
- 导出码＝'CLS1.'＋base64(serialize)；导入经 hydrate 校验，成功即落盘并整页重载。
- 存档口径（第 26 单取证逐字记档）：顶层 `{sv:1, meta, world}`；`meta` 三键 `selected/lastReflectDay/at`（**`at` 即「上次落盘的真实时刻」，第 6 单起就在存**）；`world` 19 键（`t/speed/agents/log/lidSeq/credits/sentToday/stats/weather/lastSpark/rentPaidDay/seed/rngState/saidDay/chatTopics/clips/clipBase/clipDay/fbRecent`；**`fbRecent` 为第 27 单新增，旧档缺省从空账起**）。`serialize` 是 `for(k in w)` 逐键拷贝只跳过函数，`hydrate` 只核 `sv`／`world`／`isFinite(w.t)`／`agents` 非空四项再按 `rngState` 重建 `w.rng` 闭包。体积：3 天 36.6 KB／30 天 85.5 KB（日志 400 条环形缓冲，不随天数无限长）。
- **离线追帧（第 26 单）**：开机读 `meta.at`，按 **1 真实分钟＝1 模拟分钟**（`CATCHUP_MIN_PER_MIN`）折算应补拍数，封顶 `CATCHUP_MAX_DAYS=3` 模拟日，一口气 `Sim.catchUp` 推上去。边界一律向「少补」倒：不足一拍／`speed===0`（暂停中离开）／时钟倒流／坏 `meta` 一律 0 拍。补算期间**零 AI、零渲染**（见下「AI 层」与 walkgate 位置判据），只按「AI 挂掉时那条路」补两样：跨过「夜深了」落一轮模板日记、被读掉的短信补一条「已读不回」。**本单零新增存档字段，`SAVE_V` 不动，旧档直接可用。**

## 部署（EdgeOne Pages）
- 平台：EdgeOne Pages 国际站（edgeone.ai）免费档；Pages 项目连接本仓库 main 分支，合并即自动部署（约 2 分钟）。
- 加速区域：全球可用区（不含中国大陆）。已证实：该区域的平台默认域名从中国大陆访问必返 401（合规设计，非故障），因此必须绑定自定义域名；此区域绑域名免 ICP 备案。
- 构建设置：框架预设 Other；根目录 ./；输出目录填 .（一个点，切勿填 /，会部署出空包 404）；构建命令与安装命令留空。
- 访问入口：自定义子域名（决策者名下 cydgames.com 的子域，DNSPod 托管，具体域名见 EdgeOne 项目配置）；游戏路径 /city-life-framework.html；中转函数路由 /relay ← functions/relay.js（与站点同源）。
- 密钥铁律：DEEPSEEK_API_KEY 只存在 EdgeOne 项目环境变量；仓库、聊天记录、截图零密钥；疑似泄露立即在 DeepSeek 后台作废旧钥换新钥。
- 连通自检：浏览器打开 https://子域名/relay?test=1 ，返回含「通」即链路正常。

## 当前状态
当前构建版本 **v36**（见本文件第 2 行；对照表 `version-ledger.json`）。下一步候选见 待办.md。公开发行时 LLM 通道需整体替换（见待办·模型分层，callClaude 为唯一替换点）。**本行原写「基线 v12 已入库」，停在第 1 单从未跟进，由第 28 单更正。**
第 29 单（**v37**，v36 → v37；**未动 SIM 块**：只改 DOM 层「文案什么时候上墙」与门禁；**世界指纹 `2d266238…` 逐字节不变**，SIM／CLIP 块 md5 均未变）日志墙落字顺序与占位符收尾**已落地待验收**：决策者线上实测两条病症——①时间戳靠后的对白先写完上墙、靠前的还挂着「（聊得正起劲⋯）」，像时间倒流；②D110 19:50 与 20:10 两条到 D111 06:40 仍是占位符，**挂了约 8 个游戏小时**。**取证六问先出报告再下刀，坐实两条独立根因**：①三个挂点（`enhanceMessage`／`enhanceChat`／`runReflection`）都是 fire-and-forget 的 async 函数、各自在函数尾部直接 `patchLid` ⇒ **谁先回来谁先贴**；打乱顺序的岔路至少五条（并发上限 2、语气词闸重生成要再排一次队、`callClaude` 首轮 null 后 `sleep(800)` 再排一轮、直连转中转两级死线差 11 秒、日记 `prio` 插队）。②收尾时限＝协议上界 `jobLimit×2+800`＝**60800ms**，而 `1 真秒＝10 模拟分钟`，合 **10.1 游戏小时**——**与实测的「约 8 个游戏小时」正好同量级，不必假设请求真的死了就能完整解释**（叠上重生成一轮上界翻倍到 20.3 游戏小时，日记 batch 到 40 游戏小时）。**第 2 问的明确结论：会话中途出错的孤儿没有人管**——`settleOrphanLLM`（第 26 单）全站唯一调用点在 `if(bootWorld)` 分支里，**只管开机**；而三个挂点都没有 `try/finally` 罩住 `llmPending`，函数体内任何一处抛异常就永远停在 `llmPending=true`，且 `drainLog` 的闸是 `!e.llmPending`、游标 `logSeenLid` 只增不减 ⇒ **双重永挂**。**治法**（决策者裁定方向：按时间顺序落字，等不到就用兜底句顶上）＝新增 DOM 层 `pen`（落字排队）段：`penAdd` 在占位符挂上墙的同时登记（按 `e.t,e.lid` 升序，**排在一切可能抛异常的语句之前**），`penFlush` **只从队头连续放行**（前一条没落字后面的按住不贴），`penSweep` **在主循环里按真实时钟判死线**（`PEN_TTL_MS=20000`；**刻意不用 `setTimeout`——后台标签页会把它节流到分钟级，那正是长挂的另一条独立路径**），`penReady` 对已落定的条目返回 `false` ⇒ **迟到回包整包丢弃、绝不覆写**（连副作用一并不跑）。三个挂点补 `try/catch/finally`，收尾从此覆盖超时／报错／非 200／解析失败／promise 永不 settle／函数体抛异常／页面切走后回来／关页在途（第 26 单原路未动）全部形态。兜底**只认 `e.fb`**（第 27 单落条目那一刻就抽好的那条）——死线到点是「网络说了算的时刻」，在那里掷骰子会让 rng 流跟着网络快慢漂移，故 **pen 整块一次骰子都不掷、一次 AI 都不调**（两条均已立断言，第 26 单「离线期间零调用」不受影响）。**沙箱实测（虚拟时钟，真实时间零消耗，抠生产源码原文求值）**：乱序回包情形上墙序由 `20:10→19:50→20:50→20:30` 纠正为 `19:50→20:10→20:30→20:50`；永不返回情形占位符最长存活 **60800ms → 20000ms（10.1 → 3.3 游戏小时，降 67%）**；报错情形 800ms 前后不变。门禁新增**四条闸**（落字顺序／占位符不长挂／迟到不覆写／**三条各自反向自查**——病态写法当场判红、合规写法不误伤）＋结构侧断言（三挂点各三条＋主循环位置＋零骰子零 AI＋占位符与 `e.fb` 成对写出）＋篡改档不抛错四条。**零新增入档字段**（`e.llmPending`／`e.fb` 改前就在），旧档由 `settleOrphanLLM` 开机收尾后被 `aiFloorLid` 挡在 AI 之外、不进登记表。交付件＝`docs/交付/第29单-日志墙落字顺序与占位符收尾.md`（含取证六问逐条回答、三情形前后对照实测、四条闸的反向自查实证）。**留给决策者一项**：3.3 游戏小时是否仍偏长——`PEN_TTL_MS` 单点可调，但下调到 15000ms 以下会开始砍掉中转线路的正常回包。
第 27 单（**动了 SIM 块**：新增「兜底文案扩池」段、改写 `decide()` 闲聊分支、撤掉 `CHAT_LINES` 与 `pick`；**rng 流位移，世界指纹 `d19035af…` → `2d266238…`，任务书授权**）兜底文案扩池**已落地待验收**：离线三天回来，日志墙上那 12 条睡前日记**两两不同且四人各写各的**（改前 12 条逐字全同），闲聊**逐场不同**（改前 4 组模板摊 15–25 场、最高频一组占 40–60%）。口径层比形状层要紧——旧那句「（写了两行，没写下去，合上了本子。）」是在告诉玩家「这里本该有内容」，而离线期间本来就不该有 AI，那不是缺失，那就是那天的日子；故新池写成真的日记真的对白，且**内容只许用结构上必然为真的素材、不许提任何具体事件**（与第 18 单「卡上给标签不给事件」同一条口径）。抽法分两支（闲聊 `pickV`／日记 `pickFresh`，缘由：日记一天只抽一次，当日去重对它恒为空转），容量按实测定（日记窗口＝日志墙同时挂得住的单人日记条数上限 10；闲聊照第 13 单口径 ≥ 单日抽取峰值）。**顺带治了一处既有缺陷**：harness「无话题霸屏」旧判据是极值量、改动前老代码上就已 4.00% 击穿，照第 24 单法换成话题分布的熵 ≥ 2.90 比特。交付件＝`docs/交付/第27单-兜底文案扩池.md`（含日志墙全文实录，主证据）。
第 26 单（**动了 SIM 块**：新增 `/*CATCHUP-START*/…/*CATCHUP-END*/`；`Sim.step` 逐字未动，**世界指纹 `d19035af…` 逐字节不变**）离线追帧一期**已落地待验收**：城市从此在页面关着的那段时间「补得回来」。**取证先行五问**（`tools/offline-catchup/probe.cjs`）：①`Sim.step` 的唯一驱动是 `loop()`、`loop()` 的唯一驱动是 rAF，故 rAF 停＝世界停；且 `dtFrame=Math.min(0.1,…)` 把恢复时的大 dt 削成 1 模拟分钟/帧，**靠大 dt 自动追帧这条路本来就是堵死的**。②`w.t` 与 `w.rngState` 随档落盘，重开从存档时刻继续；「上次关闭的真实时刻」（`meta.at`）**第 6 单起就一直在存**，本单零新增存档字段。③推进代价实测（node v22，含 hydrate）：1/3/7/30 天＝1.8/3.3/4.9/**12.5** ms 中位——**补 30 天比一帧还短，故任务书第二优先档「耗时不可接受」的触发条件没有成立**，封顶 3 天的理由是待办已议定＋`sim30` 只验到 30 天的验证边界＋玩家一次看得完，**不是性能妥协**。④模板兜底逐处：闲聊 4 组（`pick`，无当日去重）、日记**单条常量没有池**（连跑三天 12 条逐字全同）、短信读时 `REACT` 5 条、回信路径落「已读不回」。⑤同一份存档推同样拍数逐字节可复现（比对面含全部日志原文），故补算可以进门禁。**治法**＝开机读 `meta.at` → `Sim.catchUpPlan` 换算（1 真实分钟＝1 模拟分钟，封顶 `CATCHUP_MAX_DAYS=3`）→ `Sim.catchUp` 一口气推上去 → 落 `aiFloorLid` 水位线 → **跨夜且真出了新剪辑**则 `setScreen('clip')` 并挂横幅标明覆盖天数与被跳过的天数。**三条红线各有断言**：生存不变量——`sim30` 新增「从存档补算 30 天」模式与「从零跑」同等对待，双种子共 38 条全绿（唯一读数差是 424242 静默占比 24.65%→24.54%，成因＝补算按 AI 挂掉那条路每晚多落 1 条 sys＋4 条 diary，而 sys 在静默口径里算「有戏」，属预期）；零 AI——SIM 块结构上无 fetch＋`rawCallClaude` 补算窗口闸（可计数）＋`drainLog` 水位线，harness 用出网陷阱实数：**补算 30 天出网 0 次**，实机七档 AI 统计与独立计数**全部为 0**；零渲染层动作——补算排在 `state.vis` 建表之前、第一帧之前，`walkgate` 新增情形 ⑧ 与位置判据（正反各喂一次不恒绿），并反向复演「运行中补算」量出**凭空多走 13.4 格**；第 25 单八种情形 × 四条断言全绿、`v.warp` 白名单逐字未动。**正向审计带出一处一并改齐**：`e.llmPending` 随档落盘，本单立了水位线之后那些条目会被永远钉在占位符上，故加 `settleOrphanLLM` 照「AI 挂掉那条路」收尾（零新增文案）。门禁指纹 `a4d65e6a…` → `2e371176…`、SIM 块 md5 `91e3dadb…` → `c015937e…`（两者**预期必变**）。交付件＝`docs/交付/第26单-离线追帧一期.md`（含五问机理报告、两条换算口径的缘由、三条红线证据、实机七档表与 6 张实机截图 `docs/交付/第26单-图/`）。**决策者四项裁定已全部落实（2026-08-06）**：①换算口径 1:1 确认维持；②封顶 3 天确认维持，交付件里「封顶不是因为算不动」那句按裁定保留；③**边界情形改判据**——由「跨过夜就推」改为「**跨过夜 且 真出了新剪辑**才推，没有新卡就别推」，已改并复测七档（`B-away-60min` 由「剪辑页·0 张卡」改为「现场页·不出横幅」，其余五档读数不变）；④日记 12 条全同接受现状、另立第 27 单专治。**裁定三落地时有一处记档偏离**：判据取的是「最新剪辑日变没变大」（`clipTopDay`）而非字面的「`w.clips` 条数增量」——条数被 `CLIP_KEEP=60` 封着（`html:1474`），过了 60 个剪辑日之后增量恒为 0，照字面实现会让这条呈现规则**在第 61 个剪辑日悄悄失效且不报错**；裁定要的效果逐字守住，只是量它的尺子换了一把，harness 有 5 条断言钉住（含「饱和后条数增量恒为 0」的反面证据）。
第 25 单（代码基线 **v34**——原写「仍 v33」，第 28 单补正，**只动渲染层与门禁**）切页返回直线飞越排障**已落地待验收**：在「现场」页看角色活动 → 切到「日志」页停一会 → 切回，**全部角色从离开那一刻的旧位置沿直线飞向此刻该在的位置，穿墙穿家具**。病根**不在寻路、不在平滑器的豁免分支**，而在**一句判据的位置**：`stepDisplay`（显示位推进）的唯一调用点长在 `draw()` 内，而 `draw()` 挂在 `#scr-live.active` 下；`updateWalkers` 与 `Sim.step` 却在 `loop()` 里无条件跑。切页 ⇒ 真实位照走、显示位冻结，最大拉开 **36.35 格**（横跨整张地图）；切回时平滑器把积压当普通追赶，按直线以 `(5.5×速度+6)` 格/秒 划过去，实测单帧 **0.19167 格＝铁律一阈值的 1.99 倍**、脚底落进实体格最多 **35 帧**。**铁律一没拦住的原因是采样范围而非判据**——`walkgate.js` 的 `sample()` 每帧都调 `stepDisplay`，等于默认「显示推进从不停摆」，这些帧从来没被生成过；`v.warp` 全程未置位，「归进豁免分支」的假说实测证伪。治法落任务书第一优先档的**更强形式**：把显示推进从 `draw()` 提到 `loop()`（新增 `stepAllDisplays`），**让落差压根不产生**（每帧 `|真实位−显示位| ≡ 0`），而不是「切回时补一次直置」——后者必然要引入第三个 warp 来源、逼得第 20 单「warp 恰等于住户人数」那条断言放宽；本方案 `v.warp` 置位处**逐字未动**。第二优先档的「离开时长阈值」因**不存在直置也就不存在突兀**而无适用场景，未引入任何新常量。门禁扩：`walkgate.js` 新增「渲染停摆普查」**七种情形 × 四条断言**（真显差恒零／逐帧位移上限／零穿实体／直置零出白名单），停摆模型由源码读出而非写死——**把显示推进挪回 `draw()` 即当场变红（已真跑回归演练：4 FAIL、退出码 1）**。普查结论：全站**只有「切页」一处**把真实位推进与显示位推进拆到了两个开关上，其余八种（标签页切走/最小化、失焦、存档载入、暂停恢复、reduceMotion、倍速切换、对话框、主线程长任务）要么整条主循环一起停、要么根本不停，故本来就不飞，但均已逐条立断言。**已变基到第 24 单合并后的新 main（`1d23ae1`）；相对新 main 三个数一个也没动**——世界指纹 `d19035af…`、门禁指纹 `a4d65e6a…`、SIM 块 md5 `91e3dadb…` 均逐字节不变（门禁指纹相对旧基线 `613ff573…` 的变动全部来自第 24 单换量尺，按宪法第 26 条属预期）。**sim30 六项阈值一行未改，`sim30.js`／`worldsig.js`／`宪法.md` 三个第 24 单禁区文件 diff 为空。** 交付件＝`docs/交付/第25单-切页返回直线飞越排障.md`（含普查表、逐字归因、反向自查实证、前后对照连续帧）。
第 24 单（代码基线仍 v33，**游戏代码零改动、世界指纹 `d19035af…` 逐字节不变**）sim30 六项阈值全面复核**已落地待验收**：一次扫完六项，落成 3 处换量尺（动作熵→时长口径、街市→滞留人·分钟、最长无戏间隔→「静默>200 分」时长占比）、4 处按 5 万颗分布收紧阈值（钱包 −123→−110、基尼 0.75→0.50、闲聊 20→120、对数 3→6）、3 处一字未动（雨／发薪／交租）。**两条决策者裁定同时落实**：①裁定一「指纹拆成世界指纹／门禁指纹两个数」——采集器落成 `worldsig.js`，并按宪法第 10 条入档为规程第 26 条；②裁定二「雨密度接受现状、不动导演层」——待办该条销账，另开「天气回流进行为」一条不排期。**`GAP_MAX` 的右尾成因查清且不指向真实缺陷**（那段「无戏」里城市推了 31 条日志含一场真闲聊，是 `html:1557` 只看日志尾条所致），故未触发停手报告。门禁指纹 `613ff573…` → `a4d65e6a0dd1947d3329691c13538698`（预期必变）。交付件＝`docs/交付/第24单-sim30六项阈值全面复核.md`。**无须决策者拍板的事项**——两项裁定已落实；只有 6 条已知接受项与 5 条范围外发现待过目（均为登记性质，其中「街市那条闸检出率偏低」与「PURE.gini 对负资产不成立」已登记待办）。
第 23 单（代码基线仍 v33，**游戏代码零改动**）雨密度余量复核**已落地待验收**：sim30 第五项的量尺由「雨次数 1–59」换成「**雨占全时长比例 10%–18%**」。取证结论三条——①**这条闸不是余量薄，是已经红了**：5 万颗种子实测 `RAIN_MAX=59` 击穿 40 颗（0.080%），零代码改动、只换颗种子（580→61、644→60）门禁就红；②**立法本意查到了**（PR #4 逐字 `49×1.2=58.8 → 取 59`，初值 40 抄自 hezuriji `scripts/sim-check.mts` 的「2~40 条」，而云港自家设计就是 1.5 场/天＝45 场，一落地就注定顶穿），本意是防「导演层跑飞」的退化检查、**不是防体感太密**；③**「次数」不是合适量尺**：把一场雨时长加倍、城市湿一倍，雨次数反而 52/57 → **37/42，闸更绿**——次数只数边沿，而雨在这个世界是持续状态（雨幕挂着，`decide()` 全程不读 `w.weather`）。故落任务书第一档「换量尺」，**`RAIN_MAX` 一个字未改**（是被整条替换，不是被放宽，逐字遵守待办那条红线）。新阈值余量 **−5.72σ／+6.03σ**，5 万颗零越界（旧闸只有 +2.76σ）。反向自查：17 种人为改动，新闸拦下 13 种、旧闸只拦下 5 种。交付件＝`docs/交付/第23单-雨密度余量复核.md`。**须决策者拍板三项**（均在待办「等决策」区）：①**指纹口径请拆成「世界指纹／门禁指纹」两个数**——现行口径是门禁自己输出的 md5，故「换量尺」与「指纹逐字节不变」字面上无法同时满足，本单即第一例（世界行为逐字节未变、门禁指纹 `484e5cb2…` → `613ff573…`）；②**`GAP_MAX=1080` 同样已被击穿 2/10000**（最大 1110），病因与雨那条一字不差，按硬口径**一个字未改**，是否另立单；③**雨到底密不密**（天天下雨、白天 27.35% 挂着雨幕、1.74 场/天 vs 设计 1.5 场/天）——属决策者「用户视角终裁」，施工不代行。
第 22 单（v33）作息人格**已落地待验收**：四人起床时刻由 30 天零方差变成逐人分布（标准差 54.0／26.6／10.5／44.2 分），第 21 单七条零命中判据转正四条，剪辑大类覆盖作息 3→19 天、生存 5→24 天、薄卡 26→3 天。交付件＝`docs/交付/第22单-作息人格.md`。**须决策者拍板一项**（原两项中的雨次数一项已由第 23 单复核结案）：顾云帆被挑中 20/60（33.3%，改前四人 15/15/16/14）是否算偏。
第 21 单（v32）每日剪辑**已完成并结项**：判据经决策者验收认可，一期（选材）落地，本单只做「挑」不做「写」、零 AI。交付件＝`docs/交付/第21单-每日剪辑一期.md`，含 60 天选材全表（主证据）、四人被挑中分布 15/15/16/14、「有几天值得看」读数（强 65%／中 25%／弱 10%）、七条零命中判据逐条交代、七张实机截图。**收工时决策者三项裁定（见该文件 11.2，均只改措辞、代码零改动）：①薄卡照出，不设下限不合并——把薄卡藏起来等于用剪辑层掩盖世界的贫瘠，问题还在只是看不见了，留着是个持续的提醒；②二期不做 AI 润色——短板不在文笔在素材，给本来没事的日子配好文笔是第 18 单「素材越稀薄模型越硬凑」的翻版；③剪辑页不加筛选，四人规模不需要。三项共同指向：剪辑层到此为止，下一步该动的是这个世界本身**（即待办第 10 条「作息人格」与第 21 单登记的两条范围外发现——起床时刻零方差、清醒饥饿体力被 `eatAt`/`napAt` 夹死）。
第 18 单（v29）AI 文案层返工待决策者目验；该单交付件＝`docs/交付/第18单-AI文案层返工.md`，其第七章逐条列明「哪几条只能目验」（共 3 条）与「哪几条机器已背书」（共 4 条）。
第 19 单（v30）已落地待决策者目验，含三项：①人物落位重叠排障（原单）②时间戳跨天混叠（追加一）③语气词闸补「哈」（追加二）。交付件＝`docs/交付/第19单-人物落位重叠排障.md`，含四问机理报告、15 个锚点的全量同占普查表、全站五处时间戳列表排查表、「哈」判据边界表与误伤自查实测用例、前后对照实机截图（`docs/交付/第19单-图/`）。三项**全部为纯显示层或纯代码判据，SIM 零改动、rng 零位移，sim30 指纹逐字节不变**。追加二改了 `redoLead` 一句举例（属提示词），**真模型复测未执行**——沙箱出网 403，照第 17/18 单先例照实登记于待办。
