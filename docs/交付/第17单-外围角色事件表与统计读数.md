# 第 17 单 · 外围角色一期：事件表全文与统计读数

对应分支 `claude/peripheral-roles-work-status-uoahhw`，PR #21。
本文件为呈报交付物（交接说明「交付物一律落成仓库文件」规矩的首件）。
含补充指令一两项裁定：SIT_TTL 14 小时、好坏配平。

---

## 一、外围角色四个（不露面）

| 住户 | 职业 | workKind | 外围角色 |
|---|---|---|---|
| 顾云帆 | 程序员 | `work` | 组长 |
| 沈小满 | 便利店店员 | `clerk` | 店长 |
| 陆知秋 | 交易员 | `trade` | 客户 |
| 白一鸣 | 自由撰稿 | `write` | 编辑 |

硬口径：不进 ROOMS/ANCHORS、不在地图上、不走路、不进日志墙、不进角色页、不进短信。
外围角色不得获得内心世界／日程／语言指纹／画像——他们是事件源，不是人。
每条事件仅 `k` + `text` 两字段，harness 断言卡死（多一个字段即红）。

**称谓核查**：`客户` 已见于 `WORK_THOUGHTS.trade`（"客户来电，语气比行情刺激。"），
`编辑` 已见于 `WORK_THOUGHTS.write` 与 a4 的 `fear`（"被编辑连环催稿"）。
`组长`、`店长` 全文零出现，系任务书直接指定的职务称呼、非新造人名，照任务书采用。

---

## 二、事件表全文（24 条）

`k`：`good` 顺 ／ `bad` 逆 ／ `flat` 平淡

### 组长 → 顾云帆（程序员）　2 好 / 3 坏 / 1 平

| k | text |
|---|---|
| bad | CR 被组长打回，评语写了三行 |
| good | 负责的那版顺利上线，组长说很稳 |
| bad | 被组长塞了个加急，今晚得晚走 |
| bad | 组长转来改过的需求，昨天写的白写 |
| good | 被组长在群里点名夸了一句 |
| **flat** | 组长一天没派活，没消息 |

### 店长 → 沈小满（便利店店员）　3 好 / 2 坏 / 1 平

| k | text |
|---|---|
| bad | 排面没理齐，被店长说了两句 |
| good | 今天的排面被店长夸了 |
| good | 店长把周日的班调开了 |
| good | 店长塞来两盒临期便当，晚饭有着落 |
| bad | 盘点对不上，差了三样 |
| **flat** | 店长一天没来店里，没消息 |

### 客户 → 陆知秋（交易员）　3 好 / 2 坏 / 1 平

| k | text |
|---|---|
| bad | 客户在电话里发了火 |
| good | 客户追加了一笔 |
| bad | 客户提出要撤单 |
| good | 客户发来一句谢谢 |
| good | 方案一次过，客户没提意见 |
| **flat** | 客户一天没来消息 |

### 编辑 → 白一鸣（自由撰稿）　2 好 / 3 坏 / 1 平

| k | text |
|---|---|
| bad | 编辑来催稿了 |
| bad | 稿子被编辑退了回来 |
| good | 专栏过稿了 |
| bad | 编辑压来一篇明早要的急稿 |
| good | 编辑夸了这篇的开头 |
| **flat** | 编辑一天没来信，没消息 |

### 好坏配平口径（补充指令一）

裁定为「好坏对半」，但 **6 条 ＝ 1 平 ＋ 5 条好坏，5 为奇数，逐人对半在算术上不可能**。
既然「具体分配你定」，落法取 **全城对半**：

| | 好 | 坏 | 平 | 合计 |
|---|---|---|---|---|
| 组长 → 顾云帆 | 2 | 3 | 1 | 6 |
| 店长 → 沈小满 | 3 | 2 | 1 | 6 |
| 客户 → 陆知秋 | 3 | 2 | 1 | 6 |
| 编辑 → 白一鸣 | 2 | 3 | 1 | 6 |
| **全城** | **10** | **10** | **4** | **24** |

每人恰好 1 条平淡档、每人 6 条不变（未动池容量口径）。
翻面的两条选在店长与客户两表——组长与编辑两表系原任务书点名给过示例形状
（组长：CR 打回／上线成功／临时加班／需求变更／没消息；
编辑：催稿／退稿／过稿／临时加急／没消息），原样保留。
harness 三条断言锁死：全城好坏相等、共 24 条含 4 平、24 条文案互不重复。

---

## 三、触发与处境状态口径

- **触发**：只在该住户当天工作时段内，一天至多一次（`flags.peerDay` 封当日，封后当日零 rng 消耗）。
  随机源一律 `w.rng()`，SIM 块内无 `Math.random`（CI 强制）。
- **命中率**：`PEER_CHANCE=0.35`。`decide()` 上班期间约每 60 分钟才轮到一次
  （`setActivity` 置 `busyUntil=+60`），一个工作日仅 8 次左右机会，故 0.35 ≈ 93% 的日子会抽一次。
- **处境状态** `ag.sit = {k, from, text, i, until}`：每人至多挂 1 条，新的顶掉旧的；
  `until = w.t + SIT_TTL` 到点自然消失（不做定时清扫，按 `w.t` 判活）。
- **时效** `SIT_TTL = 14*60`（补充指令一裁定取任务书 10–14 小时口径的上限）。
- **存档**：随 `serialize` 全量入档；旧档无 `sit`、或 `until` 缺/坏 → `currentSit` 返回 `null`，绝不判坏档。
- **防连日复读**：撞上次抽中的那条则顺延一条（照 `styleAssign` 撞类顺延先例），恰好消耗 1 次 rng。

### 唯一去处：AI 角色卡的一行

无状态时**整行省略**（不留空字段）。行内写死禁令措辞，落实「状态不许被念出来」：

```
｜今天心里压着的事:店长塞来两盒临期便当，晚饭有着落（这是背景底色，只影响你的语气和你挑什么说；不许直接提起它，更不许解释自己为什么这样）
```

不上日志墙、不进 `personalLog`、不进角色页。

### 真实生产提示词实证

由 `tools/voice-check` 抽生产源码求值产出，非手写。两种形态都实证过：

```
a1 = 顾云帆（程序员）｜…｜正在:小憩｜说话方式:…                      ← 当天无处境，整行省略
a2 = 沈小满（便利店店员）｜…｜今天心里压着的事:店长塞来两盒临期便当，晚饭有着落（这是背景底色…）｜说话方式:…
a3 = 陆知秋（交易员）｜…｜正在:在家待着｜说话方式:…                   ← 当天无处境，整行省略
a4 = 白一鸣（自由撰稿）｜…｜今天心里压着的事:编辑一天没来信，没消息（这是背景底色…）｜说话方式:…
```

---

## 四、统计读数（30 天双种子，SIT_TTL = 14 小时）

| | 种子 20260803 | 种子 424242 |
|---|---|---|
| **睡前日记(21:50)挂载比例** | **112/120 ＝ 93.3%** | **114/120 ＝ 95.0%** |
| **① 组合去重种数** | 逐拍 **140** 种 / 4320 拍<br>日记时刻 **29** 种 / 30 天 | 逐拍 **152** 种 / 4320 拍<br>日记时刻 **30** 种 / 30 天 |
| **② 四人同挂同一状态** | 同一条文案 **0** 拍<br>同一档 **0** 拍 ＝ **0.0%** | 同一条文案 **0** 拍<br>同一档 **109** 拍 ＝ **2.5%** |
| **③ 每人日均挂载时长** | 顾 13.07h / 沈 12.60h / 陆 13.53h / 白 13.07h | 顾 14.00h / 沈 13.53h / 陆 12.60h / 白 13.07h |

### 12 小时 → 14 小时 前后对照

| | 12 小时（前） | 14 小时（现） |
|---|---|---|
| 日记时刻挂载比例 | 73.3% / 69.2% | **93.3% / 95.0%** |
| ① 逐拍组合去重种数 | 140 / 152 种 | 140 / 152 种（不变） |
| ① 日记时刻组合去重 | 29 / 30 种 | 29 / 30 种（不变） |
| ② 同一档占比 | 0.0% / 0.6% | 0.0% / **2.5%** |
| ③ 每人日均挂载 | 10.80–12.00h | **12.60–14.00h** |

**读数说明**

- 日记覆盖从约七成抬到约九成半。**剩下的 5–7% 已不是时效造成的**，而是那约 6.7% 的人天
  压根没抽中事件（见「已知接受项」①）——即 SIT_TTL 已不再是限制项，再往上加也补不满。
- ② 同一档占比由 0.6% 升到 2.5%（仅种子 424242）。两个原因叠加：挂载时间变长使重叠概率上升，
  好坏配平到 10/10 后同为 `good` 或同为 `bad` 的机会也变大。绝对值仍很低。
- ②「同一条文案」恒为 0 属**结构性**——四张表互不相交（harness 有断言），
  四人不可能挂同一条文案，故一并按「同一档」口径复算，那才是真正的趋同指标。
- ① 组合去重种数不随 TTL 变化：组合的多样性由抽取决定，时效只影响每种组合持续多久。

---

## 五、门禁

固定六步全绿（CI `gate` 同套自动跑）：

| # | 项 | 结果 |
|---|---|---|
| 1 | `node --check app.js` | 通过 |
| 2 | `node harness.js` | **ALL PASS**（本单新增 27 条断言） |
| 3 | `node sim30.js` | **30D ALL PASS**（双种子，**阈值一行未动**） |
| 4 | `functions/relay.js` 语法 | 通过 |
| 5 | SIM 块纯净（无 `Math.random`） | 通过 |
| 6 | `tools/voice-check` 语法＋实跑 | 通过 |

### sim30 指纹

```
原值（main d6bb5a8）：       e9f5cd49a394421bc33bc574049458a1
新值（本分支，含补充指令一）：bae21c5cd512347205efb531f5a24096
```

**补充指令一的两项改动未再次改动指纹**——与校正提交的值逐字节相同。这不是漏跑，是可解释的：
`SIT_TTL` 只改写入 `ag.sit.until` 的数值，事件表翻面只改条目的 `k`/`text`，
二者都不改变 rng 调用次数（仍是概率掷点＋下标抽取共 2 次），也不回流进任何行为分支。
sim30 采样的是 `w.stats` 与四人 money/anchor/hunger/energy，均不含 `sit`。
**这反过来实证了处境状态是纯附加、不反馈行为的**——与本单「只动三处、便于归因」的立意一致。

### 六项逐项（原值 → 新值），无一项调阈值

| 项 | 阈值 | 20260803 | 424242 |
|---|---|---|---|
| 动作熵/人 | ≥1.0 | 2.37/2.39/2.12/2.41 → 2.34/2.37/2.13/2.38 | 2.35/2.38/2.12/2.38 → 2.35/2.36/2.13/2.38 |
| 财富基尼 | ≤0.75 | 0.50 → 0.50 | 0.49 → 0.49 |
| 社交 | ≥20 次/≥3 对 | 163 次·6 对 → 163 次·6 对 | 155 次·6 对 → 165 次·6 对 |
| 雨次数 | 1–59 | 52 → **54** | 49 → **52** |
| 发薪 | ≥4 | 4 → 4 | 4 → 4 |
| 交租 | =1 | 1 → 1 | 1 → 1 |
| 街市 | ≥4 | 15 → 16 人次 | 16 → 18 人次 |
| 最长无戏间隔 | ≤1080 | 880 → 810 | 800 → 780 |

**按「逼近即报告」登记两项**（均未击穿、未放宽）：

- **雨次数是六项里离阈值最近的一项**：`RAIN_MAX=59`，现 54/52，余量仅 5–7
  （main 基线已是 52/49）。成因是导演层雨密度本就偏高——待办「导演层雨密度体感复核」条
  所记"30 天约 45–49 场"的旧估值与实测已有偏差。后续任何动 rng 流的改动都可能顶穿它。
  已写死**不得靠放宽 RAIN_MAX 解决**，建议随雨密度体感复核一并裁定。
- **发薪恰好等于下限 4**：确定性（30 天窗口只含 4 个周五），main 基线同样为 4，与本单无关。

### 浏览器实机自检

Chromium 实跑 `?seed=20260803`：无 JS 异常；日志墙**外围事件泄漏 0 条**
（正则扫全部条目正文与独白）；日志墙其余内容照常，非整体静默；构建版本显示 v28。

---

## 六、已知接受项

1. **约 6.7% 的人天当天抽不中任何事件**（实测 30 天命中 93.3%）。成因：`decide()` 上班期间
   约每 60 分钟才轮到一次，一个工作日仅 8 次左右机会。这部分人天既无「有戏」也无「平淡」档，
   角色卡整行省略。与「一天至多一次」口径不冲突；若要「没戏也必须是平淡档」，
   需改为当日定点必抽。
   > 施工判断留痕：`PEER_CHANCE` 初值曾取 0.2，实测漏抽率达 18.3%，等于在事件表的「平淡」档
   > 之外**另掺一份静默的没戏**，与「便于归因」的立意相悖，故调到 0.35。
2. **同一事件跨天可复现**：只保证相邻两次不复读，不做更长窗口去重
   （6 条池子做长窗口去重会显著压低随机性）。

---

## 七、等决策

1. **本单假说待验真（核心，必须回收）**：结构侧统计支持分化——日记时刻四人处境组合
   29/30 与 30/30 天各不相同，同档时间占比 0.0%/2.5%。但**说话像不像四个人，机器验不了**，
   须目验。若试玩后仍趋同，下一步候选是让处境状态改变**行为**而不只是进角色卡（即接目标系统）。
2. **铁律 1 的验收只能靠目验**：卡上禁令措辞已写死，但桩测试只验条款写没写清，
   **验不了模型守不守**（第 13 单同款教训）。目验判据：产出里若出现
   「因为被退稿了所以我今天……」这类播报式复述即为不合格，后手是改卡上措辞或降低注入强度。
3. **真模型复测未执行（照实登记，未绕过）**：交接说明铁律要求「每次改提示词必须跑
   voice-check 真模型复测」，本单改了 `agentCard`（属提示词），铁律适用。但线上通道
   （DeepSeek 中转站）施工沙箱出网 403，**AI 产出的实际观感本单测不了**；按任务书要求，
   **未用 Claude 直连结果冒充线上表现**（本单一次模型调用都未发生）。
   工具已修好并跑通到出提示词那一步。

---

## 八、范围外发现（登记不改，宪法第 19 条）

`decide()` 的工作时段判定**没有星期检查**——`inWork` 只看 `mod` 与 `start`/`end`，
四人周六周日照常上班，故外围角色事件周末同样触发。
既有行为、非本单引入，只登记不顺手改，已入待办。
